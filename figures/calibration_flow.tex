\begin{tikzpicture}[
  src/.style={draw=gray!60, fill=white, rounded corners=2pt,
    minimum width=3.6cm, minimum height=1.05cm, align=center},
  blk/.style={draw=gray!70!black, fill=white, rounded corners=2pt,
    minimum width=3.6cm, minimum height=1.05cm, align=center},
  sel/.style={draw=gray!70!black, fill=white, rounded corners=2pt,
    minimum width=3.6cm, minimum height=1.05cm, align=center},
  sig/.style={-{Latex[length=2mm]}, thick, gray!55!black}
]
  \node[src] (plane-data) at (0,2.0)
    {$P_1$--$P_3$\\same marked face corner};
  \node[blk] (plane-fit) at (5.0,2.0)
    {three-point plane fit\\$p_s,\ R_{\mathrm{surface}}$};
  \node[sel] (plane-check) at (10.0,2.0)
    {$P_4$ held-out check\\$0.82\,\mathrm{mm}$};

  \node[src] (tool-data) at (0,0.0)
    {$T_1$--$T_3$\\flat-face yaw poses};
  \node[blk] (tool-fit) at (5.0,0.0)
    {invariant-direction fit\\$n_{\mathrm{EE}}$};
  \node[sel] (tool-check) at (10.0,0.0)
    {$T_4$ held-out check\\$0.50^\circ$};

  \node[blk, minimum width=5.2cm] (runtime) at (7.5,-2.0)
    {run-time geometry\\$n_T=R_{\mathrm{EE}}n_{\mathrm{EE}}$ relative to $n_s$};

  % Routing. The run-time box spans x = 4.9 to 10.1 and y = -2.525 to -1.475,
  % so the two feed-in runs are kept on separate heights and separate columns:
  %   y = -1.00  the tool chain, entering the box from the north;
  %   y = -2.00  the plane chain, entering the box from the east;
  %   x =  12.6  the plane chain's descent, clear of the T_4 box at x = 11.8.
  % An earlier version dropped the tool chain to y = -1.45, which laid that run
  % along the top edge of the run-time box.
  \draw[sig] (plane-data) -- (plane-fit);
  \draw[sig] (plane-fit) -- (plane-check);
  \draw[sig] (tool-data) -- (tool-fit);
  \draw[sig] (tool-fit) -- (tool-check);
  \draw[sig] (plane-check.east) -- (12.6,2.0) -- (12.6,-2.0) -- (runtime.east);
  \draw[sig] (tool-check.south) -- (10.0,-1.0) -- (7.5,-1.0) -- (runtime.north);
\end{tikzpicture}
