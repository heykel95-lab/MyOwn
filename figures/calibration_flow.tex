\begin{tikzpicture}[
  % Every row reads: physical pose(s) -> calculation -> quantity obtained.
  % text width fixes the box width, so no line can overflow its frame.
  stg/.style={draw=black, line width=0.5pt, rounded corners=2pt,
    text width=3.15cm, minimum height=1.7cm, align=center,
    font=\footnotesize, inner sep=3pt},
  res/.style={stg, line width=1.0pt},
  sig/.style={-{Latex[length=2mm]}, thick, black},
  % Row headings, centred over the first box of each row.
  row/.style={font=\footnotesize, anchor=south}
]
  % ---- Row A: surface-reference construction ------------------------------
  \node[row] at (0,5.85) {Surface-reference construction};
  \node[stg] (a-in)  at (0,4.6)
    {Tool face seated flat\\Record one EE pose};
  \node[stg] (a-geo) at (3.9,4.6)
    {Determine $p_s$, $n_s$\\[1pt]
     {\scriptsize$n_s=R_{\mathrm{EE}}e_{z_0}$}\\[1pt]
     {\scriptsize$p_s=p_{\mathrm{EE}}+R_{\mathrm{EE}}r_{\mathrm{face,EE}}$}};
  \node[stg] (a-frm) at (7.8,4.6)
    {Construct $t_1$, $t_2$ and\\[1pt]
     {\scriptsize$R_{\mathrm{surface}}=[\,t_1\;t_2\;n_s\,]$}};
  \node[res] (a-out) at (11.7,4.6)
    {Surface reference\\in base frame\\[1pt]
     {\scriptsize$p_s,\ n_s,\ R_{\mathrm{surface}}$}};

  % ---- Row B: tool-normal calibration -------------------------------------
  \node[row] at (0,2.55) {Tool-normal calibration};
  \node[stg] (b-in)  at (0,1.3)
    {Tool face seated flat\\at three yaw poses\\$T_1$--$T_3$};
  \node[stg] (b-fit) at (3.9,1.3)
    {Estimate the\\tool-face normal};
  \node[res] (b-out) at (7.8,1.3)
    {Tool normal\\$n_{\mathrm{Tool,EE}}$\\in the EE frame};

  % ---- Routing. One horizontal run per height: y=4.6 and y=1.3. ---------
  \draw[sig] (a-in)  -- (a-geo);
  \draw[sig] (a-geo) -- (a-frm);
  \draw[sig] (a-frm) -- (a-out);

  \draw[sig] (b-in)  -- (b-fit);
  \draw[sig] (b-fit) -- (b-out);
\end{tikzpicture}
