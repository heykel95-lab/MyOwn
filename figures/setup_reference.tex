% Set-up reference generation: the commanded grinding-face point advances past
% the surface, so the endpoint is a spring reference and not a commanded travel.
\begin{tikzpicture}[
    font=\footnotesize,
    tool/.style={draw=gray!70!black, fill=white},
    cmd/.style={-{Latex[length=1.9mm]}, gray!55!black, thick},
    lbl/.style={font=\footnotesize, inner sep=1.8pt},
  ]

  % ---- surface
  \draw[gray!70!black] (0,0) -- (7.0,0);
  \node[lbl, right] at (7.0,0) {surface};

  % ---- selected point at the clearance transition, and its projection
  \draw[tool] (1.1,1.05) rectangle (3.3,1.5);
  \fill (1.5,1.05) circle (1.6pt);
  \node[lbl, left] at (1.45,1.15) {selected point at clearance};
  \draw[gray!55, densely dotted] (1.5,1.05) -- (1.5,0);
  \fill (1.5,0) circle (1.6pt);
  \node[lbl, below left] at (1.5,-0.05) {$p_{g,0}$};

  % ---- commanded motion of that point, continuing past the surface
  \draw[cmd] (1.5,0) -- (1.5,-1.35);
  \node[lbl, right] at (1.55,-0.72) {$s_{\mathrm{set}}(t)\,(-n_s)$};
  \fill (1.5,-1.35) circle (1.6pt);
  \node[lbl, below] at (1.5,-1.5) {commanded endpoint};


\end{tikzpicture}
