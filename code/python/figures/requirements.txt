# Environment that drew the committed figures.
#
# matplotlib 3.9.2 is read from the /Creator and /Producer metadata of
# figures/MAIN_D_wrench.pdf and figures/MAIN_NS_nullspace_automatic.pdf, so it
# is the version those files were drawn with rather than a guess. It needs
# Python 3.9 or newer. The numpy version was not recorded anywhere; only
# matplotlib's own floor is pinned, because inventing one would claim more than
# is known.
#
#     python3 -m venv .venv && . .venv/bin/activate
#     pip install -r requirements.txt
#
# An older matplotlib reproduces every number exactly and draws the same
# curves, but changes tick density and drops the U+2212 minus sign; see the
# Environment section of README.md.

matplotlib==3.9.2
numpy>=1.23
