# Working rules for `code/`

**The rules live in [AGENTS.md](AGENTS.md), beside this file. Open and read it
in full before touching anything in `code/`.**

This file exists because Claude Code loads `CLAUDE.md` automatically. It is a
router, not a second set of rules. Repository-wide rules are in
[../AGENTS.md](../AGENTS.md).

If a rule needs to change, change it in `code/AGENTS.md`. Do not copy rules
here.
