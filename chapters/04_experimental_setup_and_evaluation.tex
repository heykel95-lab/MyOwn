\chapter{Experimental Methodology}
\label{ch:methodology}

This chapter describes the experimental system, calibration procedure,
controller parameters, test conditions, and evaluation methods used to assess
contact-induced tool alignment. The primary investigation consists of a
surface-contact campaign in which commanded tilt, directional stiffness, and
virtual-compliance-centre placement are varied under otherwise matched
conditions. A separate Cartesian hold study evaluates the two null-space
torque contributions without surface contact.


\section{Experimental Questions and Test Matrix}
\label{sec:experimental_design}

The surface-contact campaign investigated how four controller parameters
influence the rotation produced while the grinding face is pressed against the
surface:

\begin{itemize}
    \item the commanded angular offset before contact;
    \item the rotational stiffness about the tilted surface tangent;
    \item the translational stiffness perpendicular to the tilted tangent; and
    \item the position and sign of the virtual-compliance-centre lever.
\end{itemize}

The commanded offset establishes the initial angular error. The rotational and
translational stiffness entries determine the resistance to the corresponding
contact-induced motion, while the compliance-centre lever determines the
translation--rotation coupling generated by the point-shifted impedance.

A fifth case repeats the commanded tilt about the axes of the grinding face
rather than directly about the surface tangents. This comparison helps
distinguish an asymmetry associated with the calibrated surface frame from one
associated with the tool mounting or grinding-face geometry.

Two changes distinguish the reported campaign from the preliminary campaign
documented in \Cref{app:previous_campaign}. First, the surface plane and the
grinding-face direction were calibrated independently. The commanded tilt is
therefore defined relative to a measured surface frame. Second, rotational
stiffness was varied about one surface tangent at a time, allowing the
\(t_1\) and \(t_2\) responses to be evaluated separately.

Except for the parameter explicitly varied in each case, all contact runs used
the same baseline Cartesian impedance:

\begin{equation}
K_p
=
\operatorname{diag}(2000,2000,350)
\,\mathrm{N/m}
\qquad
\text{in base coordinates},
\label{eq:experimental_baseline_translation}
\end{equation}

\begin{equation}
K_R
=
\operatorname{diag}(5,5,50)
\,\mathrm{N\,m/rad}
\qquad
\text{in the surface frame }[t_1,t_2,n_s].
\label{eq:experimental_baseline_rotation}
\end{equation}

The common motion parameters were a \(60\,\mathrm{mm}\) commanded press,
a press speed of \(50\,\mathrm{mm/s}\), and a \(4\,\mathrm{s}\) set-up
timeout. The null-space controller remained in the combined damping and
singular-value-conditioning mode throughout the contact campaign. The complete
fixed parameter set is given in
\Cref{sec:experimental_controller_parameters}.

\begin{table}[H]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\caption{Experimental cases of the surface-contact campaign.}
\label{tab:experimental_test_cases}
\begin{tabular}{@{}
>{\centering\arraybackslash}p{0.055\textwidth}
>{\raggedright\arraybackslash}p{0.155\textwidth}
>{\raggedright\arraybackslash}p{0.15\textwidth}
>{\raggedright\arraybackslash}p{0.28\textwidth}
>{\centering\arraybackslash}p{0.035\textwidth}
>{\raggedright\arraybackslash}p{0.155\textwidth}@{}}
\toprule
\textbf{Case}
&
\textbf{Investigation}
&
\textbf{Commanded tilt}
&
\textbf{Varied parameter and tested values}
&
\(\boldsymbol{n}\)
&
\textbf{Experimental question}
\\
\midrule

A
&
Commanded tilt
&
Varied
&
\(0^\circ\), \(+5^\circ\), and \(+10^\circ\) about \(t_1\);
\( +5^\circ\) and \(+10^\circ\) about \(t_2\)
&
15
&
How does the initial commanded tilt affect alignment about each surface
tangent?
\\
\addlinespace

B
&
Rotational stiffness
&
\(10^\circ\) about \(t_1\) or \(t_2\)
&
Stiffness of the tilted tangent:
\(5\), \(15\), and \(50\,\mathrm{N\,m/rad}\);
the orthogonal tangent remains at
\(5\,\mathrm{N\,m/rad}\)
&
12
&
How does the rotational stiffness of each tangent affect the resulting
alignment?
\\
\addlinespace

C
&
Translational stiffness
&
\(10^\circ\) about \(t_1\) or \(t_2\)
&
Stiffness perpendicular to the tilted tangent:
\(300\), \(800\), and \(2000\,\mathrm{N/m}\);
one additional condition per tilt axis combines
\(300\,\mathrm{N/m}\) with
\(50\,\mathrm{N\,m/rad}\)
&
18
&
How does cross-axis translational stiffness affect alignment, and how does it
interact with rotational stiffness?
\\
\addlinespace

D
&
Virtual centre of compliance
&
\(10^\circ\) about \(t_1\) or \(t_2\)
&
Perpendicular surface-tangent lever:
\(-60\), \(0\), and \(+60\,\mathrm{mm}\);
additional lever at the grinding-face centre:
\((0,-3.5,19.7)\,\mathrm{mm}\) for the \(t_1\) study and
\((3.5,0,19.7)\,\mathrm{mm}\) for the \(t_2\) study
&
24
&
How do the position and sign of the compliance-centre lever affect alignment?
\\
\addlinespace

E
&
Tilt about grinding-face axes
&
\(10^\circ\) about \(y_{\mathrm{EE}}\) or \(x_{\mathrm{EE}}\)
&
Tool-axis tilts resolved onto the surface tangents:
\[
\begin{aligned}
y_{\mathrm{EE}}:\;& t_1=-4.23^\circ,\\
                  & t_2=+9.06^\circ,\\
x_{\mathrm{EE}}:\;& t_1=+9.06^\circ,\\
                  & t_2=+4.23^\circ
\end{aligned}
\]
&
6
&
Is the difference between the tangent responses associated primarily with the
surface frame or with the grinding-face axes?
\\

\bottomrule
\end{tabular}

\vspace{0.6em}
\begin{minipage}{0.98\textwidth}
\footnotesize
Each setting was repeated three times. Cases A, B, C, and E used the decoupled
Cartesian impedance. Case D used the point-shifted impedance with the listed
levers expressed in the surface frame according to
\(r_c=p_{\mathrm{TCP}}-p_c\). The baseline gains and motion parameters were
retained except where a varied value is stated explicitly.
\end{minipage}
\end{table}

Cases A--E comprise \(75\) surface-contact runs across \(25\) parameter
settings. The declared independent variable was changed between matched
conditions, while the remaining controller parameters, calibrated geometry,
phase sequence, and evaluation procedure were retained.

The primary response was the change in alignment relative to the independently
calibrated physical surface. Contact-induced rotation, displacement of the
point or edge of the grinding face used for the set-up motion, TCP
displacement, and the model-estimated external load were used as supporting
quantities. Their definitions and statistical treatment are presented in
\Cref{sec:experimental_evaluation}.

The \(75\) contact runs form the primary experimental campaign. The separate
Cartesian hold study described in \Cref{sec:nullspace_hold_study} uses an
internally commanded disturbance to evaluate projected null-space damping and
singular-value conditioning independently of the surface-contact cases.

\section{Experimental System}
\label{sec:experimental_system}


\subsection{Robot, Tool, and Surface}
\label{subsec:experimental_robot_tool_surface}

The experiments were performed with a seven-degree-of-freedom Franka Emika
Panda operated through the Franka Control Interface. Joint-torque commands were
transmitted through libfranka at the nominal control period

\begin{equation}
T_s=1\,\mathrm{ms},
\label{eq:sampling_time_contact}
\end{equation}

corresponding to a nominal control rate of \(1\,\mathrm{kHz}\). The real-time
controller and its phase sequence are described in
\Cref{ch:implementation}.

The rectangular grinding tool was held by the Franka Hand using custom gripper
fingers. Its grinding face measured \(40\,\mathrm{mm}\) along
\(X_{\mathrm{EE}}\) and \(120\,\mathrm{mm}\) along \(Y_{\mathrm{EE}}\). The
centre of the grinding face was located \(20\,\mathrm{mm}\) along
\(+Z_{\mathrm{EE}}\) from the reported end-effector origin. The complete
calibrated tool geometry is presented in
\Cref{sec:surface_tool_calibration}.

The gripper permitted approximately \(\pm2^\circ\) of relative tool rotation
about \(y_{\mathrm{EE}}\). Consequently, the grinding-face direction used in
the evaluation was calculated from the measured end-effector orientation and
the calibrated grinding-face direction in the end-effector frame. Relative
motion between the tool and the gripper contributes to the measurement
uncertainty.

The surface plate was mounted on a raised base board. All experiments used dry
contact between the grinding face and the plate. The controller included no
explicit environment-friction model. The surface plane and grinding-face
direction were calibrated separately before the experimental campaign, as
described in \Cref{sec:surface_tool_calibration}.

During approach, the controller determines which point or edge of the
rectangular grinding face lies furthest in the descent direction. This point
or edge is used for the geometric clearance calculation and the subsequent
set-up motion. Its position was calculated from the measured end-effector pose
and the calibrated tool geometry.

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \input{figures/setup_schematic.tex}
    }
    \caption{Experimental system and surface-frame contact geometry.}
    \label{fig:experimental_setup_photo}
\end{figure}


\subsection{Software and Real-Time Computer}
\label{subsec:experimental_software_computer}

The controller computer ran Ubuntu~20.04.3 with the
\texttt{5.9.1-rt20} Linux kernel and the \texttt{PREEMPT\_RT} patch. The
controller was compiled using \texttt{g++~9.3}, the C++14 language standard,
and the \texttt{-O2} optimisation level. Eigen~3.3.7 and libfranka~0.7.1 were
used for the matrix calculations and communication with the robot.

The controller operated through the libfranka joint-torque interface. At each
control cycle, the measured robot state and the quantities provided by the
robot model were used to calculate the commanded joint torque. The
implementation of the real-time callback is documented in
\Cref{sec:real_time_control_loop}.

Each contact run followed the implemented sequence of tool orientation,
surface approach, set-up, and grinding. Pose hold and manual guidance were used
only for preparation or separate experiments. The combined null-space mode
remained active throughout the contact sequence.


\subsection{Recorded Experimental Quantities}
\label{subsec:experimental_recorded_quantities}

The quantities required for the subsequent evaluation were written to the
controller's in-memory logging buffer during each run and exported to a CSV
file after torque control had ended. The recorded quantities included:

\begin{itemize}
    \item the measured end-effector position and orientation;
    \item the measured joint position and velocity;
    \item the commanded Cartesian reference and Cartesian error;
    \item the position of the point or edge of the grinding face used for the
          set-up motion;
    \item the active controller phase and null-space mode;
    \item the commanded Cartesian wrench and final joint-torque command; and
    \item the model-estimated external force and moment reported by libfranka.
\end{itemize}

The commanded wrench and the model-estimated external wrench were retained as
separate quantities. The latter was used as a supporting measure of the contact
response and represents the model-based estimate supplied by libfranka rather
than a direct force measurement. The complete logging implementation and CSV
structure are described in \Cref{sec:recorded_signals}.


\subsection{Reproducibility Record}
\label{subsec:reproducibility_record}

The documented experimental system is summarised in
\Cref{tab:reproducibility_record}. Controller parameters that differ between
the individual experimental cases are reported separately in
\Cref{sec:experimental_controller_parameters}.

{\small
\begin{longtable}{@{}
>{\raggedright\arraybackslash}p{0.29\textwidth}
>{\raggedright\arraybackslash}p{0.64\textwidth}@{}}
\caption{Documented configuration of the experimental system.}
\label{tab:reproducibility_record}\\
\toprule
\textbf{Item}
&
\textbf{Configuration used in the reported experiments}
\\
\midrule
\endfirsthead

\multicolumn{2}{c}{
    \tablename~\thetable\ continued
}
\\[2pt]
\toprule
\textbf{Item}
&
\textbf{Configuration used in the reported experiments}
\\
\midrule
\endhead

\midrule
\multicolumn{2}{r}{Continued on the next page}
\\
\endfoot

\bottomrule
\endlastfoot

Robot
&
Franka Emika Panda with seven actuated joints.
\\

Robot interface
&
libfranka joint-torque interface with a nominal control period of
\(1\,\mathrm{ms}\).
\\

Gripper and tool mounting
&
Franka Hand with custom gripper fingers and an approximately
\(\pm2^\circ\) relative rotational play about \(y_{\mathrm{EE}}\).
\\

Grinding tool
&
Rectangular grinding face measuring \(40\times120\,\mathrm{mm}\), with its
centre located \(20\,\mathrm{mm}\) along \(+Z_{\mathrm{EE}}\) from the
reported end-effector origin.
\\

Surface
&
Plane surface mounted on a raised base board and used under dry-contact
conditions.
\\

Operating system
&
Ubuntu~20.04.3.
\\

Real-time kernel
&
Linux \texttt{5.9.1-rt20} with \texttt{PREEMPT\_RT}.
\\

Compiler
&
\texttt{g++~9.3}, C++14, optimisation level \texttt{-O2}.
\\

Software libraries
&
Eigen~3.3.7 and libfranka~0.7.1.
\\

Recorded state quantities
&
Robot pose, joint position and velocity, Cartesian reference and error,
controller phase, and position of the grinding-face point or edge used for the
set-up motion.
\\

Recorded command and load quantities
&
Commanded Cartesian wrench, commanded joint torque, and the model-estimated
external force and moment reported by libfranka.
\\

Contact condition
&
Dry contact; no explicit environment-friction model was included in the
controller.
\\

\end{longtable}
}

The software versions, nominal control period, tool dimensions, and surface
calibration were documented. The real-time-computer hardware, payload
parameters, and complete robot transform configuration were not retained in the
experimental record. Their influence on exact dynamic and timing
reproducibility is considered in \Cref{ch:conclusion}.

\subsection{Configured and Physical Plane}
\label{subsec:plane_calibration_protocol}
\ReviewMark{grey}{The two plane definitions are separated correctly, but the
physical-plane estimate has substantial angular spread and only one held-out
point. Absolute residual-angle claims therefore remain weak.}

Two plane definitions were used, and they are not interchangeable. The
controller operated with the configured plane alone: the plane point
\(p_s=[\,0.55,\,-0.06,\,0.025\,]^\top\,\mathrm{m}\) and the normal
\(n_s\), obtained from \(a=0^\circ\) and \(b=5^\circ\)
in \cref{eq:implemented_surface_normal_angles}. Approach, clearance, feature
projection, surface frame and commanded tool orientation all follow from it.

The physical orientation of the surface was estimated for the alignment
metric. Three robot-probed points defined the plane, and a fourth independent
point lay \(0.82\,\mathrm{mm}\) from it. The procedure was repeated three times,
giving \(a_{\mathrm{phys}}=-0.72\pm0.31^\circ\) and
\(b_{\mathrm{phys}}=14.99\pm1.91^\circ\) in the tilt parametrisation of
\cref{eq:implemented_surface_normal_angles}, from which \(n_{\mathrm{phys}}\)
follows. The controller itself plays no part in this estimate: it is configured
with \(a\) and \(b\) directly and has no calibration routine.

Between \(n_s\) and \(n_{\mathrm{phys}}\) lies \(10.01^\circ\), and
that separation is the initial geometric misalignment the alignment study works
on. The repeatability of the probing limits how far an absolute residual angle
can be trusted, the spread on \(b_{\mathrm{phys}}\) being the larger of the two.
A change measured within one run is less exposed to that offset, which is why
the reported outcome is a change rather than a final absolute angle.

\section{Contact Geometry}
\label{sec:contact_geometry}
% Forward reference from the test matrix until the calibration section is written.
\label{sec:surface_tool_calibration}
\ReviewMark{yellow}{The geometry is necessary, but much of the surface frame,
tool-axis target, rectangular face, and lever notation repeats Chapter 3. A
shorter experiment-specific statement with cross-references would be clearer.}

The task geometry is defined by a surface plane, a tool edge, and a selected
local linearised centre of compliance. Let

\begin{equation}
n_s \in \mathbb{R}^3,
\qquad
\|n_s\| = 1
\label{eq:surface_normal}
\end{equation}

be the surface normal expressed in the robot base frame. In the implemented
configuration it is generated from rotations \(a\) about base \(x\) and \(b\)
about base \(y\),

\begin{equation}
n_s
=
R_y(b)R_x(a)e_z
=
\begin{bmatrix}
\sin(b)\cos(a) & -\sin(a) & \cos(b)\cos(a)
\end{bmatrix}^{\top}.
\label{eq:implemented_surface_normal_angles}
\end{equation}

Here \(a=0^\circ\) and \(b=5^\circ\), with the plane through
\(p_s=[\,0.55,\,-0.06,\,0.025\,]^\top\,\mathrm{m}\). Two orthonormal tangents
\(t_1\) and \(t_2\) span it: projecting the configured auxiliary direction into
the plane gives the first, and \(t_2=n_s\times t_1\) the second. The
implemented column order is

\begin{equation}
R_{\mathrm{surface}}
=
\begin{bmatrix}
t_1 & t_2 & n_s
\end{bmatrix},
\label{eq:surface_task_frame}
\end{equation}

matching the surface task frame of \cref{eq:surface_task_frame_matrix}. Every
surface-frame vector and every diagonal gain in this chapter follows the order
\([\,t_1,t_2,n_s\,]\) unless a table states another semantic order explicitly.
With the active auxiliary direction \(a_s=e_x\) and the angles above, the axes
come out as
\[
t_1=[\,\cos 5^\circ,\ 0,\ -\sin 5^\circ\,]^\top,
\qquad
t_2=[\,0,\ 1,\ 0\,]^\top .
\]
Rotating about \(t_2\) is therefore rotation about base \(y\), while rotating
about \(t_1\) tips the tool about the orthogonal tangent, which lies in the
tilted base \(x\)-\(z\) plane.

One fixed point would not describe the grinding face, so the face is carried
explicitly. Its centre sits \(20\,\mathrm{mm}\) along \(+Z_{\mathrm{EE}}\) from
the end-effector origin, and it measures \(40\,\mathrm{mm}\) along
\(X_{\mathrm{EE}}\) by \(120\,\mathrm{mm}\) along \(Y_{\mathrm{EE}}\). The
controller transforms all four corners into the base frame and picks whichever
corner, edge, or complete face leads along the descent direction, averaging any
features whose projected heights agree to within \(0.1\,\mathrm{mm}\). Without
that tolerance an exact single-axis tilt would pick one endpoint of a physical
edge at random.

The configured physical face-normal axis is \(a_{\mathrm{EE}}\), as defined in
\cref{sec:tool_orientation_given_surface}. For the current tool
\(a_{\mathrm{EE}}=e_z\), and the orientation target
satisfies

\begin{equation}
R_d a_{\mathrm{EE}}=-n_s .
\label{eq:implemented_tool_axis_target}
\end{equation}

The minus sign turns the \(+Z_{\mathrm{EE}}\) grinding face toward the surface.
What the controller applies is the minimum rotation carrying the starting
nominal tool axis onto \(-n_s\), which preserves as much of the remaining twist
as it can. Setting \(R_d=R_{\mathrm{surface}}\) would not: it would command the
twist as well.

The parallel-jaw grasp allowed approximately \(\pm2^\circ\) of relative tool
rotation about \(y_{\mathrm{EE}}\). This direction lies mainly in the
surface-tangent plane for the tested mounting pose, but it is not identical to
\(t_2\). The calibrated tangent is fixed in the base frame, whereas
\(y_{\mathrm{EE}}=R_{\mathrm{EE}}e_{y,\mathrm{EE}}\) moves with the
end-effector.

A point \(p_c\) carries the selected centre of compliance, with the lever
\(r_c=p_{\mathrm{TCP}}-p_c\) of \cref{eq:pole_to_tcp_lever}. Its
\(p_{\mathrm{TCP}}\) is the origin of the end-effector pose the robot reports,
so relating that point to the physical hand and tool means carrying any
configured flange-to-end-effector transform along with it.

Case matters in the subscript. Lower-case \(c\) belongs to the chosen virtual
centre \(p_c\); upper-case \(C\) appears later for the physical contact point
\(p_C\), whose lever \(r_C=p_C-p_{\mathrm{TCP}}\) points the other way. The two
are never interchangeable.

A force \(f\) at the contact produces a moment about the TCP through
\(r_C\), which is why the selected stiffness matrix will not read as three
independent translational springs beside three independent rotational ones.
Virtual-centre behaviour requires translation and rotation to be selected
together.

\section{Gain Selection Methodology}
\label{sec:cartesian_stiffness_selection}
% Forward reference from the test matrix and reproducibility record.
\label{sec:experimental_controller_parameters}
\ReviewMark{yellow}{The section is technically complete but very long. It
mixes frame bookkeeping, sizing assumptions, damping policy, compliance-centre
construction, and experimental variation before the reader reaches the actual
tested levels.}

Notation follows Chapter~\ref{ch:theory}. A subscript \(\mathrm{task}\) marks a
gain written in a local task frame, and a surface-frame quantity takes the axis
order \([t_1,t_2,n_s]\). No single frame serves every phase and every gain
block, however:

\begin{itemize}
    \item approach translation and rotation are diagonal in the surface frame
    and transformed to the base frame;
    \item set-up and grind translation are diagonal directly in base
    \([x,y,z]\), while their rotational gains are diagonal in
    \([t_1,t_2,n_s]\);
    \item hold gains are isotropic in the base frame; the enabled pre-set-up
    gate overrides only translation with an isotropic base-frame gain and
    retains the isotropic approach rotational gain; and
    \item during set-up only, the selected base-frame translational and
    rotational blocks can be shifted from a commanded centre of compliance to the TCP, producing
    a coupled \(6\times6\) gain.
\end{itemize}

\Cref{tab:active_stiffness_design_points} maps the stiffness across every phase
--- hold, both approach states, the pre-set-up gate, set-up, grind and manual
guidance --- and \cref{tab:active_automatic_damping_policy} gives the matching
inertia-scaled-damping policy. The coordinate frame is part of the gain definition
and must be retained whenever the gain is calculated or transformed.

Base-frame errors and base-frame gains determine the control law of
\cref{eq:cartesian_impedance_control_law}. For experimental interpretation, the
same quantities are expressed in the surface task frame as

\begin{equation}
e_{\mathrm{task}}
=
\begin{bmatrix}
e_{p,\mathrm{task}}\\
e_{R,\mathrm{task}}
\end{bmatrix}
=
T_R^\top
\begin{bmatrix}
e_p\\
e_R
\end{bmatrix},
\qquad
\dot{x}_{\mathrm{task}}
=
T_R^\top
\begin{bmatrix}
\dot{p}_{\mathrm{EE}}\\
\omega_{\mathrm{EE}}
\end{bmatrix},
\label{eq:experiment_task_frame_errors}
\end{equation}

where \(T_R=\operatorname{diag}(R_{\mathrm{surface}},R_{\mathrm{surface}})\).
The representation serves interpretation, where both blocks are being read in
the surface frame. This representation does not alter the stored set-up
translation block, which remains diagonal in base XYZ.

The complete local Cartesian stiffness matrix keeps the task-frame notation of
\cref{eq:full_task_frame_gain_blocks}. In the standard spatial-stiffness block
form \citep{Fasse1997SpatialImpedance,Ott2008},

\begin{equation}
K_{\mathrm{task}}
=
\begin{bmatrix}
K_{p,\mathrm{task}} & K_{pR,\mathrm{task}}\\
K_{Rp,\mathrm{task}} & K_{R,\mathrm{task}}
\end{bmatrix}.
\label{eq:contact_stiffness_block}
\end{equation}

Translational displacement reaches force through \(K_{p,\mathrm{task}}\) and
rotational displacement reaches moment through \(K_{R,\mathrm{task}}\), while
the off-diagonal blocks \(K_{pR,\mathrm{task}}\) and \(K_{Rp,\mathrm{task}}\)
represent the translation--rotation coupling. This block form separates the
coupling terms from the diagonal translational and rotational terms. During
set-up, the full matrix is constructed analytically from a selected centre of
compliance and the source stiffness blocks. The resulting 36 entries are given
below.

The construction assumes a symmetric positive-definite stiffness and a
symmetric positive-semidefinite damping matrix,

\begin{equation}
K_{\mathrm{task}} = K_{\mathrm{task}}^\top,
\qquad
K_{\mathrm{task}} \succ 0.
\label{eq:contact_stiffness_spd}
\end{equation}

Both properties are preserved by the congruence and were verified for the
assembled \(6\times6\) matrices. This verification detects sign and transpose
errors without introducing an additional tuning parameter.

\subsection{Quasi-Static Stiffness Sizing and Inertia-Scaled Damping}
\label{subsec:initial_stiffness_damping_values}
\ReviewMark{grey}{The selected force, moment, displacement, and angle scales
are engineering choices rather than measured contact requirements. The blocked
spring calculations are useful bounds, but they do not validate the chosen
gains or the coupled damping.}

The stiffness values were sized offline and the calculation was not evaluated
on the robot. It defines a virtual-spring command scale rather than an estimated
contact force. At low velocity the elastic parts of the implemented command
separate from damping and inertia,

\begin{equation}
f=K_pe_p,
\qquad
m=K_Re_R .
\label{eq:quasi_static_translational_stiffness}
\end{equation}

For a principal coordinate \(i\), the stiffness is the ratio between the chosen
elastic-load scale and the corresponding error scale:

\begin{align}
k_{p,i}
&=
\frac{|f_i|}{|e_{p,i}|},
\label{eq:initial_translational_stiffness}\\
k_{R,i}
&=
\frac{|m_i|}{|e_{R,i}|}.
\label{eq:initial_rotational_stiffness}
\end{align}

The values inserted into these ratios were sizing values rather than measured
contact loads. Where a moment scale came from an offset force, its component
about the selected unit axis \(a_i\) was
\[
\left|a_i^\top(r_C\times f_C)\right|
\leq
\lVert r_C\rVert\lVert f_C\rVert ,
\]
rather than the scalar lever-arm bound being used without its axis direction
\citep{Murray1994,Siciliano2009}.

\paragraph{Frame-consistent directional stiffness.}
Outside a principal-gain frame the scalar rule of
\cref{eq:initial_translational_stiffness} no longer applies directly. For a
gain diagonal in frame \(R_F\), the base-frame matrix and the effective slope
along a base-frame unit direction \(u\) are

\begin{equation}
K_{p,0}
=
R_F\operatorname{diag}(k_{p,1},k_{p,2},k_{p,3})R_F^\top,
\qquad
k_{p,\mathrm{eff}}(u)
=
u^\top K_{p,0}u
=
\sum_{i=1}^{3}k_{p,i}(r_i^\top u)^2 .
\label{eq:directional_effective_stiffness}
\end{equation}

An error \(e_p=\delta u\) gives the elastic component
\(\delta k_{p,\mathrm{eff}}(u)\) along \(u\), and along a non-principal
direction it also raises the transverse component \(\delta(I-uu^\top)K_{p,0}u\).
The same construction covers \(K_{R,0}\) and a rotational direction. Set-up
translation needs it: that block is diagonal in base XYZ while the push follows
the tilted surface normal.

\paragraph{Set-up/grind calculation.}
Base \(y\) and the active \(t_2\) coincide, which makes the lateral entry easy
to size. Treating the completely blocked \(30\,\mathrm{mm}\) grind stroke as a
\(60\,\mathrm{N}\) elastic design scale gives

\begin{equation}
k_y=\frac{60}{0.030}=2000\,\mathrm{N/m},
\qquad
k_x=k_y .
\label{eq:setup_lateral_stiffness_design}
\end{equation}

The press direction takes a nominal \(65\,\mathrm{N}\) elastic scale at the
configured \(0.180\,\mathrm{m}\) endpoint, so
\(k_{p,\mathrm{eff}}(n_s)=65/0.180=361.11\,\mathrm{N/m}\). That coordinate
starts near \(-0.020\,\mathrm{m}\) at the clearance hand-off and is scheduled
to \(+0.180\,\mathrm{m}\), roughly \(0.200\,\mathrm{m}\) of reference travel,
which at \(0.050\,\mathrm{m/s}\) takes \(4\,\mathrm{s}\) --- deliberately the
same as the set-up timeout. Of that travel the first \(20\,\mathrm{mm}\) closes
the geometric clearance, and the remaining \(180\,\mathrm{mm}\) is the endpoint
against which the blocked-load design scale is defined.
With \(a=0^\circ\), \(b=5^\circ\), and
\(K_{p,0}=\operatorname{diag}(k_x,k_y,k_z)\), solving
\[
k_{p,\mathrm{eff}}(n_s)=k_x\sin^2 b+k_z\cos^2 b
\]
gives

\begin{equation}
k_z
=
\frac{k_{p,\mathrm{eff}}(n_s)-k_x\sin^2(5^\circ)}
     {\cos^2(5^\circ)}
=
348.6\,\mathrm{N/m}
\;\longrightarrow\;
350\,\mathrm{N/m}.
\label{eq:setup_base_z_stiffness_design}
\end{equation}

Rounded, the implemented block is
\(\operatorname{diag}(2000,2000,350)\,\mathrm{N/m}\) in base XYZ. The same
matrix in the active surface frame reads

\begin{equation}
R_{\mathrm{surface}}^\top K_{p,0}R_{\mathrm{surface}}
\approx
\begin{bmatrix}
1987.47 & 0 & 143.26\\
0 & 2000 & 0\\
143.26 & 0 & 362.53
\end{bmatrix}
\mathrm{N/m}.
\label{eq:active_setup_translation_surface_representation}
\end{equation}

A completely blocked \(0.180\,\mathrm{m}\) normal reference change then works
out at roughly \(65.3\,\mathrm{N}\) along the normal, \(25.8\,\mathrm{N}\)
along \(t_1\), and \(70.2\,\mathrm{N}\) of total elastic-force magnitude. All
three assume the tracking error never closes, whereas the robot follows part of
the reference, so they are upper bounds on the virtual spring rather than
predicted loads.

The rotational block starts from a \(15\,\mathrm{N}\) design force acting
through the \(60\,\mathrm{mm}\) tool half-length, a moment scale of about
\(0.90\,\mathrm{N\,m}\). Allowing roughly \(10^\circ\) about the two surface
tangents but only \(1^\circ\) about the surface normal, and converting to
radians,

\begin{equation}
k_{R,t_1}=k_{R,t_2}
\approx\frac{0.90}{0.1745}=5.16
\;\longrightarrow\;5,
\qquad
k_{R,n}
\approx\frac{0.90}{0.01745}=51.6
\;\longrightarrow\;50
\quad\mathrm{N\,m/rad}.
\label{eq:setup_rotational_stiffness_design}
\end{equation}

The selected ratio provides lower rotational stiffness about the surface
tangents than about the surface normal. During set-up these are source gains at the
selected centre of compliance. Once the congruence shift has been applied they
are, in general, no longer the diagonal rotational slopes a pure rotation at
the TCP would meet. Grind applies the same source blocks without a point shift.

\begin{table}[H]
\centering
\small
\caption{Phase-specific stiffness and design points.}
\label{tab:active_stiffness_design_points}
\begin{tabular}{@{}
>{\raggedright\arraybackslash}p{0.16\textwidth}
>{\raggedright\arraybackslash}p{0.33\textwidth}
>{\raggedright\arraybackslash}p{0.41\textwidth}@{}}
\toprule
Phase / mode & Source stiffness and frame & Design-point interpretation and command structure \\
\midrule
Hold &
\(K_p=300I_3\,\mathrm{N/m}\) and
\(K_R=40I_3\,\mathrm{N\,m/rad}\), base &
\(3\,\mathrm{N}\) at \(10\,\mathrm{mm}\);
\(0.70\,\mathrm{N\,m}\) at \(1^\circ\); decoupled. \\
Approach orient and descend &
\(K_p=150I_3\,\mathrm{N/m}\) and
\(K_R=90I_3\,\mathrm{N\,m/rad}\), surface definition &
\(3\,\mathrm{N}\) at \(20\,\mathrm{mm}\);
\(3.15\,\mathrm{N\,m}\) at the \(0.035\,\mathrm{rad}\) orientation threshold;
decoupled. \\
Pre-set-up gate &
\(K_p=5000I_3\,\mathrm{N/m}\), base; retains
\(K_R=90I_3\,\mathrm{N\,m/rad}\) &
\(25\,\mathrm{N}\) at \(5\,\mathrm{mm}\); only translation is overridden;
decoupled. \\
Set-up &
\(k_p=[2000,2000,350]\,\mathrm{N/m}\), base XYZ;
\(k_R=[5,5,50]\,\mathrm{N\,m/rad}\), surface \([t_1,t_2,n_s]\) &
\(60\,\mathrm{N}\) per blocked \(30\,\mathrm{mm}\) \(t_2\) stroke and
approximately \(65\,\mathrm{N}\) per blocked \(180\,\mathrm{mm}\) normal
reference; source blocks are shifted through the centre of compliance. \\
Grind &
same \(K_p\) and \(K_R\) source blocks as set-up &
Same design scales; the command is decoupled and the active sweep is along
\(t_2\). \\
Manual guidance &
no Cartesian stiffness &
Coriolis compensation plus \(0.5\,\mathrm{N\,m\,s/rad}\) joint damping. \\
\bottomrule
\end{tabular}
\end{table}

The optional pre-grind gate stayed disabled for every reported run. Enabling it
holds the sequence in set-up and continues the coupled pressed command; the
\(5000\,\mathrm{N/m}\) pre-set-up lock is a different gate and is not engaged.
Null-space mode~3 adds a projected joint torque on top of all of this,
independently of the Cartesian stiffness map.

\paragraph{Inertia-scaled damping.}
Cartesian damping was calculated at entry to each phase group using
\cref{eq:implementation_cartesian_inertia,eq:implementation_inertia_scaled_damping}. The Cartesian
inertia was first expressed in the coordinate frame of the corresponding gain
block. Some phases used the damping ratio \(\zeta\) directly. For the hold and
pre-set-up gate, a common damping ratio was derived from the fixed damping
matrix listed in \cref{tab:active_automatic_damping_policy}.

\begin{table}[H]
\centering
\small
\caption{Phase-specific inertia-scaled-damping policy.}
\label{tab:active_automatic_damping_policy}
\begin{tabular}{@{}
>{\raggedright\arraybackslash}p{0.17\textwidth}
>{\raggedright\arraybackslash}p{0.23\textwidth}
>{\raggedright\arraybackslash}p{0.27\textwidth}
>{\raggedright\arraybackslash}p{0.24\textwidth}@{}}
\toprule
Phase / mode & Inertia/gain frame & Ratio or derived target & Manual fallback \\
\midrule
Approach orient and descend &
surface for translation and rotation &
\(\zeta=1.9\), computed once and shared &
\(D_p=[20,20,20]\);
\(D_R=[12,12,12]\) \\
Pre-set-up gate &
base translation; surface rotation &
separate damping ratios derived from \(D_p=200I_3\) and the cached approach \(D_R\) &
\(D_p=200I_3\); inherited cached or manual approach \(D_R\) \\
Set-up and grind &
base translation; surface rotation &
\(\zeta=1.0\), computed after clearance and shared &
\(D_p=[10,10,175]\);
\(D_R=[10.01,10.01,10]\) \\
Hold &
base for translation and rotation &
one damping ratio derived from \(D_p=50I_3\), then also applied to rotation &
\(D_p=50I_3\);
\(D_R=8I_3\) \\
Manual guidance &
not applicable &
no Cartesian inertia-scaled damping &
\(0.5\,\mathrm{N\,m\,s/rad}\) joint damping \\
\bottomrule
\end{tabular}
\end{table}

Only finite, positive directional inertia values were accepted. The fixed
phase-specific matrix in the final column of
\cref{tab:active_automatic_damping_policy} supplied the damping whenever the
inertia-based value was unavailable.

Two things limit \cref{eq:implementation_inertia_scaled_damping}. It reads only the
diagonal of the TCP task-inertia estimate and drops the off-diagonal coupling,
and its \(\zeta\) is a dimensionless tuning factor rather than an identified
modal damping ratio. During set-up the source damping blocks are calculated at
the TCP and then shifted through the centre of compliance by the same
congruence as the stiffness. Symmetry and positive semidefiniteness survive
that shift. Critical damping of the compliance-centre/contact system does not
follow from \(\zeta=1\).

\subsection{Compliance-Centre Gain Design}
\label{subsec:impedance_around_pole}
\ReviewMark{green}{The active mixed-frame source gains, constant reference,
stored lever convention, surface-frame resolution, and moment sign check are
all explicit.}

Only \texttt{set\_up} applies the compliance-centre construction. A rotation
into the surface frame changes the principal directions of the translational
and rotational blocks but leaves the translation--rotation blocks equal to
zero. The centre of compliance consequently remains at the TCP unless the
point shift of
\cref{eq:offset_adjoint,eq:coupled_tcp_stiffness,eq:coupled_tcp_damping} is
applied. The signs and the complete block-matrix derivation are given in
\cref{sec:coupled_pole_stiffness}.

The active source stiffness, written wholly in base-frame coordinates, is

\begin{equation}
K_{c,0}
=
\operatorname{blkdiag}\!\left(
\operatorname{diag}(2000,2000,350),
R_{\mathrm{surface}}\operatorname{diag}(5,5,50)
R_{\mathrm{surface}}^\top
\right).
\label{eq:active_mixed_frame_pole_stiffness}
\end{equation}

Its translational entries carry \(\mathrm{N/m}\) and its rotational entries
\(\mathrm{N\,m/rad}\). Written out this way the mixed-frame construction shows
plainly: translation diagonal in base XYZ, rotation alone diagonal in surface
coordinates \([t_1,t_2,n_s]\).

This transformation changes only the gain matrices; it introduces neither an
additional wrench nor a time-varying task-space rotation. Its experimental
effect was evaluated from the EE-inferred tool-axis alignment, the selected
feature and TCP displacements, and the commanded joint torques.

\paragraph{Choosing the centre of compliance.}
The implementation accepts an arbitrary base-frame offset from the projected
leading-feature reference captured at clearance:

\begin{equation}
p_c=p_{\Pi,\mathrm{clr}}+\Delta p_c^0,
\qquad
\Delta p_c^0=
\begin{bmatrix}-0.04&0.08&0\end{bmatrix}^{\top}\mathrm{m}.
\label{eq:active_pole_choice}
\end{equation}

Its superscript \(0\) is a reminder that the three components are base XYZ and
not \([t_1,t_2,n_s]\). The reference is held constant for the run. A scalar
choice \(p_c=p_s+h n_s\) is a theoretical special case that the active
parameterisation does not use. Holding the centre of compliance constant from
first contact holds the lever constant too,
\[
r_c=p_{\mathrm{TCP,clr}}-p_c,
\]
where \(p_{\mathrm{TCP,clr}}\) is the TCP position captured at the same
clearance hand-off. A different \(\Delta p_c^0\) means a different coupled pair
\(K_{\mathrm{TCP}},D_{\mathrm{TCP}}\) out of
\cref{eq:coupled_tcp_stiffness,eq:coupled_tcp_damping}, so the sign and the
frame of the vector are stored with every trial. Neither can be recovered from
the alignment record afterwards.

Resolved in surface coordinates on the configured \(a=0^\circ\),
\(b=5^\circ\) surface, the base-XYZ offset becomes

\begin{equation}
R_{\mathrm{surface}}^\top\Delta p_c^0
\simeq
\begin{bmatrix}-39.85&80.00&-3.49\end{bmatrix}^{\top}\mathrm{mm}.
\label{eq:active_pole_offset_surface_components}
\end{equation}

The centre of compliance therefore lies mainly in the \(-t_1,+t_2\) quadrant
relative to the projected selected feature. The offset is not itself the lever
the adjoint uses; the exact lever picks up the captured TCP-to-feature geometry
as well. Writing \(f=K_{p,0}e_p\) for a purely translational error, the moment
the coupling block produces gives a direct sign check,

\begin{equation}
m_c=[r_c]_\times^\top f=-r_c\times f.
\label{eq:active_pole_moment_sign_check}
\end{equation}

At the nominal aligned geometry the transverse lever is approximately
\(0.03985t_1-0.08t_2\), and a blocked press with \(f\approx-Fn_s\), \(F>0\),
gives \(m_c\approx-F(0.08t_1+0.03985t_2)\). The selected quadrant thus biases
negative rotation about both surface tangents, and reversing either transverse
offset reverses the corresponding bias. This relation determined the selected
direction. Because the translational stiffness is diagonal in the base frame,
the commanded force need not be exactly normal to the surface. The bias assists
alignment only where its signed components oppose the residual orientation
error, which was evaluated from the measured physical-plane alignment.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[>=Latex,font=\small,scale=1.12]
    \coordinate (Q) at (0,0);
    \coordinate (C) at (-1.85,0.20);
    \coordinate (F) at (0.20,1.05);
    \coordinate (T) at (1.10,2.85);
    \fill[gray!10]
      (-3.0,-0.65)--(3.0,-0.65)--(4.0,0.15)--(-2.0,0.15)--cycle;
    \draw[gray!65]
      (-3.0,-0.65)--(3.0,-0.65)--(4.0,0.15)--(-2.0,0.15)--cycle;
    \draw[->,thick] (Q)--++(2.55,0) node[below right] {$t_1$};
    \draw[->,thick] (Q)--++(1.55,0.85) node[below right] {$t_2$};
    \draw[->,thick] (Q)--++(-0.65,2.05) node[above left] {$n_s$};
    \draw[densely dashed] (Q)--(F);
    \draw[thick] (F)--(T);
    \draw[->,very thick,orange!80!black] (Q)--(C)
      node[midway,above] {$\Delta p_c^0$};
    \draw[->,very thick,blue!70!black] (C)--(T)
      node[pos=0.58,above left] {$r_c=p_{\mathrm{TCP}}-p_c$};
    \filldraw (Q) circle (1.6pt)
      node[below=4pt] {$p_{\Pi,\mathrm{clr}}$};
    \filldraw (C) circle (1.6pt) node[above left] {$p_c$};
    \filldraw (F) circle (1.6pt)
      node[right=4pt,yshift=5pt] {selected feature at clearance};
    \filldraw (T) circle (1.6pt) node[above right] {$p_{\mathrm{TCP}}$};
  \end{tikzpicture}
  \caption{Compliance-centre geometry during set-up.}
  \label{fig:active_pole_geometry}
\end{figure}

The quantity \(p_c\) denotes a local linearised centre of compliance rather than
a physical hinge. The tool need not pivot about it during contact, and the
relation between the two is
affected by four factors: the finite rotational stiffness, the lever itself,
friction and constraints at the surface, and the active null-space sigma torque.

\subsection{Gain Construction and Implementation Sequence}
\label{sec:role_of_gain_methods}
\ReviewMark{red}{The numbered workflow and arrow-chain equation restate the
preceding gain construction in a generic process template. They add little
technical evidence and can be removed without breaking the methodology.}

Calculation and implementation follow the controller's own order, so the five
steps below are applied in the sequence given:

\begin{enumerate}
    \item State a phase-specific force--error or moment--angle design pair and
    calculate the principal source stiffness using
    \cref{eq:initial_translational_stiffness}.
    \item Attach each diagonal to its implemented frame, form the base-frame
    block, and check the commanded path with
    \cref{eq:directional_effective_stiffness}. For set-up, the base-XYZ
    translation and surface-frame rotation blocks remain distinct.
    \item At phase-group entry, calculate and cache the damping from the
    current TCP task inertia using
    \cref{eq:implementation_cartesian_inertia,eq:implementation_inertia_scaled_damping}.
    \item During set-up only, shift the selected source stiffness and damping
    through the centre of compliance using
    \cref{eq:coupled_tcp_stiffness,eq:coupled_tcp_damping}. Grind immediately
    returns to the decoupled source blocks.
    \item Check the resulting elastic load and joint-torque scales, then change
    one declared gain family at a time while retaining the frame labels,
    computed damping, compliance-centre offset, and remaining parameters with the run
    record.
\end{enumerate}

The resulting workflow is

\begin{equation}
\begin{aligned}
\text{phase design envelope}
&\rightarrow \text{frame-labelled source }K
\rightarrow \text{task-inertia }D\\
&\rightarrow \text{set-up-only compliance-centre shift}\\
&\rightarrow \text{load/torque check}
\rightarrow \text{controlled trial}.
\end{aligned}
\label{eq:gain_selection_workflow}
\end{equation}

\subsection{Experimental Gain Variation Strategy}
\label{sec:direct_stiffness_comparison}
\ReviewMark{green}{The one-factor comparisons, held quantities, tested levels,
interaction corner, repetition count, and interpretation rule are stated
clearly.}

The effect of each stiffness entry was investigated by varying one parameter
while the remaining controller settings were held fixed. This
one-factor-at-a-time procedure separates the response associated with the
selected gain from changes caused by other controller parameters, which the
paired sweep of the earlier campaign could not do.

In Case B the rotational stiffness of the tilted surface tangent was tested at
\(5\), \(15\) and \(50\,\mathrm{N\,m/rad}\). When \(K_{R,t_1}\) was varied,
\(K_{R,t_2}\) was held at its baseline value, and the roles were reversed for
the \(t_2\) study. This separates the two tangent responses. The normal
rotational stiffness \(K_{R,n}\) remained at its baseline value because it
resists twist about the surface normal rather than tipping about a tangent.

In Case C the translational stiffness perpendicular to the tilted tangent was
tested at \(300\), \(800\) and \(2000\,\mathrm{N/m}\), and one further condition
combined the lowest translational stiffness with the highest tested rotational
stiffness to examine their interaction. The other source gains, the
compliance-centre offset, the phase timing, and the null-space parameters were
held constant. A systematic change in the measured response could therefore be
associated with the stiffness coordinate that was varied.

Damping needed no separate tuning. At the next entry to the relevant phase group
the controller recalculated it from the changed stiffness and the task inertia.
Because \cref{eq:implementation_inertia_scaled_damping} takes the square root of the
stiffness, scaling an entry by \(\alpha\) scales its damping coefficient by
\(\sqrt{\alpha}\),

\begin{equation}
d_{i,\mathrm{test}}
=
\sqrt{\alpha}\,d_{i,\mathrm{base}} ,
\label{eq:damping_change_with_stiffness}
\end{equation}

so doubling a stiffness raises the damping by about \(41\,\%\) rather than by
\(100\,\%\). This is an algebraic consequence of the damping law and not a
measured result. It holds while the Cartesian inertia \(\Lambda_{F,ii}\) and the
factor \(\zeta\) are unchanged and the coefficient stays between the configured
floor and ceiling; since the inertia depends on the pose at phase-group entry,
the realised coefficients were recorded for every run. The value is cached at
phase-group entry and held for the rest of that phase, and the stiffness alone
does not reconstruct it without the phase-entry pose, the Jacobian \(J\) and the
joint-space mass matrix \(M(q)\).

Approach, gate and hold gains follow the same one-at-a-time rule against the
design pairs of \cref{tab:active_stiffness_design_points}. Where a translational
entry is assigned from a desired effective normal stiffness, the base-\(z\)
value follows \cref{eq:setup_base_z_stiffness_design}, which carries the surface
orientation into the assignment.

\paragraph{Evaluation of candidate gains.}
Candidate stiffnesses were compared through the quantities of
\cref{sec:alignment_evaluation}, with the physical-plane alignment improvement
\(\Delta\theta_{\mathrm{align,phys}}\) as the primary outcome. The
rotational-stiffness study of \cref{sec:results_case_b} was evaluated using the
alignment improvement, the physical-plane residual angle and the
model-estimated normal load. Contact-induced rotation and the displacements of
the selected feature and TCP were used to check that the compared runs followed
the same contact motion.
Each setting was repeated three times and summarised by its mean and standard
deviation. A change was interpreted as a parameter effect only when it exceeded
the interpretation threshold of \cref{sec:results_repeatability} and was not
accompanied by an incompatible change in contact load or displacement.

\subsection{Null-Space Settings}
\label{subsec:nullspace_parameter_rationale}
\ReviewMark{green}{The contact and hold configurations are reconciled in one
table and routed to the common theoretical definitions.}

The primary contact campaign used the combined null-space mode. The internally
commanded hold study used the same projector and probe settings while
separating the two torque terms. The corresponding values are listed in
\cref{tab:nullspace_parameter_rationale}.

\begin{table}[H]
\centering
\small
\caption{Null-space settings in the two campaigns.}
\label{tab:nullspace_parameter_rationale}
\begin{tabular}{@{}lll@{}}
\toprule
Quantity & Primary contact campaign & Internally commanded hold study \\
\midrule
Control mode & damping and sigma & off, damping only, or sigma only \\
\(d_{\mathrm{null}}\) & \(2\,\mathrm{N\,m\,s/rad}\) &
\(0\) or \(2\,\mathrm{N\,m\,s/rad}\) \\
\(k_\sigma\) & \(1\,\mathrm{N\,m}\) &
\(0\), \(1.5\), or \(2\,\mathrm{N\,m}\) \\
\(\alpha_{\mathrm{probe}}\) & \(0.08\,\mathrm{rad}\) & \(0.08\,\mathrm{rad}\) \\
\(\delta_\sigma\) & \(10^{-6}\) & \(10^{-6}\) \\
\(\rho\) & \(10^{-4}\) & \(10^{-4}\) \\
\bottomrule
\end{tabular}
\end{table}

Projected damping opposes \(N_\tau\dot q\), while the conditioning torque acts
along the selected \(v_7\) direction. Their definitions are given in
\cref{sec:nullspace_svd}. The contact results describe the combined controller;
the internally commanded hold results provide the separated comparison.

\subsection{Internally Commanded Null-Space Disturbance Study}
\label{subsec:nullspace_automatic_method}
% Forward reference from the test matrix.
\label{sec:nullspace_hold_study}
\ReviewMark{grey}{The internal point-force equivalent is repeatable and the
acceptance rule is explicit, but it does not validate a physical external
disturbance, link contact, or the combined null-space mode.}

Projected damping and the singular-value bias were isolated in the Cartesian
hold mode without surface contact. A repeatable internal disturbance represented
the joint-torque equivalent of a point force. It was neither a physical external
force nor a force-sensor measurement.

The force point was fixed to link~3 at the link-frame offset
\(r_p=[0,0,0.200]^\top\,\mathrm{m}\). At the initial pose, the structural null
direction \(v_7(q_0)\) and the translational point Jacobian \(J_p(q_0)\) set the
base-frame unit-force direction

\begin{equation}
u_f=\frac{J_p(q_0)v_7(q_0)}{\|J_p(q_0)v_7(q_0)\|_2}.
\label{eq:nullspace_disturbance_direction}
\end{equation}

This direction maximises the virtual work along the initial structural null
direction among unit point forces and was held fixed for the complete trial.
The controller evaluated the current point Jacobian and applied

\begin{equation}
\tau_{\mathrm{dist}}(t)
=J_p(q(t))^\top f_d(t),
\qquad
f_d(t)=20s(t)u_f\,\mathrm{N},
\label{eq:nullspace_automatic_disturbance}
\end{equation}

where \(s(t)\) rose from zero to one through a half-cosine between
\(5\,\mathrm{s}\) and \(7\,\mathrm{s}\), remained at one until
\(8\,\mathrm{s}\), and returned to zero through a one-second half-cosine. A
\(3\,\mathrm{N\,m}\) bound was imposed on
\(\|\tau_{\mathrm{dist}}\|_2\).

Twelve main trials were acquired. Three repetitions were recorded without a
null-space torque. A further three used projected damping of
\(2\,\mathrm{N\,m\,s/rad}\). Sigma-only trials used
\(k_\sigma=1.5\) and \(2\,\mathrm{N\,m}\), with three repetitions per
setting. The predefined acceptance limit was a peak Cartesian position error
of \(2\,\mathrm{mm}\). Every trial remained within this limit and was included
in the comparison.

The disturbance response was measured by the cumulative projected joint
excursion

\begin{equation}
E_N=\int_{5\,\mathrm{s}}^{9\,\mathrm{s}}
\|N_v(q)\dot q\|_2\,\mathrm{d}t.
\label{eq:nullspace_excursion_metric}
\end{equation}

For comparison with the other angular results, the cumulative excursion is
reported in degrees.

The conditioning response was evaluated from the final change in
\(\sigma_{\min}\), while Cartesian task retention was represented by the peak
position-error norm over the hold.

\section{Experimental Procedure}
\label{sec:experimental_procedure_contact}
\ReviewMark{green}{The procedure states what occurred in each run, distinguishes
geometric hand-offs from contact detection, and carries the active values and
stopping conditions needed for replication.}

Every run followed the sequence below, in this order:

\begin{enumerate}
    \item The active parameter set is recorded, and the Panda moves to the
    configured initial joint pose. Optionally it is repositioned under damped
    manual guidance, and the selected start pose is recaptured.
    \item The configured gripper action is executed: in the current
    configuration a grasp at \(70\,\mathrm{mm}\) width and \(60\,\mathrm{N}\)
    rather than an open command. The tool is seated as consistently as its
    approximately \(\pm2^\circ\) rotational play about \(y_{\mathrm{EE}}\)
    allows, and that non-rigid mounting condition is recorded with the run
    metadata.
    \item The known plane and \(R_{\mathrm{surface}}=[t_1,t_2,n_s]\) are
    constructed, together with the \(40\times120\) mm tool face and the active
    base-XYZ compliance-centre offset. The Franka collision behaviour is
    configured before torque control starts.
    \item In \texttt{approach\_orient} the TCP position is held while the
    physical \(+Z_{\mathrm{EE}}\) axis rotates toward \(-n_s\). The phase
    continues only after at least \(0.5\,\mathrm{s}\) and a tool-axis error no
    greater than \(0.035\,\mathrm{rad}\).
    \item In \texttt{approach\_descend} the reference moves along \(-n_s\) at
    \(0.1\,\mathrm{m/s}\), with a maximum commanded travel of
    \(0.640\,\mathrm{m}\), and the leading face, edge or corner is selected
    continuously. The transition occurs when the exact most-leading corner
    height is at most \(20\,\mathrm{mm}\) above the plane; the averaged tied
    feature supplies the projected reference and can differ by at most the
    \(0.1\,\mathrm{mm}\) tie tolerance. Estimated force does not trigger this
    transition. The enabled pre-set-up gate holds the
    reached pose until the operator presses Enter.
    \item At this clearance hand-off the selected tool feature, TCP pose,
    orientation reference, external-wrench baseline and projected
    leading-feature surface reference are selected and held constant. This
    transition is defined geometrically and does not indicate force-detected
    contact.
    \item In \texttt{set\_up} the signed push coordinate ramps continuously from
    its captured value, approximately \(-0.020\,\mathrm{m}\), toward
    \(+0.180\,\mathrm{m}\) at \(0.050\,\mathrm{m/s}\) along \(-n_s\), under the
    coupled compliance-centre \(6\times6\) stiffness and damping. The
    approximately \(0.200\,\mathrm{m}\) coordinate span takes \(4\,\mathrm{s}\),
    matching the timeout; the final \(+0.180\,\mathrm{m}\) coordinate is a
    virtual post-plane spring reference and not a claim of
    \(180\,\mathrm{mm}\) physical penetration. After \(2\,\mathrm{s}\), a
    \(150\,\mathrm{N\,m}\) change in the model-estimated external-moment norm may
    end the phase. This is a permissive placeholder rather than a calibrated
    contact criterion, so unless such a large change occurs the independent
    \(4\,\mathrm{s}\) timeout governs the transition.
    \item In \texttt{grind} the final set-up preload is retained and a quintic
    smooth ping-pong sweep of \(0.03\,\mathrm{m}\) amplitude at
    \(0.2\,\mathrm{Hz}\) is commanded along \(t_2\). In the callback that
    satisfies the set-up exit condition, the controller switches immediately,
    without gain interpolation, to the decoupled set-up gain blocks; the
    point-shifted branch is no longer active. A wrench and
    \(J^\top w_{\mathrm{cmd}}\) step is therefore possible at this hand-off.
    \item The run stops with \texttt{e+Enter}, the buffered log is persisted,
    and the EE-inferred tool-axis error, edge and TCP displacement, commanded
    joint-torque trace and phase timing are evaluated.
\end{enumerate}

Between repetitions of a controlled comparison, only the declared independent
variable was changed. Runs were pooled only when the controller configuration,
tool geometry, phase duration, and fixed gains matched; all other runs were
treated as separate configurations. The resulting comparison identifies the
largest measured alignment among the tested compliance-centre settings for this
tool, plane, and contact sequence. It does not establish a universally optimal
stiffness matrix.

\section{Evaluation Metrics}
\label{sec:alignment_evaluation}
% Forward reference from the test matrix.
\label{sec:experimental_evaluation}
\ReviewMark{grey}{The equations are clear, but the primary angle depends on a
nominal tool-to-EE transform while the tool can rotate in the gripper. The
physical grinding-face angle is therefore not independently observed.}

The primary angle and the controller rotation error describe different
quantities. The nominal grinding-face axis inferred from the end-effector pose
is
\[
a_T=R_{\mathrm{EE}}a_{\mathrm{EE}} .
\]
Its angle to the independently calibrated physical plane is

\begin{equation}
\theta_{\mathrm{align,phys}}
=
\cos^{-1}\!\left(-a_T^\top n_{\mathrm{phys}}\right).
\label{eq:contact_alignment_error_cross}
\end{equation}

Both \(a_T\) and \(n_{\mathrm{phys}}\) are unit vectors, so the argument lies in
\([-1,1]\) and the inverse cosine is defined for every attainable pose. The
minus sign follows from the tool-axis convention in
\cref{eq:implemented_tool_axis_target}. The value is inferred from the
end-effector pose. Rotation of the tool within the gripper is therefore part of
the measurement uncertainty rather than a separate state in the calculation.
The corresponding angle to the configured normal \(n_s\) is used by the
controller, but the physical-plane angle above is used for the reported
results.

The physical-plane alignment improvement is

\begin{equation}
\Delta\theta_{\mathrm{align,phys}}
=
\theta_{\mathrm{align,phys,before}}
-
\theta_{\mathrm{align,phys,after}}.
\label{eq:alignment_error_improvement}
\end{equation}

A positive value means that alignment improved. Because the tool mount carries
roughly \(2^\circ\) of rotational play, the metric describes the combined
robot--gripper--tool response. Contact-induced rotation is reported separately
as the finite before--after rotation vector

\begin{equation}
\Delta\theta_C
=
\operatorname{Log}\!\left(
R_{\mathrm{after}}R_{\mathrm{before}}^\top
\right)^\vee .
\label{eq:contact_induced_rotation}
\end{equation}

Displacement of the selected physical feature is

\begin{equation}
\Delta p_C
=
p_{C,\mathrm{after}}-p_{C,\mathrm{before}},
\qquad
s_C=\lVert\Delta p_C\rVert .
\label{eq:contact_feature_displacement}
\end{equation}

TCP displacement is reported in the same way. These quantities show whether
an angular improvement is accompanied by translation or sliding.

The controller error \(e_R\) is measured from the orientation held at the
clearance transition. It is therefore not the same as the physical-plane
alignment angle and is not used as the primary outcome.

\subsection{Metric Use by Experimental Case}
\label{sec:evaluation_criteria_contact}
\ReviewMark{red}{The bold metric catalogue and following case table repeat the
same information in two uniform forms. This templated duplication reads as
machine-organised rather than as necessary experimental reasoning.}

Physical-plane alignment improvement is the primary response in all five cases.
What separates them is the independent variable stored with each run, and the
supporting quantities carried alongside that primary outcome.

\noindent\textbf{Primary---physical-plane alignment improvement.}
\(\Delta\theta_{\mathrm{align,phys}}\), with a positive value denoting
improvement.

\noindent\textbf{Physical-plane residual.}
\(\theta_{\mathrm{align,phys,after}}\), reported as a range when the
physical-plane calibration uncertainty affects its absolute value.

\noindent\textbf{Contact-induced rotation.}
The magnitude and direction of the finite before--after rotation
\(\Delta\theta_C\).

\noindent\textbf{Contact displacement.}
Selected-feature and TCP displacement during set-up.

\noindent\textbf{Model-estimated normal load.}
The steady model-estimated normal load, explicitly distinguished from a direct
force measurement.

\noindent\textbf{Settling check.}
The residual changes in tip angle, configured-normal angle, and estimated load
after the selected evaluation time.

\begin{table}[H]
\centering
\small
\caption{Metric use by experimental case.}
\label{tab:metrics_by_test_case}
\begin{tabular}{@{}
>{\centering\arraybackslash}p{0.07\textwidth}
>{\raggedright\arraybackslash}p{0.40\textwidth}
>{\raggedright\arraybackslash}p{0.42\textwidth}@{}}
\toprule
Case & Quantities used & Role in the evaluation \\
\midrule
A &
Physical-plane alignment before and after set-up, its signed \(t_1\) and
\(t_2\) components, and the steady model-estimated normal load &
Establish the response to the commanded tilt and the baseline for the
remaining cases. \\
B &
The Case-A quantities against the rotational stiffness of the tilted tangent &
Quantify the separate \(K_{R,t_1}\) and \(K_{R,t_2}\) contributions. \\
C &
The Case-A quantities against the translational stiffness perpendicular to the
tilted tangent &
Quantify the translational contribution and the stiffness interaction. \\
D &
The Case-A quantities against the surface-frame compliance-centre coordinates,
with contact-induced rotation and feature displacement &
Quantify the response to lever placement, including its sign. \\
E &
Alignment improvement for tilt about \(y_{EE}\) and \(x_{EE}\), compared
with the Case-A tangent-axis tilts &
Attribute the tangent-axis asymmetry to the surface frame or to the tool
face. \\
\bottomrule
\end{tabular}
\end{table}

Commanded tilt, stiffness entry, and compliance-centre lever were stored with
each run as independent variables rather than evaluated as metrics. The primary
alignment outcome was interpreted together with displacement and estimated
load. Commanded torque, oscillation scores, and stability scores were excluded
from the reported outcomes.

\section{Safety Limits}
\label{sec:damping_safety_limits_contact}
\ReviewMark{grey}{The reflex thresholds and procedural guards are documented,
but they are not command saturations and do not establish a measured safety
margin. The large moment threshold is acknowledged as a permissive placeholder.}

Phase-specific damping comes from the task inertia through
\cref{eq:implementation_cartesian_inertia,eq:implementation_inertia_scaled_damping}.
This directional calculation does not establish contact safety. The implemented
controller remains discrete, model dependent, and coupled to a stiff environment
\citep{Pollayil2023ImpedancePlanning}.

Ahead of the torque callback the reported configuration calls
\texttt{setCollisionBehavior} with equal joint-side acceleration and nominal
thresholds of \(140\,\mathrm{N\,m}\), and equal Cartesian force and moment
thresholds of \(240\,\mathrm{N}\) or \(\mathrm{N\,m}\) across all six components.
These values specify the thresholds at which the Franka reflex is triggered;
they do not saturate the commanded torque or wrench. The user callback applies
no additional Cartesian-force, moment, joint-torque, or torque-rate limit. The
values document the robot configuration used in the experiments and do not
constitute a claim of human safety.

Procedure and geometry supply the remaining guards:

\begin{itemize}
    \item a \(0.640\,\mathrm{m}\) maximum approach travel and a geometric
    \(20\,\mathrm{mm}\) clearance hand-off;
    \item an operator gate before set-up, using a
    \(5000\,\mathrm{N/m}\) isotropic position hold with calculated
    inertia-scaled damping coefficients;
    \item a \(4\,\mathrm{s}\) set-up timeout independent of the permissive
    \(150\,\mathrm{N\,m}\) external-moment-change stopping threshold; the timeout
    is expected to govern unless the estimated moment changes exceptionally;
    \item the configured set-up stiffness and push must be checked against the
    collision settings before a run; the scalar product
    \(350\,\mathrm{N/m}\times0.180\,\mathrm{m}\approx63\,\mathrm{N}\) is only a
    base-\(z\) approximation, while the frame-consistent blocked-reference
    scales are \(65.3\,\mathrm{N}\) normal and \(70.2\,\mathrm{N}\) in total
    from \cref{eq:active_setup_translation_surface_representation}; none is a
    contact-force measurement.
\end{itemize}

The \(150\,\mathrm{N\,m}\) set-up threshold applies to the change in
libfranka's model-estimated external moment. It is not a measured contact
threshold. In the reported runs, set-up ended at the configured timeout rather
than at this threshold.
