\chapter{Experimental Methodology}
\label{ch:methodology}

This chapter defines the experimental methodology for the phase-controlled
surface-grinding and alignment study.

The method considered here investigates unscripted task-space alignment during
a phase-controlled surface-grinding sequence. The robot first orients the
physical grinding-face axis, descends to a geometrically defined clearance,
presses the selected leading tool feature into the plane during set-up, and then
executes the grinding motion. The response is evaluated from the physical
tool-axis angle, the rotation during contact, the feature and TCP
displacements, and the commanded joint torques.

Here, ``unscripted task-space alignment'' means that set-up holds the clearance
orientation as a soft reference and commands no time-varying task-space tipping
trajectory. The active null-space mode nevertheless adds a switched sigma
torque throughout the automatic sequence. The term therefore does not imply
isolated passive contact motion or closed-loop passivity.

\section{Experimental Question}
\label{sec:experimental_question}

The central experimental question is:

\begin{quote}
How should the Cartesian stiffness matrix \(K\) be selected so that the
pole-coupled impedance biases alignment during contact about a chosen local
linearized center of compliance, while the motion remains smooth and
within the configured hardware limits?
\end{quote}

This question connects three parts of the thesis. First, Cartesian impedance
control defines the relation between pose displacement and commanded wrench
\citep{Hogan1985,Ott2008,Albu2003}. Second, stiffness and damping selection
is a practical design problem in its own right, because the selected gains
determine how strongly disturbances, contact forces, and task errors are
converted into motion and commanded torque
\citep{Pollayil2023ImpedancePlanning}. Third, spatial impedance,
remote-center-compliance, and variable compliance-center ideas show that
translation and rotation can be coupled so that contact forces create
favorable alignment behavior instead of jamming or increasing misalignment
\citep{Fasse1997SpatialImpedance,Nevins1989ComputerControlledAssembly,Watson1978RCC,Wang2019VariableComplianceCenter,Hartisch2022FlexureRCC}.
Finally, the Franka Emika Panda implementation must keep the commanded joint
torques bounded and compatible with the real-time torque-control interface
\citep{FrankaFCI2023,Mayr2024CartesianImpedance,Hjorth2023EnergyAwareImpedance}.

\section{Robot and Software Setup}
\label{sec:robot_software_setup_contact}

The experiment is performed on the Franka Emika Panda using the torque-control
interface provided by libfranka. The controller runs inside the Franka Control
Interface with the nominal control period

\begin{equation}
T_s = 1\,\mathrm{ms}.
\label{eq:sampling_time_contact}
\end{equation}

At each control cycle, the current robot state is read, the Cartesian
impedance control law is evaluated, and the resulting joint torque command is
sent back to the robot. The controller uses the end-effector transformation
\(T_{\mathrm{EE}}\), the joint velocity vector \(\dot{q}\), the geometric
Jacobian \(J(q)\), and the model-based Coriolis term provided by the libfranka
model interface. The commanded torque has the form

\begin{equation}
\tau_{\mathrm{cmd}}
=
J^\top(q)\mathbf{F}_{\mathrm{cmd}}
+
\tau_{\mathrm{null}}
+
\tau_c(q,\dot{q}),
\label{eq:chapter4_contact_commanded_torque}
\end{equation}

where \(\mathbf{F}_{\mathrm{cmd}}\) is the commanded Cartesian impedance
wrench and \(\tau_{\mathrm{null}}\) is the projected null-space torque. The
gravity compensation is not added as a separate term in the user callback,
because the external torque command sent through libfranka is already handled
with robot-side gravity and motor-friction compensation by the Franka Control
Interface.

The implementation described in this chapter corresponds to the controller tag
\texttt{FULL-Working-Clean} (commit \texttt{bbe7e85}). Its active sequence is
\texttt{approach\_orient}, \texttt{approach\_descend}, \texttt{set\_up}, and
\texttt{grind}. Hold and manual-guidance modes are available at start-up but are
not additional contact phases. The null-space mode is selected independently
and, when enabled, remains active throughout the automatic sequence. The
version identifier and the parameter files must be recorded with every data set
so that phase names, gains, tool geometry, durations, and pole offsets are
unambiguous.

\subsection{Reproducibility Record and Missing Run Metadata}
\label{subsec:reproducibility_record}

The software stack inspected on the controller host is Ubuntu~20.04.3 with the
\texttt{5.9.1-rt20} \texttt{PREEMPT\_RT} kernel, \texttt{g++~9.3} using
C++14 and \texttt{-O2}, Eigen~3.3.7, and libfranka~0.7.1 at commit
\texttt{80197c6}. Together with controller tag \texttt{FULL-Working-Clean}
at commit \texttt{bbe7e85}, these values identify the software build. They do
not replace the physical metadata that must accompany each experimental
campaign. Table~\ref{tab:reproducibility_record} separates recovered facts
from information that is absent from the present repository; an absent entry
is left explicitly unresolved rather than inferred.

{\small
\begin{longtable}{@{}
>{\raggedright\arraybackslash}p{0.18\textwidth}
>{\raggedright\arraybackslash}p{0.31\textwidth}
>{\raggedright\arraybackslash}p{0.42\textwidth}@{}}
\caption{Reproducibility record for the current controller and experimental
setup.}
\label{tab:reproducibility_record}\\
\toprule
Item & Recovered or configured record & Information required with the run \\
\midrule
\endfirsthead
\multicolumn{3}{c}{\tablename~\thetable\ continued} \\[2pt]
\toprule
Item & Recovered or configured record & Information required with the run \\
\midrule
\endhead
\midrule
\multicolumn{3}{r}{Continued on the next page} \\
\endfoot
\bottomrule
\endlastfoot
Controller &
\texttt{FULL-Working-Clean}, commit \texttt{bbe7e85} &
Archive the five parameter files and any uncommitted parameter changes. \\
Operating system and kernel &
Ubuntu~20.04.3; Linux \texttt{5.9.1-rt20} with
\texttt{PREEMPT\_RT} &
Record the booted kernel and real-time scheduling settings on the actual run
date. \\
Compiler and libraries &
\texttt{g++~9.3}, C++14, \texttt{-O2}; Eigen~3.3.7;
libfranka~0.7.1, commit \texttt{80197c6} &
Archive the build output or dependency manifest. \\
Real-time computer &
CPU, memory, network interface, and scheduling configuration are not recorded &
Record the host model, CPU, memory, FCI network path, process priority, and CPU
affinity. \\
Tool and surface &
Tool-face geometry is configured, but material grades, abrasive/tool part, and
surface material are not recorded &
Record material, surface finish, wear state, and tool identifier. \\
Fixture and friction condition &
Not recorded &
Photograph and describe the tool and workpiece fixation; state whether contact
is dry, lubricated, or uses coolant, and record any measured or assumed
friction range. \\
Payload &
The program calls neither \texttt{setLoad} nor \texttt{setEE}; numeric
robot-side tool/payload mass, center of mass, and inertia are not archived &
Record the active payload mass, center of mass, inertia tensor, and the
procedure used to configure them on the robot. \\
Robot transforms &
\texttt{F\_T\_EE} and \texttt{EE\_T\_K} are supplied in the runtime robot
state, but their numeric matrices are not archived &
Store both \(4\times4\) transforms with every campaign and identify the TCP and
stiffness-frame conventions. \\
Plane calibration &
The controller records a configured point and normal; no measured calibration
residual or uncertainty is archived &
Store the touched points, fitted plane, normal-sign rule, residuals, and
repeatability estimate described below. \\
\end{longtable}
}

The controller contains no environment contact or friction model and uses no
friction coefficient in its command. Friction therefore remains an
uncontrolled physical property of a run unless the materials, surface
condition, lubrication state, and an assumed or measured range are recorded.

\begin{figure}[H]
  \centering
  \IfFileExists{figures/experimental_setup_photo.jpg}{%
    \includegraphics[width=0.82\textwidth]{figures/experimental_setup_photo.jpg}
    \caption{Experimental setup, showing the Panda, tool, workpiece, and
    fixation.}
  }{%
    \fbox{\parbox[c][48mm][c]{0.82\textwidth}{\centering
      Experimental setup photograph not yet archived.\\[3pt]
      Add \texttt{figures/experimental\_setup\_photo.jpg} showing the complete
      robot, tool, workpiece, and fixture.}}
    \caption{Required experimental-setup photograph placeholder.}
  }
  \label{fig:experimental_setup_photo}
\end{figure}

\subsection{Plane Calibration Protocol}
\label{subsec:plane_calibration_protocol}

The source parameters specify a plane analytically, but a reproducible
experiment should determine it from at least three non-collinear points and
preferably more than three so that a residual can be estimated. For measured
surface points \(p_j\), \(j=1,\ldots,N\), compute

\begin{equation}
\bar p=\frac{1}{N}\sum_{j=1}^{N}p_j,
\qquad
S=\sum_{j=1}^{N}(p_j-\bar p)(p_j-\bar p)^\top .
\label{eq:plane_fit_scatter}
\end{equation}

The least-squares plane point is \(p_s=\bar p\), and \(n_s\) is the normalized
eigenvector of \(S\) associated with its smallest eigenvalue. Its sign is chosen
against a recorded reference direction so that the commanded descent remains
\(-n_s\). The normal residual is reported as

\begin{equation}
\epsilon_{\mathrm{plane,rms}}
=
\sqrt{\frac{1}{N}\sum_{j=1}^{N}
\left[n_s^\top(p_j-\bar p)\right]^2}.
\label{eq:plane_fit_rms}
\end{equation}

Repeating the point acquisition provides a position spread for \(p_s\) and an
angular spread between fitted normals. These two quantities, together with the
touch/reference tool and the numeric
\(\texttt{F\_T\_EE}\) and \(\texttt{EE\_T\_K}\) transforms, form the plane
calibration uncertainty record. With exactly three noiseless points the plane
is defined, but the same three points cannot provide a meaningful residual
uncertainty estimate.

\section{Contact Geometry}
\label{sec:contact_geometry}

The task geometry is defined by a surface plane, a tool edge, and a selected
local linearized center of compliance. Let

\begin{equation}
n_s \in \mathbb{R}^3,
\qquad
\|n_s\| = 1
\label{eq:surface_normal}
\end{equation}

be the surface normal expressed in the robot base frame. In the implemented
configuration it is generated from rotations \(a\) about base \(x\) and \(b\)
about base \(y\),

\begin{equation}
n_s
=
R_y(b)R_x(a)e_z
=
\begin{bmatrix}
\sin(b)\cos(a) & -\sin(a) & \cos(b)\cos(a)
\end{bmatrix}^{\top}.
\label{eq:implemented_surface_normal_angles}
\end{equation}

The current values are \(a=0^\circ\) and \(b=5^\circ\), and the plane passes
through \(p_s=[\,0.55,\,-0.06,\,0.025\,]^\top\,\mathrm{m}\). Two orthonormal
tangent directions \(t_1\) and \(t_2\) span the surface plane. The first tangent
is obtained by projecting the configured auxiliary direction into the plane,
and \(t_2=n_s\times t_1\). The implemented column order is therefore

\begin{equation}
R_{\mathrm{surface}}
=
\begin{bmatrix}
t_1 & t_2 & n_s
\end{bmatrix},
\label{eq:surface_task_frame}
\end{equation}

consistent with the surface task frame of
\cref{eq:surface_task_frame_matrix}. Consequently, every surface-frame vector
or diagonal gain in this chapter follows the order
\([\,t_1,t_2,n_s\,]\), unless a table explicitly states another semantic order.

The grinding face is represented explicitly rather than by one fixed point. Its
centre is \(20\,\mathrm{mm}\) along \(+Z_{\mathrm{EE}}\) from the end-effector
origin, with total dimensions \(40\,\mathrm{mm}\) along \(X_{\mathrm{EE}}\) and
\(120\,\mathrm{mm}\) along \(Y_{\mathrm{EE}}\). The controller transforms its
four corners to the base frame and selects the leading corner, edge, or complete
face along the descent direction. Features whose projected heights differ by no
more than \(0.1\,\mathrm{mm}\) are averaged. This prevents an exact single-axis
tilt from arbitrarily choosing one endpoint of a physical edge.

Let \(a_{\mathrm{tool,EE}}\) denote the configured physical face-normal axis.
For the current tool \(a_{\mathrm{tool,EE}}=e_z\), and the orientation target
satisfies

\begin{equation}
R_d a_{\mathrm{tool,EE}}=-n_s .
\label{eq:implemented_tool_axis_target}
\end{equation}

The minus sign points the \(+Z_{\mathrm{EE}}\) grinding face toward the surface.
The controller applies the minimum rotation that maps the starting physical
tool axis to \(-n_s\), thereby preserving the remaining twist as closely as
possible; it does not set \(R_d=R_{\mathrm{surface}}\).

The selected centre of compliance is represented by a point \(p_c\), with the
pole-to-TCP lever \(r_c=p_{\mathrm{TCP}}-p_c\) defined in
\cref{eq:pole_to_tcp_lever}. Here \(p_{\mathrm{TCP}}\) is the origin associated with
\texttt{state.O\_T\_EE}; any configured \texttt{F\_T\_EE} transform must
therefore be included when relating this point to the physical hand and tool.
The lower-case subscript \(c\) is reserved for the chosen virtual centre
\(p_c\). The upper-case subscript \(C\) is used later for the physical contact
point \(p_C\), for which the distinct contact lever is
\(r_C=p_C-p_{\mathrm{TCP}}\). These two lever arms have different directions
and must not be interchanged.

If a force \(\mathbf{f}\) acts at the contact, the corresponding moment about
the TCP depends on \(r_C\). Therefore, the selected stiffness
matrix should not be interpreted only as three independent translational
springs and three independent rotational springs. For a virtual-center
behavior, translation and rotation must be selected consistently.

\section{Gain Selection Methodology}
\label{sec:cartesian_stiffness_selection}

This chapter keeps the notation of Chapter~\ref{ch:theory}. A subscript
\(\mathrm{task}\) means that a gain is written in a local task frame; for a
surface-frame quantity the axis order is \([t_1,t_2,n_s]\). The implementation
does not, however, use one common frame for every phase and every gain block:

\begin{itemize}
    \item approach translation and rotation are diagonal in the surface frame
    and transformed to the base frame;
    \item set-up and grind translation are diagonal directly in base
    \([x,y,z]\), while their rotational gains are diagonal in
    \([t_1,t_2,n_s]\);
    \item hold gains are isotropic in the base frame; the enabled pre-set-up
    gate overrides only translation with an isotropic base-frame gain and
    retains the isotropic approach rotational gain; and
    \item during set-up only, the selected base-frame translational and
    rotational blocks can be shifted from a commanded pole to the TCP, producing
    a coupled \(6\times6\) gain.
\end{itemize}

The phase-complete stiffness map is given in
\cref{tab:active_stiffness_design_points}; the matching automatic-damping
policy is given in \cref{tab:active_automatic_damping_policy}. These tables
include hold, both approach states, the pre-set-up gate, set-up, grind, and
manual guidance. Frame labels are part of every gain value and must be retained
when a value is calculated or changed.

The connection to the controller notation is the following. The controller law
of \cref{eq:cartesian_impedance_control_law} uses base-frame errors and
base-frame gains. For the experimental interpretation, the same quantities can
be written in the surface task frame as

\begin{equation}
\mathbf{e}_{\mathrm{task}}
=
\begin{bmatrix}
\mathbf{e}_{p,\mathrm{task}}\\
\mathbf{e}_{R,\mathrm{task}}
\end{bmatrix}
=
T_R^\top
\begin{bmatrix}
\mathbf{e}_p\\
\mathbf{e}_R
\end{bmatrix},
\qquad
\dot{x}_{\mathrm{task}}
=
T_R^\top
\begin{bmatrix}
\dot{\mathbf{p}}_{\mathrm{EE}}\\
\omega_{\mathrm{EE}}
\end{bmatrix},
\label{eq:experiment_task_frame_errors}
\end{equation}

where \(T_R=\operatorname{diag}(R_{\mathrm{surface}},R_{\mathrm{surface}})\).
This representation is used when both blocks are being interpreted in the
surface frame. It must not be used to imply that the implemented set-up
translation block is surface-frame diagonal; that block is already defined in
base XYZ.

The complete local Cartesian stiffness matrix uses the same task-frame notation
introduced in \cref{eq:full_task_frame_gain_blocks}. In the standard
spatial-stiffness block form \citep{Fasse1997SpatialImpedance,Ott2008},

\begin{equation}
K_{\mathrm{task}}
=
\begin{bmatrix}
K_{p,\mathrm{task}} & K_{pR,\mathrm{task}}\\
K_{Rp,\mathrm{task}} & K_{R,\mathrm{task}}
\end{bmatrix}.
\label{eq:contact_stiffness_block}
\end{equation}

Here, \(K_{p,\mathrm{task}}\) relates translational displacement to force,
\(K_{R,\mathrm{task}}\) relates rotational displacement to moment, and the
off-diagonal blocks \(K_{pR,\mathrm{task}}\) and \(K_{Rp,\mathrm{task}}\)
describe translation--rotation coupling.
This block form is useful for explaining the physical coupling between sliding
and rotation during edge contact. The current controller constructs the full
matrix analytically from a manually selected pole during set-up. The pole and
the source stiffness blocks are the design variables; the 36 matrix entries are
not tuned independently.

For a passive linear stiffness, the complete matrix should be symmetric
positive definite \citep{Fasse1997SpatialImpedance,Ott2008},

\begin{equation}
K_{\mathrm{task}} = K_{\mathrm{task}}^\top,
\qquad
K_{\mathrm{task}} \succ 0.
\label{eq:contact_stiffness_spd}
\end{equation}

This condition is important because it allows the stiffness to be interpreted
as stored elastic energy \citep{Hogan1985,Ott2008,Fasse1997SpatialImpedance},

\begin{equation}
V_K
=
\frac{1}{2}
\mathbf{e}_{\mathrm{task}}^\top
K_{\mathrm{task}}
\mathbf{e}_{\mathrm{task}},
\qquad
\mathbf{e}_{\mathrm{task}} =
\begin{bmatrix}
\mathbf{e}_{p,\mathrm{task}}\\
\mathbf{e}_{R,\mathrm{task}}
\end{bmatrix}.
\label{eq:contact_elastic_energy}
\end{equation}

This energy interpretation is also the reason why the thesis separates the
geometric usefulness of a stiffness setting from its safety. A setting can
create the intended local center-of-compliance geometry but still be rejected
if it stores too much energy, creates large torque peaks, or requires damping
that is not reliable in contact \citep{Hjorth2023EnergyAwareImpedance}.
Symmetry and positive definiteness of the source stiffness, and positive
semidefiniteness of the damping, are algebraic properties of the local
impedance matrices. They do not by themselves prove closed-loop stability or
passivity of the sampled robot--controller--environment system. Such a claim
would additionally require the robot dynamics, delays, phase switching,
time-varying references, active null-space torque, and contact model to be
included in the analysis.

\subsection{Quasi-Static Stiffness Sizing and Automatic Damping}
\label{subsec:initial_stiffness_damping_values}

The quasi-static calculation is an offline method for sizing and tracing the
stiffness values. It is not executed by the controller and it is not a contact
force estimator. At low velocity, the elastic parts of the implemented command
are considered separately from damping and inertia,

\begin{equation}
\mathbf f_K=K_p\mathbf e_p,
\qquad
\mathbf m_K=K_R\mathbf e_R .
\label{eq:quasi_static_translational_stiffness}
\end{equation}

For a principal coordinate \(i\), a declared elastic force--error design pair
\((F_i^\star,\Delta x_i^\star)\), or moment--angle pair
\((M_i^\star,\Delta\theta_i^\star)\), gives

\begin{align}
k_{p,i}
&=
\frac{F_i^\star}{\Delta x_i^\star},
\label{eq:initial_translational_stiffness}\\
k_{R,i}
&=
\frac{M_i^\star}{\Delta\theta_i^\star}.
\label{eq:initial_rotational_stiffness}
\end{align}

The starred quantities are engineering design scales. They are not measured
contact loads, certified maxima, or software limits. This distinction prevents
a chosen virtual-spring load from being reported as an observed environment
force. If a moment scale is estimated from an offset force, its component about
the selected unit axis \(a_i\) is
\[
M_i^\star
=
\left|a_i^\top(r_C\times f_C)\right|
\leq
\lVert r_C\rVert\lVert f_C\rVert ,
\]
rather than the scalar lever-arm bound being used without its axis direction
\citep{Murray1994,Siciliano2009}.

\paragraph{Frame-consistent directional stiffness.}
The scalar rule in \cref{eq:initial_translational_stiffness} applies directly
only in a principal-gain frame. If a gain is diagonal in frame \(R_F\), its
base-frame matrix and its effective slope along a base-frame unit direction
\(u\) are

\begin{equation}
K_{p,0}
=
R_F\operatorname{diag}(k_{p,1},k_{p,2},k_{p,3})R_F^\top,
\qquad
k_{p,\mathrm{eff}}(u)
=
u^\top K_{p,0}u
=
\sum_{i=1}^{3}k_{p,i}(r_i^\top u)^2 .
\label{eq:directional_effective_stiffness}
\end{equation}

For an error \(\mathbf e_p=\delta u\), the elastic component along \(u\) is
\(\delta k_{p,\mathrm{eff}}(u)\). A non-principal direction can also produce
the transverse component
\(\delta(I-uu^\top)K_{p,0}u\). The same construction applies to
\(K_{R,0}\) and a rotational direction. This calculation is essential for the
active set-up translation, which is diagonal in base XYZ while the push follows
the tilted surface normal.

\paragraph{Set-up/grind calculation.}
The active \(t_2\) direction equals base \(y\). Taking the completely blocked
\(30\,\mathrm{mm}\) grind stroke as a \(60\,\mathrm{N}\) elastic design scale
gives

\begin{equation}
k_y=\frac{60}{0.030}=2000\,\mathrm{N/m},
\qquad
k_x=k_y .
\label{eq:setup_lateral_stiffness_design}
\end{equation}

For the press direction, choose a nominal \(65\,\mathrm{N}\) elastic scale at
the configured \(0.180\,\mathrm{m}\) endpoint, so that
\(k_{n,\mathrm{eff}}^\star=65/0.180=361.11\,\mathrm{N/m}\).
The coordinate starts near \(-0.020\,\mathrm{m}\) at the clearance hand-off
and ends at \(+0.180\,\mathrm{m}\). The complete scheduled reference travel is
therefore approximately \(0.200\,\mathrm{m}\), which takes \(4\,\mathrm{s}\) at
\(0.050\,\mathrm{m/s}\) and deliberately matches the set-up timeout. The first
\(20\,\mathrm{mm}\) closes the geometric clearance; the \(180\,\mathrm{mm}\)
endpoint is the virtual post-plane spring coordinate used for the blocked-load
design scale. It is neither a commanded material penetration nor an expected
robot displacement.
With \(a=0^\circ\), \(b=5^\circ\), and
\(K_{p,0}=\operatorname{diag}(k_x,k_y,k_z)\), solving
\[
k_{n,\mathrm{eff}}^\star
=
k_x\sin^2 b+k_z\cos^2 b
\]
gives

\begin{equation}
k_z
=
\frac{k_{n,\mathrm{eff}}^\star-k_x\sin^2(5^\circ)}
     {\cos^2(5^\circ)}
=
348.6\,\mathrm{N/m}
\;\longrightarrow\;
350\,\mathrm{N/m}.
\label{eq:setup_base_z_stiffness_design}
\end{equation}

Thus the implemented rounded block is
\(\operatorname{diag}(2000,2000,350)\,\mathrm{N/m}\) in base XYZ. Expressing
this same matrix in the active surface frame gives

\begin{equation}
R_{\mathrm{surface}}^\top K_{p,0}R_{\mathrm{surface}}
\approx
\begin{bmatrix}
1987.47 & 0 & 143.26\\
0 & 2000 & 0\\
143.26 & 0 & 362.53
\end{bmatrix}
\mathrm{N/m}.
\label{eq:active_setup_translation_surface_representation}
\end{equation}

Consequently, a completely blocked \(0.180\,\mathrm{m}\) normal reference
change would correspond to approximately \(65.3\,\mathrm{N}\) along the normal,
\(25.8\,\mathrm{N}\) along \(t_1\), and \(70.2\,\mathrm{N}\) total elastic-force
magnitude. These are conservative virtual-spring scales for a hypothetical
full tracking error. The robot normally follows part of the reference, so they
are neither predicted measured forces nor physical penetration values.

For the rotational block, a \(15\,\mathrm{N}\) design force through the
\(60\,\mathrm{mm}\) tool half-length gives a moment scale of approximately
\(0.90\,\mathrm{Nm}\). Allowing about \(10^\circ\) about the two surface
tangents but only \(1^\circ\) about the surface normal and converting the
angles to radians gives

\begin{equation}
k_{R,t_1}=k_{R,t_2}
\approx\frac{0.90}{0.1745}=5.16
\;\longrightarrow\;5,
\qquad
k_{R,n}
\approx\frac{0.90}{0.01745}=51.6
\;\longrightarrow\;50
\quad\mathrm{Nm/rad}.
\label{eq:setup_rotational_stiffness_design}
\end{equation}

The ten-to-one ratio makes tipping about the tangents compliant while retaining
stronger twist restraint. During set-up these are source gains at the selected
pole; after the congruence shift they are not, in general, the diagonal
rotational slopes seen for a pure rotation at the TCP. Grind uses the same
source blocks without the pole shift.

\begin{table}[H]
\centering
\small
\caption{Phase-complete active stiffness and equivalent design-point
interpretation. The design points reproduce or explain the selected values;
they are not measured contact loads or certified limits.}
\label{tab:active_stiffness_design_points}
\begin{tabular}{@{}
>{\raggedright\arraybackslash}p{0.16\textwidth}
>{\raggedright\arraybackslash}p{0.33\textwidth}
>{\raggedright\arraybackslash}p{0.41\textwidth}@{}}
\toprule
Phase / mode & Source stiffness and frame & Design-point interpretation and command structure \\
\midrule
Hold &
\(K_p=300I_3\,\mathrm{N/m}\) and
\(K_R=40I_3\,\mathrm{Nm/rad}\), base &
\(3\,\mathrm{N}\) at \(10\,\mathrm{mm}\);
\(0.70\,\mathrm{Nm}\) at \(1^\circ\); decoupled. \\
Approach orient and descend &
\(K_p=150I_3\,\mathrm{N/m}\) and
\(K_R=90I_3\,\mathrm{Nm/rad}\), surface definition &
\(3\,\mathrm{N}\) at \(20\,\mathrm{mm}\);
\(3.15\,\mathrm{Nm}\) at the \(0.035\,\mathrm{rad}\) orientation threshold;
decoupled. \\
Pre-set-up gate &
\(K_p=5000I_3\,\mathrm{N/m}\), base; retains
\(K_R=90I_3\,\mathrm{Nm/rad}\) &
\(25\,\mathrm{N}\) at \(5\,\mathrm{mm}\); only translation is overridden;
decoupled. \\
Set-up &
\(k_p=[2000,2000,350]\,\mathrm{N/m}\), base XYZ;
\(k_R=[5,5,50]\,\mathrm{Nm/rad}\), surface \([t_1,t_2,n_s]\) &
\(60\,\mathrm{N}\) per blocked \(30\,\mathrm{mm}\) \(t_2\) stroke and
approximately \(65\,\mathrm{N}\) per blocked \(180\,\mathrm{mm}\) normal
reference; source blocks are shifted through the frozen pole. \\
Grind &
same \(K_p\) and \(K_R\) source blocks as set-up &
Same design scales; the command is decoupled and the active sweep is along
\(t_2\). \\
Manual guidance &
no Cartesian stiffness &
Coriolis compensation plus \(0.5\,\mathrm{Nm\,s/rad}\) joint damping. \\
\bottomrule
\end{tabular}
\end{table}

The optional pre-grind gate is disabled. If enabled, it remains in set-up and
continues the coupled pressed command; it does not activate the
\(5000\,\mathrm{N/m}\) pre-set-up lock. Null-space mode~3 adds a projected
joint torque independently of the Cartesian stiffness map.

\paragraph{Automatic damping.}
Stiffness is selected offline, whereas Cartesian damping is calculated by the
controller at entry to each phase group. Starting from the joint-space mass
matrix and Jacobian, the implementation forms

\begin{equation}
\begin{aligned}
\mathcal H_0&=JM_q^{-1}J^\top,\\
\Lambda_0&=\operatorname{sym}\!\left[
\left(\operatorname{sym}(\mathcal H_0)+10^{-9}I_6\right)^{-1}
\right],\\
\Lambda_F&=\operatorname{sym}\!\left(T_F^\top\Lambda_0T_F\right),
\qquad
\operatorname{sym}(A)=\tfrac12(A+A^\top).
\end{aligned}
\label{eq:implemented_cartesian_inertia}
\end{equation}

Only the positive diagonal entries of \(\Lambda_F\) are retained. For each
principal stiffness \(k_i\), the coefficient is

\begin{equation}
d_i
=
\min\!\left(
d_{\max},
\max\!\left(d_{i,\min},\,2\zeta\sqrt{\Lambda_{F,ii}k_i}\right)
\right).
\label{eq:directional_damping_heuristic}
\end{equation}

Here \(d_{\max}=8000\). In the active configuration,
\texttt{auto\_damping\_min\_from\_manual=0}, hence \(d_{i,\min}=0\); configured
manual coefficients are fallbacks, not lower bounds. For a fitted phase, let
\(c_i=2\sqrt{\Lambda_{F,ii}k_i}\). The least-squares factor used to approach a
target vector \(d^\star\) is

\begin{equation}
\beta^\star
=
\frac{c^\top d^\star}{c^\top c}.
\label{eq:fitted_automatic_damping_factor}
\end{equation}

\begin{table}[H]
\centering
\small
\caption{Active automatic-damping policy. Bracketed fallbacks follow the frame
order in the second column.}
\label{tab:active_automatic_damping_policy}
\begin{tabular}{@{}
>{\raggedright\arraybackslash}p{0.17\textwidth}
>{\raggedright\arraybackslash}p{0.23\textwidth}
>{\raggedright\arraybackslash}p{0.27\textwidth}
>{\raggedright\arraybackslash}p{0.24\textwidth}@{}}
\toprule
Phase / mode & Inertia/gain frame & Factor or fitted target & Manual fallback \\
\midrule
Approach orient and descend &
surface for translation and rotation &
\(\zeta=1.9\), computed once and shared &
\(D_p=[20,20,20]\);
\(D_R=[12,12,12]\) \\
Pre-set-up gate &
base translation; surface rotation &
separate \(\beta_p^\star\) toward \(D_p=200I_3\) and
\(\beta_R^\star\) toward cached approach \(D_R\) &
\(D_p=200I_3\); inherited cached or manual approach \(D_R\) \\
Set-up and grind &
base translation; surface rotation &
\(\zeta=1.0\), computed after clearance and shared &
\(D_p=[10,10,175]\);
\(D_R=[10.01,10.01,10]\) \\
Hold &
base for translation and rotation &
one \(\beta^\star\) fitted toward \(D_p=50I_3\), then also applied to rotation &
\(D_p=50I_3\);
\(D_R=8I_3\) \\
Manual guidance &
not applicable &
no Cartesian automatic damping &
\(0.5\,\mathrm{Nm\,s/rad}\) joint damping \\
\bottomrule
\end{tabular}
\end{table}

Approach and set-up retry the inertia calculation on a later callback when it
is invalid. Hold and the pre-set-up gate latch their fallback for that entry.
The resulting numeric damping is pose dependent and is printed at phase entry;
it is not a constant determined by the stiffness table and is not currently a
field in the general sequence CSV.

Equation~\eqref{eq:directional_damping_heuristic} is a directional damping
heuristic based on the diagonal entries of the TCP task-inertia estimate; it
omits off-diagonal inertia coupling. Here \(\zeta\) is a dimensionless tuning
factor, not an identified modal damping ratio. During set-up, the source
damping blocks are calculated at the TCP and are then shifted through the pole
with the same congruence as stiffness. The shifted matrix remains symmetric
positive semidefinite, but \(\zeta=1\) does not prove exact modal critical
damping of the coupled pole/contact system.

\subsection{Pole-Based Gain Design}
\label{subsec:impedance_around_pole}

The pole-based design is the construction used during \texttt{set\_up}. A
surface-frame rotation changes the principal directions of the translational
and rotational blocks but leaves the translation--rotation blocks zero. It
therefore does not move the centre of compliance away from the TCP. The
point-shift construction of
\cref{eq:offset_adjoint,eq:coupled_tcp_stiffness,eq:coupled_tcp_damping}
creates the required translation--rotation coupling. The complete sign,
energy, and block-matrix derivation is kept in
\cref{sec:coupled_pole_stiffness}; it is not repeated here.

The active source stiffness, written wholly in base-frame coordinates, is

\begin{equation}
K_{\mathrm{pole},0}
=
\operatorname{blkdiag}\!\left(
\operatorname{diag}(2000,2000,350),
R_{\mathrm{surface}}\operatorname{diag}(5,5,50)
R_{\mathrm{surface}}^\top
\right).
\label{eq:active_mixed_frame_pole_stiffness}
\end{equation}

The entries of the translational and rotational blocks have units
\(\mathrm{N/m}\) and \(\mathrm{Nm/rad}\), respectively. Equation
\eqref{eq:active_mixed_frame_pole_stiffness} makes the mixed-frame construction
explicit: translation is diagonal in base XYZ, whereas only rotation is
diagonal in surface coordinates \([t_1,t_2,n_s]\).

This construction changes the gain matrices without adding a separate force,
moment, or time-varying task-space rotation command. Its experimental effect is
judged from the physical tool-axis alignment, selected-feature and TCP
displacements, and commanded joint torques.

\paragraph{Choosing the pole.}
The implementation accepts an arbitrary base-frame offset from the projected
leading-feature reference captured at clearance:

\begin{equation}
p_c=p_{\Pi,\mathrm{clr}}+\Delta p_c^0,
\qquad
\Delta p_c^0=
\begin{bmatrix}-0.04&0.08&0\end{bmatrix}^{\top}\mathrm{m}.
\label{eq:active_pole_choice}
\end{equation}

The superscript \(0\) emphasizes that the three components are base XYZ, not
\([t_1,t_2,n_s]\). The reference is frozen for the run. A scalar choice
\(p_c=p_s+h n_s\) is a useful theoretical special case, but it is not the active
parameterization. Because \texttt{coupled\_pole\_freeze\_at\_contact=1}, the
active lever is also frozen,
\[
r_c=p_{\mathrm{TCP,clr}}-p_c,
\]
where \(p_{\mathrm{TCP,clr}}\) is the TCP position captured at the same
clearance hand-off. Each \(\Delta p_c^0\) yields different coupled
\(K_{\mathrm{TCP}},D_{\mathrm{TCP}}\) through
\cref{eq:coupled_tcp_stiffness,eq:coupled_tcp_damping}; the sign and frame of
the vector must therefore
be stored with every trial.

For the configured \(a=0^\circ\), \(b=5^\circ\) surface, resolving the
base-XYZ offset in surface coordinates gives

\begin{equation}
R_{\mathrm{surface}}^\top\Delta p_c^0
\simeq
\begin{bmatrix}-39.85&80.00&-3.49\end{bmatrix}^{\top}\mathrm{mm}.
\label{eq:active_pole_offset_surface_components}
\end{equation}

The pole is therefore placed mainly in the \(-t_1,+t_2\) quadrant relative to
the projected selected feature. The offset is not itself the lever used by the
adjoint: the exact lever also contains the captured TCP-to-feature geometry.
For a purely translational error, define \(f=K_{p,0}e_p\). The moment produced
by the coupling block is the direct sign check

\begin{equation}
m_c=[r_c]_\times^\top f=-r_c\times f.
\label{eq:active_pole_moment_sign_check}
\end{equation}

At the nominal aligned geometry, the transverse lever is approximately
\(0.03985t_1-0.08t_2\). A blocked press with
\(f\approx-Fn_s\), \(F>0\), consequently gives
\(m_c\approx-F(0.08t_1+0.03985t_2)\): the selected quadrant biases negative
rotation about both surface tangents. Reversing either transverse offset
reverses the associated moment bias. This is the geometric rationale for the
chosen direction. It is nevertheless a directional design hypothesis, not a
guarantee of correction: the exact base-diagonal force need not be perfectly
normal, and the bias assists alignment only when its signed components oppose
the signed residual orientation error. That comparison is deferred until the
physical alignment signal is added to the run record.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[>=Latex,font=\small,scale=1.12]
    \coordinate (Q) at (0,0);
    \coordinate (C) at (-1.85,0.20);
    \coordinate (F) at (0.20,1.05);
    \coordinate (T) at (1.10,2.85);
    \fill[gray!10]
      (-3.0,-0.65)--(3.0,-0.65)--(4.0,0.15)--(-2.0,0.15)--cycle;
    \draw[gray!65]
      (-3.0,-0.65)--(3.0,-0.65)--(4.0,0.15)--(-2.0,0.15)--cycle;
    \draw[->,thick] (Q)--++(2.55,0) node[below right] {$t_1$};
    \draw[->,thick] (Q)--++(1.55,0.85) node[below right] {$t_2$};
    \draw[->,thick] (Q)--++(-0.65,2.05) node[above left] {$n_s$};
    \draw[densely dashed] (Q)--(F);
    \draw[thick] (F)--(T);
    \draw[->,very thick,orange!80!black] (Q)--(C)
      node[midway,above] {$\Delta p_c^0$};
    \draw[->,very thick,blue!70!black] (C)--(T)
      node[pos=0.58,above left] {$r_c=p_{\mathrm{TCP}}-p_c$};
    \filldraw (Q) circle (1.6pt)
      node[below=4pt] {$p_{\Pi,\mathrm{clr}}$};
    \filldraw (C) circle (1.6pt) node[above left] {$p_c$};
    \filldraw (F) circle (1.6pt)
      node[right=4pt,yshift=5pt] {selected feature at clearance};
    \filldraw (T) circle (1.6pt) node[above right] {$p_{\mathrm{TCP}}$};
  \end{tikzpicture}
  \caption{Schematic frozen set-up geometry. The pole is offset from the
  selected feature projected onto the plane, whereas the congruence uses the
  pole-to-TCP lever \(r_c\). Because the offset is specified in base XYZ,
  \(p_c\) need not lie exactly in the surface plane. The drawing is not to
  scale.}
  \label{fig:active_pole_geometry}
\end{figure}

The selected point \(p_c\) is a local linearized center of compliance. Finite
rotational stiffness, the frozen lever, environment friction and constraints,
and the active null-space sigma torque mean that it is not necessarily the
physical pivot observed during contact.

\subsection{Code-Aligned Selection Order}
\label{sec:role_of_gain_methods}

The calculation and implementation must be applied in the same order as the
controller:

\begin{enumerate}
    \item State a phase-specific force--error or moment--angle design pair and
    calculate the principal source stiffness using
    \cref{eq:initial_translational_stiffness}.
    \item Attach each diagonal to its implemented frame, form the base-frame
    block, and check the commanded path with
    \cref{eq:directional_effective_stiffness}. For set-up, the base-XYZ
    translation and surface-frame rotation blocks remain distinct.
    \item At phase-group entry, calculate and cache the damping from the
    current TCP task inertia using
    \cref{eq:implemented_cartesian_inertia,eq:directional_damping_heuristic}.
    \item During set-up only, shift the selected source stiffness and damping
    through the frozen pole using
    \cref{eq:coupled_tcp_stiffness,eq:coupled_tcp_damping}. Grind immediately
    returns to the decoupled source blocks.
    \item Check the resulting elastic load and joint-torque scales, then change
    one declared gain family at a time while retaining the controller tag,
    frame labels, computed damping, and pole offset with the run record.
\end{enumerate}

The resulting workflow is

\begin{equation}
\begin{aligned}
\text{phase design envelope}
&\rightarrow \text{frame-labelled source }K
\rightarrow \text{task-inertia }D\\
&\rightarrow \text{set-up-only pole shift}
\rightarrow \text{load/torque check}
\rightarrow \text{controlled trial}.
\end{aligned}
\label{eq:gain_selection_workflow}
\end{equation}

\subsection{Controlled Adjustment Around the Active Gains}
\label{sec:direct_stiffness_comparison}

Let \(k_i^{(0)}\) denote one active principal value from
\cref{tab:active_stiffness_design_points}. A reproducible local sweep uses

\begin{equation}
k_i^{(\alpha)}=\alpha k_i^{(0)},
\qquad
\alpha\in\{0.5,\ 0.75,\ 1.0,\ 1.25,\ 1.5\},
\label{eq:active_gain_scale_sweep}
\end{equation}

while all other source gains, the pole offset, phase timing, and null-space
parameters remain fixed. The design load in that coordinate scales by
\(\alpha\). At an unchanged pose and with the same automatic-damping factor,
\cref{eq:directional_damping_heuristic} gives

\begin{equation}
d_i^{(\alpha)}
\approx
\sqrt{\alpha}\,d_i^{(0)} .
\label{eq:damping_change_with_stiffness}
\end{equation}

The controller performs this damping update automatically on the next entry to
the relevant phase group; a cached coefficient is not recomputed continuously
within the phase.

For contact-alignment tuning, the first independent family is
\((K_{R,t_1},K_{R,t_2})\), because it directly sets the allowed tipping
deflection. \(K_{R,n}\) is kept separate because it restrains twist. The next
families are the base \(x,y\) translation values and the effective normal
stiffness. If a new target \(k_{n,\mathrm{eff}}^\star\) is chosen while \(k_x\)
and the surface tilt \(b\) are fixed, the required base-\(z\) value follows
from
\[
k_z
=
\frac{k_{n,\mathrm{eff}}^\star-k_x\sin^2 b}{\cos^2 b},
\]
not by assigning the desired normal value directly to \(K_{p,zz}\).
Approach, gate, and hold gains are changed with the corresponding design pairs
in \cref{tab:active_stiffness_design_points}. Every run records the printed
automatic-damping coefficients because they cannot be reconstructed from
stiffness alone without the entry pose, \(J\), and \(M_q\).

\subsection{Null-Space Conditioning Parameter Rationale}
\label{subsec:nullspace_parameter_rationale}

The active mode combines projected joint damping with the two-point
smallest-singular-value conditioning bias derived in
\cref{sec:nullspace_svd}. The numerical values in
Table~\ref{tab:nullspace_parameter_rationale} are bounded engineering seed
values chosen to make the secondary action modest relative to the Cartesian
task and to suppress numerically ambiguous sign changes. They are not derived
design optima and have not yet been validated by a parameter sweep.

\begingroup
\small
\begin{longtable}{@{}
>{\raggedright\arraybackslash}p{0.20\textwidth}
>{\raggedright\arraybackslash}p{0.17\textwidth}
>{\raggedright\arraybackslash}p{0.54\textwidth}@{}}
\caption{Active null-space parameters, their immediate quantitative effect,
and their present engineering rationale.}
\label{tab:nullspace_parameter_rationale}\\
\toprule
Parameter & Active value & Quantitative consequence and rationale \\
\midrule
\endfirsthead
\multicolumn{3}{@{}l}{\textit{Table~\thetable\ continued}}\\
\toprule
Parameter & Active value & Quantitative consequence and rationale \\
\midrule
\endhead
\midrule
\multicolumn{3}{r@{}}{\textit{Continued on the next page}}\\
\endfoot
\bottomrule
\endlastfoot
Projected damping \(d_{\mathrm{null}}\) &
\(2\,\mathrm{Nm\,s/rad}\) &
Commands \(-2N_\tau\dot q\). For an ideal orthogonal truncated-SVD
projector,
\(\|\tau_d\|_2\leq2\|\dot q\|_2\,\mathrm{Nm}\); thus each
\(1\,\mathrm{rad/s}\) of projected joint-speed norm produces at most
\(2\,\mathrm{Nm}\) damping-torque norm. With the \(1\,\mathrm{Nm}\) sigma
term, the ideal scalar torque balance is \(0.5\,\mathrm{rad/s}\); this is a
command scale, not a predicted steady speed. It dissipates redundant motion
but does not return the joints to a preferred posture. \\
Sigma torque \(k_\sigma\) &
\(1\,\mathrm{Nm}\) &
Multiplies a unit \(v_7\) direction. Ideally
\(\|\tau_\sigma\|_2\leq1\,\mathrm{Nm}\), with equality when the structural
null direction is retained exactly. This bounds the active bias independently
of the probe distance. \\
Probe distance \(\alpha_{\mathrm{probe}}\) &
\(0.08\,\mathrm{rad}\) &
Evaluates \(q\pm0.08v_7\): each sample is \(4.58^\circ\) away in Euclidean
joint-space angle, and the two samples are \(0.16\,\mathrm{rad}\)
(\(9.17^\circ\)) apart. This is a finite directional comparison, not a
gradient magnitude and not an exact finite motion on the self-motion
manifold. \\
Sign deadband \(\delta_\sigma\) &
\(10^{-6}\) &
Suppresses the sigma torque when
\(\lvert\sigma_{\min,+}-\sigma_{\min,-}\rvert\leq10^{-6}\). Because the
geometric Jacobian is unscaled and mixes translational and rotational rows,
this is a numerical deadband in the implementation's singular-value scale,
not a unit-invariant physical threshold. With
\(\alpha_{\mathrm{probe}}=0.08\,\mathrm{rad}\), it corresponds to a
central-difference slope threshold of
\(6.25\times10^{-6}\,\mathrm{rad}^{-1}\) in that same scale. \\
Relative SVD cutoff \(\rho\) &
\(10^{-4}\) &
Only \(\sigma_i>\rho\sigma_1\) is reciprocated. Hence the retained-subspace
singular-value ratio is below \(10^4\), and every retained inverse is smaller
than \(10^4/\sigma_1\). Directions below the threshold enter the numerical
null space instead of being amplified. \\
\end{longtable}
\endgroup

The selected \(\alpha_{\mathrm{probe}}\), \(\delta_\sigma\), and \(\rho\)
trade local validity against numerical resolution; the present records do not
demonstrate that they are optimal. The two probe configurations are not checked
against joint limits, and the constant-magnitude sigma torque does not use the
size of the singular-value improvement. A future sweep should therefore vary
one of these parameters at a time, record the retained rank and projector
residual, and reject settings that increase task leakage, chatter, joint-limit
risk, or callback deadline misses.

\section{Experimental Procedure}
\label{sec:experimental_procedure_contact}

For the tagged controller version, the experiment is executed as follows:

\begin{enumerate}
    \item Record the controller tag and all five parameter files. Move the Panda
    to the configured initial joint pose, or use the optional damped manual
    guidance and recapture the selected start pose.
    \item Execute the configured gripper action. In the current configuration
    this is a grasp at \(70\,\mathrm{mm}\) width and \(60\,\mathrm{N}\), not an
    open command.
    \item Construct the known plane and
    \(R_{\mathrm{surface}}=[t_1,t_2,n_s]\), construct the \(40\times120\) mm tool
    face, and set the active base-XYZ pole offset. Configure the Franka collision
    behavior before starting torque control.
    \item In \texttt{approach\_orient}, hold the TCP position and rotate the
    physical \(+Z_{\mathrm{EE}}\) axis toward \(-n_s\). Continue only after at
    least \(0.5\,\mathrm{s}\) and a tool-axis error no greater than
    \(0.035\,\mathrm{rad}\).
    \item In \texttt{approach\_descend}, move along \(-n_s\) at
    \(0.1\,\mathrm{m/s}\), with a maximum commanded travel of
    \(0.640\,\mathrm{m}\). Select the leading face/edge/corner continuously.
    Transition when the exact most-leading corner height is at most
    \(20\,\mathrm{mm}\) above the plane; the averaged tied feature is used for
    the projected reference and can differ by at most the
    \(0.1\,\mathrm{mm}\) tie tolerance. Estimated force is logged and printed
    but does not trigger this transition. The enabled pre-set-up gate holds
    the reached pose until the operator presses Enter.
    \item At this clearance hand-off, freeze the selected tool feature, TCP pose,
    orientation reference, external-wrench baseline, and projected
    leading-feature surface reference. The source variable names contain
    \texttt{first\_contact}, but no force contact has yet been declared.
    \item In \texttt{set\_up}, ramp the signed push coordinate continuously from
    its captured value, approximately \(-0.020\,\mathrm{m}\), toward
    \(+0.180\,\mathrm{m}\) at
    \(0.050\,\mathrm{m/s}\) along \(-n_s\). Apply the current coupled pole-based
    \(6\times6\) stiffness and damping. The approximately \(0.200\,\mathrm{m}\)
    coordinate span takes \(4\,\mathrm{s}\), matching the timeout; the final
    \(+0.180\,\mathrm{m}\) coordinate is a virtual post-plane spring reference,
    not a claim of \(180\,\mathrm{mm}\) physical penetration. After
    \(2\,\mathrm{s}\), a \(150\,\mathrm{Nm}\) change in the model-estimated
    external-moment norm may end the phase. Operationally, this is a permissive
    placeholder rather than a calibrated contact criterion; unless such a
    large change occurs, the
    independent \(4\,\mathrm{s}\) timeout governs the transition.
    \item In \texttt{grind}, retain the final set-up preload and command a
    quintic smooth ping-pong sweep of \(0.03\,\mathrm{m}\) amplitude at
    \(0.2\,\mathrm{Hz}\) along \(t_2\). In the callback that satisfies the
    set-up exit condition, the controller switches immediately, without gain
    interpolation, to the decoupled set-up gain blocks; the pole-coupled branch
    is no longer active. A wrench and \(J^\top w_{\mathrm{cmd}}\) step is
    therefore possible at this hand-off.
    \item Stop with \texttt{e+Enter}, persist the buffered log, and evaluate the
    physical tool-axis error, edge/TCP displacement, commanded joint-torque
    trace, and phase timing.
\end{enumerate}

For a controlled comparison, only the declared independent variable---for
example block-diagonal versus pole-coupled impedance or the base-XYZ pole
offset---may change between repetitions. Pool only runs with the same controller
tag, tool geometry, phase duration, and fixed gains; otherwise report them as
separate configurations. The comparison is not meant to
prove a universal optimal stiffness matrix; it identifies which tested
centre-of-compliance setting gives the most useful behavior for this tool,
plane, and contact sequence.

\section{Evaluation Metrics}
\label{sec:alignment_evaluation}

The physical alignment metric must be distinguished from the controller's
rotation error. Let
\(a_T=R_{\mathrm{EE}}a_{\mathrm{tool,EE}}\) be the current grinding-face axis and
let \(a_d=-n_s\) be its configured target. The tool-axis alignment angle and its
small-angle corrective vector are

\begin{equation}
\theta_{\mathrm{align}}
=
\cos^{-1}\!\left(\operatorname{clip}(a_T^\top a_d,-1,1)\right),
\qquad
\mathbf{e}_{\mathrm{align}}=a_T\times a_d .
\label{eq:contact_alignment_error_cross}
\end{equation}

The minus sign in \(a_d=-n_s\) is essential for the current tool convention.
The approach phase already reduces this angle below \(0.035\,\mathrm{rad}\);
set-up evaluates whether contact reduces the remaining error further. The main
before--after condition is therefore

\begin{equation}
\theta_{\mathrm{align,after}}
<
\theta_{\mathrm{align,before}}.
\label{eq:alignment_error_reduction}
\end{equation}

Equivalently, the improvement can be written as

\begin{equation}
\Delta\theta_{\mathrm{align}}
=
\theta_{\mathrm{align,before}}
-
\theta_{\mathrm{align,after}}.
\label{eq:alignment_error_improvement}
\end{equation}

If \(\Delta\theta_{\mathrm{align}}>0\), the physical alignment improved during
contact. The rotation during contact itself is described by the base-frame rotation
vector of the finite before--after relative rotation,

\begin{equation}
\Delta\boldsymbol{\theta}_C
=
\operatorname{Log}\!\left(
R_{\mathrm{after}}R_{\mathrm{before}}^\top
\right)^\vee .
\label{eq:contact_induced_rotation}
\end{equation}

The corrective vector
\(\mathbf{e}_{\mathrm{align}}=a_T\times(-n_s)\) points locally along the
rotation that carries the physical tool axis toward its configured target. The
rotation during contact is favorable when it has a positive component along
this corrective axis,

\begin{equation}
\mathbf{e}_{\mathrm{align,before}}^\top
\Delta\boldsymbol{\theta}_C
>
0,
\label{eq:contact_rotation_condition}
\end{equation}

which is a local sign check rather than a global large-rotation test.

The corresponding displacement of the selected physical feature is

\begin{equation}
\Delta p_C
=
p_{C,\mathrm{after}}-p_{C,\mathrm{before}},
\qquad
s_C=\lVert\Delta p_C\rVert .
\label{eq:contact_feature_displacement}
\end{equation}

The TCP displacement is reported in the same way. These values expose
translation and sliding that an orientation-only metric would miss.

During set-up, the controller instead logs
\(\mathbf{e}_R
=R_{\mathrm{EE}}\operatorname{Log}
(R_{\mathrm{EE}}^\top R_{\mathrm{clr}})^\vee\), expressed in the base frame
after masking, where \(R_{\mathrm{clr}}\) is the orientation frozen at the
clearance hand-off. This control error begins near zero and can
\emph{increase} as the tool tips during contact. It must not be plotted as
\(\theta_{\mathrm{align}}\) without reconstructing the physical tool axis.
The current general CSV does not contain \(R_{\mathrm{EE}}\) explicitly, so a
claim of surface-alignment reduction requires either a reconstructable
orientation reference or an additional logged tool-axis metric.

\subsection{Evaluation Criteria and Acceptance Conditions}
\label{sec:evaluation_criteria_contact}

The experiment is evaluated using the following criteria:

\begin{itemize}
    \item \textbf{Alignment direction:} the measured rotation should reduce the
    angle between the physical grinding-face axis and \(-n_s\).
    \item \textbf{Pole setting:} record the commanded center-of-compliance
    offset and compare physical alignment plus selected-feature/TCP
    displacement across pole settings.
    \item \textbf{Contact displacement:} the translational displacement should
    remain small and bounded.
    \item \textbf{Torque behavior:} the commanded torques should remain smooth,
    bounded, and below the selected limits.
    \item \textbf{Damping behavior:} after contact or release, the response
    should not show sustained oscillations.
\end{itemize}

The central result is not tracking accuracy alone, but whether the selected
Cartesian stiffness matrix supports useful
unscripted task-space alignment during contact.

\section{Safety Limits}
\label{sec:damping_safety_limits_contact}

The phase-specific damping is computed from the task inertia using
\cref{eq:implemented_cartesian_inertia,eq:directional_damping_heuristic}. This
directional calculation does not itself establish safe contact; the real
controller remains discrete, model dependent, and coupled to a stiff
environment \citep{Pollayil2023ImpedancePlanning}.

Before the torque callback, the current configuration calls
\texttt{setCollisionBehavior} with equal joint-side acceleration and nominal
thresholds of \(140\,\mathrm{Nm}\), and equal Cartesian force/moment thresholds
of \(240\,\mathrm{N}\) or \(\mathrm{Nm}\) for all six components. These are
Franka reflex thresholds, not commanded-torque or wrench saturations. The user
callback implements no separate Cartesian-force clipping, moment clipping,
joint-torque clipping, or torque-rate limiter. The values therefore document
the tested robot configuration; they are not a general human-safety claim.

Additional procedural and geometric guards are:

\begin{itemize}
    \item a \(0.640\,\mathrm{m}\) maximum approach travel and a geometric
    \(20\,\mathrm{mm}\) clearance hand-off;
    \item an operator gate before set-up, using a
    \(5000\,\mathrm{N/m}\) isotropic position hold with automatically fitted
    damping;
    \item a \(4\,\mathrm{s}\) set-up timeout independent of the permissive
    \(150\,\mathrm{Nm}\) external-moment-change stopping threshold; the timeout
    is expected to govern unless the estimated moment changes exceptionally;
    \item the configured set-up stiffness and push must be checked against the
    collision settings before a run; the scalar product
    \(350\,\mathrm{N/m}\times0.180\,\mathrm{m}\approx63\,\mathrm{N}\) is only a
    base-\(z\) approximation, while the frame-consistent blocked-reference
    scales are \(65.3\,\mathrm{N}\) normal and \(70.2\,\mathrm{N}\) in total
    from \cref{eq:active_setup_translation_surface_representation}; none is a
    contact-force measurement; and
    \item after a normally completed sequence run, the general CSV is inspected
    for phase transitions, commanded torque, displacement, and oscillation.
\end{itemize}

Franka reflex/contact flags are not columns of the 112-column general sequence
log. They are available only in the compact sigma diagnostic during eligible
hold tests. Moreover, a sequence-ending reflex exception does not guarantee
that the general CSV is written; only the compact diagnostic buffer is
explicitly persisted on that exception path. Hardware/controller event records
must therefore supplement the CSV when diagnosing an aborted sequence.

These checks are necessary because a stiffness matrix that is geometrically
meaningful can still be unsuitable for real hardware if it produces aggressive
torques or poorly damped contact behavior. In particular, the
\(150\,\mathrm{Nm}\) set-up threshold is applied to the moment change in
\texttt{O\_F\_ext\_hat\_K}. This is libfranka's model-based estimate associated
with stiffness frame \(K\), not a separately measured TCP contact moment. Its
large value avoids treating ordinary model fluctuations as a contact event and
makes the timeout the practical default transition. It is an engineering
placeholder, not a measured safety boundary or a validated contact threshold.
Its stopping role must be established from the phase logs and retuned against
the observed moment range before it is used as an event detector.
Energy-aware impedance control studies make the same point from a safety
perspective: contact tasks should limit not only position error, but also the
energy and power that the controller can inject during unexpected interaction
\citep{Hjorth2023EnergyAwareImpedance}.
