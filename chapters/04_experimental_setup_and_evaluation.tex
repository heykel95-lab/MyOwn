\chapter{Experimental Methodology}
\label{ch:methodology}
\ReviewMark{purple}{The chapter was revised in full and every paragraph is
settled. No further editing is to be applied to it, and any remaining concern
is raised as a comment rather than as a change.}

\Comment{In the settled prose, use ``commanded tool orientation offset'' for
the deliberate angular command and ``commanded rotation axis'' or ``commanded
rotation direction'' for its components. Retain ``tilt'' only for a physical
inclination or a calibrated surface angle.}

\Revised{This chapter presents the experimental configuration, calibration procedure,
controller parameters, test sequence, and evaluation quantities used to
characterise the contact-induced alignment of the grinding tool. The main test
series consists of surface-contact measurements in which the commanded tilt,
directional Cartesian stiffness, and virtual centre of compliance \mbox{\(p_c\)} were varied
under otherwise matched conditions. Complementary Cartesian pose-hold trials
quantified the two null-space torque \mbox{\(\tau_{\mathrm{null}}\)} contributions under a repeatable
internally commanded disturbance.}


\section{Experimental Design}
\label{sec:experimental_design}
\label{sec:experimental_question}

\Revised{The cases are experimental stages within the three research
questions. Cases~A and C answer the stiffness question;
Case~C tests whether the geometric selection rule extends to combined
tangent-plane directions; and Cases~D to~H establish the compliance centre from
a reference condition carrying no lever at all, separating the frame the lever
is defined in from its direction and its position.
\mbox{\Cref{tab:experimental_question_map}} gives
this mapping before the complete parameter matrix.}

\begin{table}[H]
\centering
\small
\caption{Research-question structure of the surface-contact investigation.}
\label{tab:experimental_question_map}
\begin{tabular}{@{}p{0.08\textwidth}p{0.48\textwidth}p{0.30\textwidth}@{}}
\toprule
RQ & Experimental purpose & Principal evidence \\
\midrule
RQ1 & Effect of directional rotational and translational stiffness on
alignment & Stiffness variations in B and C \\
RQ2 & Effect of compliance-centre lever direction, sign, and magnitude on
alignment & Reference without a lever in J; tool-fixed tangential lever in K;
in-plane and tool-axis position in M and N \\
RQ3 & Derivation and test of an orientation-dependent geometric selection rule &
Four-direction generality and fixed-lever comparison in H; definition frame of
the lever in L \\
\bottomrule
\end{tabular}
\end{table}

\Revised{Within that structure, the surface-contact measurements were divided
into cases. Case~A varied the rotational stiffness about the tilted
tangent, while Case~B varied the translational stiffness perpendicular to that
tangent. Case~C commanded the tilt in four directions in the tangent plane,
compared a direction-selected lever with a fixed one, and varied the normal
lever coordinate.}

\Revised{Cases~D to~H examine the compliance centre with its offset held fixed
to the tool. Case~D places the centre on the TCP, so the press makes no moment
through an offset and what acts is the contact alone. It is measured on both
surface tangents and at both signs of the commanded offset, because the
grinding face is three times longer across one tangent than the other and the
two need not behave alike.}

\Revised{Case~E displaces the centre along the tangent perpendicular to the
commanded rotation axis, seven positions from \mbox{\(-40\)} to
\mbox{\(+40\,\mathrm{mm}\)}, at every combination of tangent and sign. That
grid is what settles whether the assisting direction follows the sign of the
commanded offset. Case~F names one of those offsets in surface coordinates
instead of end-effector coordinates, so the definition frame is separated from
the offset itself.}

\Revised{Cases~G and~H vary the remaining two quantities. Case~G moves the
centre along the tool axis, the direction a first-order coupling model leaves
without effect. Case~H halves the commanded offset at two of the Case-E
positions, so what the centre does is told apart from what the size of the
commanded offset does.}

\Revised{The commanded angular offset defines the orientation difference present at the
beginning of set-up. The rotational stiffness determines the resistance to
rotation about the tilted tangent, whereas the translational stiffness
perpendicular to that tangent affects the translational response coupled to the
alignment motion. Shifting the Cartesian impedance from a selected
centre of compliance \mbox{\(p_c\)} to the TCP introduces translation--rotation coupling and
therefore changes the moment produced by the commanded press.}

\Revised{The surface plane and the grinding-face direction were calibrated
independently and used to define the surface and tool geometry throughout the
measurements. The rotational stiffness about \mbox{\(t_1\)} and \mbox{\(t_2\)} was varied
separately, while the orthogonal rotational-stiffness entry remained at
\mbox{\(5\,\mathrm{N\,m/rad}\)}. This arrangement allowed the response associated with each
surface tangent \mbox{\(t_1\)} or \mbox{\(t_2\)} to be evaluated independently.}

\Revised{Unless stated otherwise for a particular case, the surface-frame
translational stiffness \mbox{\(K_p\)} used during set-up and grinding was}

\begin{equation}
K_p
=
\begin{bmatrix}
2000 & 0 & 0\\
0 & 2000 & 0\\
0 & 0 & 350
\end{bmatrix}
\,\mathrm{N/m}.
\label{eq:experimental_baseline_translation}
\end{equation}

\Revised{The corresponding surface-frame rotational stiffness \mbox{\(K_R\)}
was}

\begin{equation}
K_R
=
\begin{bmatrix}
5 & 0 & 0\\
0 & 5 & 0\\
0 & 0 & 50
\end{bmatrix}
\,\mathrm{N\,m/rad}.
\label{eq:experimental_baseline_rotation}
\end{equation}

\Revised{also in the surface frame. Both matrices use the coordinate order
\mbox{\([t_1,t_2,n_s]\)}. The fixed phase, damping, null-space, and trajectory
parameters are specified in \mbox{\Cref{sec:experimental_controller_parameters}}.}

\Revised{Neither matrix is a neutral starting point. The entries were chosen
for the roles they play during set-up, and the choice is stated here because it
bounds what varying them could show. The controller regulates pose and not
force: the commanded wrench is \mbox{\(K\,\Delta x+D\,\Delta\dot x\)}
throughout, with no force target and no force feedback anywhere in the loop.
Whatever load the tool carries is a consequence of a commanded displacement
acting through a stiffness.}

\Revised{The normal translational entry is the compliant one,
\mbox{\(350\,\mathrm{N/m}\)} against \mbox{\(2000\,\mathrm{N/m}\)} in the
plane. The press reference is ramped past the surface and the surface stops the
tool, so the load that arises is that entry times the penetration the contact
refuses. A stiff normal entry would turn a small geometric error into a large
load, which is what makes the compliant choice the workable one. The stiff
in-plane entries hold the contact point where the geometry places it instead of
letting it slide.}

\Revised{The rotational entries are chosen the other way round. Those about the
two surface tangents are nearly free at \mbox{\(5\,\mathrm{N\,m/rad}\)},
because turning onto the surface is the motion the set-up phase exists to
permit and a stiff entry there would hold the tool at the attitude it arrived
with. The entry about the surface normal is stiff at
\mbox{\(50\,\mathrm{N\,m/rad}\)}, because rotation about the tool axis
contributes nothing to alignment.}

\Revised{Two consequences follow for the experiments. The stiffness variations
of Cases~A and~B ask how much the chosen values matter rather than search for
better ones, and their outcome supports the choice: raising the rotational
entry about the commanded tangent reduced the alignment response. The entry
about the surface normal was not varied, because the alignment error carries no
component about that axis by construction and the measured rotation about it
stayed below \mbox{\(0.71^\circ\)} in every reported run, leaving that entry
little to act on.}

\Revised{The normal translational entry was not varied either. Raising it would
raise the press and, through the compliance-centre offset, the moment the press
makes. Whether that improves the alignment is not established here, and the
reported measurements give reason for doubt: across Cases~D to~H the press lay
between \mbox{\(76\)} and \mbox{\(85\,\mathrm{N}\)} while the alignment change
spanned \mbox{\(-4.60\)} to \mbox{\(+7.65^\circ\)}, and two conditions pressing
alike at approximately \mbox{\(80\,\mathrm{N}\)} differ by
\mbox{\(6.3^\circ\)} in what they achieve. The load is therefore not what
separates them over this range, and a larger one cannot be assumed to help.}

\needspace{0.55\textheight}%
\begingroup
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{longtable}{@{}
>{\centering\arraybackslash}p{0.075\textwidth}
>{\raggedright\arraybackslash}p{0.145\textwidth}
>{\raggedright\arraybackslash}p{0.12\textwidth}
>{\raggedright\arraybackslash}p{0.295\textwidth}
>{\centering\arraybackslash}p{0.035\textwidth}
>{\raggedright\arraybackslash}p{0.17\textwidth}@{}}
\caption{Parameter variations used in the surface-contact measurements.}
\label{tab:experimental_test_cases}\\
\toprule
\textbf{Case}
&
\textbf{Varied quantity}
&
{\scriptsize\textbf{Commanded tool orientation offset}}
&
\textbf{Tested values}
&
\(\boldsymbol{n}\)
&
\textbf{Comparison}
\\
\midrule
\endfirsthead
\caption[]{Parameter variations used in the surface-contact measurements
(continued).}\\
\toprule
\textbf{Case}
&
\textbf{Varied quantity}
&
{\scriptsize\textbf{Commanded tool orientation offset}}
&
\textbf{Tested values}
&
\(\boldsymbol{n}\)
&
\textbf{Comparison}
\\
\midrule
\endhead
\midrule
\endfoot
\bottomrule
\endlastfoot

A
&
Rotational stiffness
&
\(+10^\circ\) about \(t_1\) or \(t_2\)
&
\(15\) and \(50\,\mathrm{N\,m/rad}\) about the commanded rotation axis. The
orthogonal tangent remained at \(5\,\mathrm{N\,m/rad}\). The matched
\(5\,\mathrm{N\,m/rad}\), \(2000\,\mathrm{N/m}\) reference setting is counted
here and shared with Case~B.
&
18
&
Tangent-specific influence of rotational stiffness on the alignment response.
\\
\addlinespace

B
&
Translational stiffness
&
\(+10^\circ\) about \(t_1\) or \(t_2\)
&
\(300\) and \(800\,\mathrm{N/m}\) perpendicular to the commanded rotation axis. The
matched \(2000\,\mathrm{N/m}\) reference is the setting counted in Case~A. One
additional condition for each commanded rotation direction combined
\(300\,\mathrm{N/m}\) with \(50\,\mathrm{N\,m/rad}\).
&
18
&
Influence of the cross-axis translational stiffness and its interaction with
rotational stiffness.
\\
\addlinespace

C
&
Commanded rotation direction and normal lever coordinate
&
\(+10^\circ\) in four tangent-plane directions
&
Lever of \(60\,\mathrm{mm}\) selected for each commanded rotation direction, the same lever
held fixed while the direction turned, and normal coordinates of \(-60\),
\(+20\), \(+60\) and \(+120\,\mathrm{mm}\).
&
33
&
Whether one lever serves every commanded rotation direction, and the effect of
the normal coordinate.
\\
\addlinespace

D
&
Contact alone; the centre of compliance was placed at the TCP
&
None, and \(\pm10^\circ\) about \(t_1\) or \(t_2\)
&
No offset, and one pair carrying \(+40\,\mathrm{mm}\) at the zero offset.
&
18
&
Response of the contact alone on each tangent, and the residual the
arrangement leaves with nothing commanded, which is the floor every other
residual is read against.
\\
\addlinespace

E
&
Direction and sign of the compliance-centre offset
&
\(\pm10^\circ\) about \(t_1\) or \(t_2\)
&
\(-40\), \(-20\), \(-10\), \(0\), \(+10\), \(+20\) and \(+40\,\mathrm{mm}\)
along the tangent perpendicular to the commanded rotation axis, positive in
the direction that assists a positive offset.
&
72
&
Whether the assisting direction follows the sign of the commanded offset, and
what the offset adds on each tangent.
\\
\addlinespace

F
&
Frame in which the offset is defined
&
None and \(+10^\circ\) about \(t_1\)
&
The \(40\,\mathrm{mm}\) offset of Case~E named in surface coordinates.
&
6
&
Whether the definition frame changes the response once the tool turns.
\\
\addlinespace

G
&
Compliance-centre position along the tool axis
&
\(+10^\circ\) about \(t_1\)
&
\(-40\), \(-20\), \(-10\), \(+10\), \(+20\) and \(+40\,\mathrm{mm}\) along the
tool axis. The \(0\,\mathrm{mm}\) entry is the corresponding trial of Case~E.
&
18
&
Dependence on a direction that produces no first-order moment.
\\
\addlinespace

H
&
Commanded magnitude
&
\(+5^\circ\) about \(t_1\) or \(t_2\)
&
\(0\) and \(+40\,\mathrm{mm}\) along the assisting tangent, read against the
same positions at \(+10^\circ\) in Case~E.
&
12
&
Whether the correction scales with the commanded offset.
\\

\end{longtable}
\endgroup

\vspace{0.6em}
\begin{minipage}{0.98\textwidth}
Each parameter setting was repeated three times. Cases~A and C used the
decoupled Cartesian impedance. Case~C used the point-shifted
impedance with the listed levers expressed in the surface frame according to
\(r_c=p_{\mathrm{TCP}}-p_c\). The stated gains, calibrated geometry, phase
sequence, press motion, and null-space settings were held constant except where a
varied value is stated explicitly.
\end{minipage}
\vspace{1.2em}

\Revised{The eight cases contain \mbox{\(171\)} surface-contact runs
distributed across \mbox{\(57\)} parameter settings. Within each controlled comparison, only
the stated parameter was changed. The remaining gain entries, calibrated surface
and tool geometry, phase sequence, and evaluation procedure were held constant.}
\Revised{The principal evaluation quantity was the change in the angle between the
calibrated surface normal \mbox{\(n_s\)} and the grinding-face direction calculated from the
measured end-effector orientation. The rotation during set-up, the displacement
of the point or edge of the grinding face used for the set-up motion, the TCP
displacement \mbox{\(\Delta p_{\mathrm{TCP}}\)}, and the model-estimated external
load \mbox{\(F_n\)} were evaluated alongside
this angle. Their definitions and treatment across repeated runs are presented
in \mbox{\Cref{sec:experimental_evaluation}}.}

\Revised{The surface-contact measurements and Cartesian pose-hold trials formed
complementary data sets. Contact trials measured alignment with projected
damping as the active null-space term. The pose-hold trials in
\mbox{\Cref{sec:nullspace_hold_study}} quantified projected damping and
singular-value conditioning under a repeatable internally commanded disturbance.}

\section{Experimental System}
\label{sec:experimental_system}


\subsection{Robot, Tool, and Surface}
\label{subsec:experimental_robot_tool_surface}

\Revised{The experiments were performed with a seven-degree-of-freedom Franka Emika
Panda operated through the Franka Control Interface. Joint-torque commands were
transmitted through libfranka at the nominal sampling period \mbox{\(T_s\)}:}

\begin{equation}
T_s=1\,\mathrm{ms},
\label{eq:sampling_time_contact}
\end{equation}

\Revised{corresponding to a nominal control rate of \mbox{\(1\,\mathrm{kHz}\)}. The real-time
controller and its phase sequence are described in
\mbox{\Cref{ch:implementation}}.}

\Revised{The rectangular grinding tool was held by the Franka Hand using custom gripper
fingers. Its grinding face measured \mbox{\(40\,\mathrm{mm}\)} along
\mbox{\(X_{\mathrm{EE}}\)} and \mbox{\(120\,\mathrm{mm}\)} along \mbox{\(Y_{\mathrm{EE}}\)}. The
centre of the grinding face was located \mbox{\(20\,\mathrm{mm}\)} along
\mbox{\(+Z_{\mathrm{EE}}\)} from the reported end-effector origin. The complete
calibrated tool geometry is presented in
\mbox{\Cref{sec:surface_tool_calibration}}.}

\Revised{The pads holding the tool were screwed to the gripper fingers. Clearance in
that screwed joint permitted a small relative rotation of the tool about
\mbox{\(y_{\mathrm{EE}}\)}.}

\Revised{The grinding-face direction was reconstructed from the measured end-effector
orientation and its calibrated direction in the end-effector frame. This
frame-consistent metric quantifies the combined robot--gripper--tool response.
The approximately \mbox{\(\pm2^\circ\)} relative tool rotation about
\mbox{\(y_{\mathrm{EE}}\)} defines the mount-related contribution to its uncertainty.}

\Revised{The surface plate was mounted on a raised base board. All experiments used dry
contact between the grinding face and the plate. The controller command followed
the calibrated geometry and Cartesian impedance law. The surface plane and
grinding-face direction were calibrated separately before the experimental
campaign, as described in \mbox{\Cref{sec:surface_tool_calibration}}.}

\Revised{During approach, the controller determines which point or edge of the
rectangular grinding face lies furthest in the descent direction. This point
or edge is used for the geometric clearance calculation and the subsequent
set-up motion. Its position was calculated from the measured end-effector pose
and the calibrated tool geometry.}

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{
        \input{figures/setup_schematic.tex}
    }
    \caption{Experimental system and surface-frame contact geometry.}
    \label{fig:experimental_setup_photo}
\end{figure}


\subsection{Software and Real-Time Computer}
\label{subsec:experimental_software_computer}

\Revised{The controller computer ran Ubuntu~20.04.3 with the
\mbox{\texttt{5.9.1-rt20}} Linux kernel and the \mbox{\texttt{PREEMPT\_RT}} patch. The
controller was compiled using \mbox{\texttt{g++~9.3}}, the C++14 language standard,
and the \mbox{\texttt{-O2}} optimisation level. Eigen~3.3.7 and libfranka~0.7.1 were
used for the matrix calculations and communication with the robot.}

\Revised{Before the contact measurements, the real-time computer and robot network
were checked with the \mbox{\texttt{communication\_test}} program supplied with
libfranka~0.7.1 \mbox{\citep{libfrankaCommunicationTest}}. The program reports the
fraction of control commands received by the robot and issues its lower
communication-quality warning below an average success rate of \mbox{\(0.90\)}.
The experimental setup passed this criterion.}

\Revised{The controller operated through the libfranka joint-torque interface. At each
control cycle, the measured robot state and the quantities provided by the
robot model were used to calculate the commanded joint torque
\mbox{\(\tau_{\mathrm{cmd}}\)}. The
implementation of the real-time callback is documented in
\mbox{\Cref{sec:real_time_control_loop}}.}

\Revised{Each contact run followed the implemented sequence of tool orientation,
surface approach, set-up, and grinding. Manual guidance supported preparation,
while Cartesian pose hold provided the complementary null-space trials.
Projected damping supplied the null-space contribution throughout the contact
sequence.}


\subsection{Recorded Experimental Quantities}
\label{subsec:experimental_recorded_quantities}

\Revised{The quantities required for the subsequent evaluation were written to the
controller's in-memory logging buffer during each run and exported to a CSV
file after torque control had ended. The recorded quantities included:}

\begin{itemize}
    \item \Revised{the measured end-effector position and orientation;}
    \item \Revised{the measured joint position \mbox{\(q\)} and velocity
          \mbox{\(\dot q\)};}
    \item \Revised{the commanded Cartesian reference and Cartesian error \mbox{\(e\)};}
    \item \Revised{the position of the point or edge of the grinding face used for the
          set-up motion;}
    \item \Revised{the active controller phase and null-space mode;}
    \item \Revised{the commanded Cartesian wrench \mbox{\(F\)} and final joint-torque
          command \mbox{\(\tau_{\mathrm{cmd}}\)}; and}
    \item \Revised{the model-estimated external force \mbox{\(\hat f_{\mathrm{ext}}\)} and
          moment reported by libfranka.}
\end{itemize}

\Revised{The commanded wrench \mbox{\(F\)} and the model-estimated external wrench were recorded as
separate quantities. After the clearance-transition reference was subtracted,
the libfranka estimate provided a common supporting load signal across matched runs. The complete
logging implementation and CSV structure are described in
\mbox{\Cref{sec:recorded_signals}}.}


\subsection{Reproducibility Record}
\label{subsec:reproducibility_record}

\Revised{The documented experimental system is summarised in
\mbox{Table~\ref{tab:reproducibility_record}}. Controller parameters that differ between
the individual experimental cases are reported separately in
\mbox{\Cref{sec:experimental_controller_parameters}}.}

{\small
\begin{longtable}{@{}
>{\raggedright\arraybackslash}p{0.29\textwidth}
>{\raggedright\arraybackslash}p{0.64\textwidth}@{}}
\caption{Documented configuration of the experimental system.}
\label{tab:reproducibility_record}\\
\toprule
\textbf{Item}
&
\textbf{Configuration used in the reported experiments}
\\
\midrule
\endfirsthead

\toprule
\textbf{Item}
&
\textbf{Configuration used in the reported experiments}
\\
\midrule
\endhead

\midrule
\endfoot

\bottomrule
\endlastfoot

Robot
&
Franka Emika Panda with seven actuated joints.
\\

Robot interface
&
libfranka joint-torque interface with a nominal control period of
\(1\,\mathrm{ms}\).
\\

Gripper and tool mounting
&
Franka Hand with custom gripper fingers and an approximately
\(\pm2^\circ\) relative rotational play about \(y_{\mathrm{EE}}\).
\\

Grinding tool
&
Rectangular grinding face measuring \(40\times120\,\mathrm{mm}\), with its
centre located \(20\,\mathrm{mm}\) along \(+Z_{\mathrm{EE}}\) from the
reported end-effector origin.
\\

Surface
&
Plane surface mounted on a raised base board and used under dry-contact
conditions.
\\

Operating system
&
Ubuntu~20.04.3.
\\

Real-time kernel
&
Linux \texttt{5.9.1-rt20} with \texttt{PREEMPT\_RT}.
\\

Compiler
&
\texttt{g++~9.3}, C++14, optimisation level \texttt{-O2}.
\\

Software libraries
&
Eigen~3.3.7 and libfranka~0.7.1.
\\

Recorded state quantities
&
Robot pose, joint position and velocity, Cartesian reference and error,
controller phase, and position of the grinding-face point or edge used for the
set-up motion.
\\

Recorded command and load quantities
&
Commanded Cartesian wrench, commanded joint torque, and the model-estimated
external force and moment reported by libfranka.
\\

Contact condition
&
Dry contact; controller command determined by the calibrated geometry and
Cartesian impedance law.
\\

\end{longtable}
\Revised{}}

\Revised{The reproducibility record contains the software versions, nominal control
period, controller parameters, tool dimensions, and surface calibration. These
quantities define the implemented control configuration and calibrated contact
geometry used in the experiments.}

\section{Surface and Tool Calibration}
\label{sec:surface_tool_calibration}
\label{sec:contact_geometry}

\Revised{The surface plane and the direction normal to the grinding face were
calibrated independently before the contact experiments. The surface
calibration provided the point, normal direction, and tangent directions used
by the controller. The tool calibration provided the grinding-face direction
expressed in the end-effector frame.}

\Revised{The calibrated quantities were written to the controller parameter files and
were subsequently used for approach, clearance calculation, commanded tool
orientation, gain orientation, compliance-centre placement, and evaluation of
the experimental results.}

\Revised{The two calibration chains were independent during estimation and were
combined only when the run-time tool direction was evaluated against the
surface normal \mbox{\(n_s\)}. Their fit and validation stages are summarised in
\mbox{\cref{fig:calibration_flow}}.}

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{\input{figures/calibration_flow.tex}}
  \caption{Surface and tool calibration flow.}
  \label{fig:calibration_flow}
\end{figure}


\subsection{Surface-Plane Calibration}
\label{subsec:plane_calibration_protocol}

\Revised{Four positions distributed over the plate were probed with the same
marked \mbox{\(+X_{\mathrm{EE}},+Y_{\mathrm{EE}}\)} corner of the grinding face.
The first three corner positions, \mbox{\(P_1\)}--\mbox{\(P_3\)}, determined the
plane. The fourth, \mbox{\(P_4\)}, was excluded from the fit and provided an
independent geometric check.}

\Revised{At each position \mbox{\(i\)}, the read-only capture utility obtained the
measured end-effector position \mbox{\(p_{\mathrm{EE},i}\)} and orientation
\mbox{\(R_{\mathrm{EE},i}\)}. The fixed end-effector-frame probe offset
\mbox{\(r_{\mathrm{probe,EE}}\)} was the grinding-face centre plus the positive
half-width and half-length vectors:}

\begin{equation}
r_{\mathrm{probe,EE}}
=
\begin{bmatrix}
0.020\\
0.060\\
0.020
\end{bmatrix}
\mathrm{m}.
\label{eq:plane_probe_corner_offset}
\end{equation}

\Revised{The base-frame corner coordinate \mbox{\(p_i\)} stored for the plane fit was
therefore}

\begin{equation}
p_i
=
p_{\mathrm{EE},i}
+R_{\mathrm{EE},i}r_{\mathrm{probe,EE}}.
\label{eq:plane_probe_corner_position}
\end{equation}

\Revised{The plane used by the controller is represented by}

\begin{equation}
n_s^\top(p-p_s)=0,
\label{eq:calibrated_surface_plane}
\end{equation}

\Revised{where \mbox{\(p_s\)} is one point on the plane and \mbox{\(n_s\)} is its unit normal,
expressed in the robot base frame.}

\Revised{The final calibrated plane point stored in
the controller parameters was}

\begin{equation}
p_s
=
\begin{bmatrix}
0.5153\\
-0.1072\\
0.0031
\end{bmatrix}
\mathrm{m}.
\label{eq:calibrated_surface_point}
\end{equation}

\Revised{This point is the centroid of the three fitted corner coordinates, not a
captured grinding-face-centre position.}

\Revised{The controller stores the surface orientation through rotations \mbox{\(a\)} about
the base \mbox{\(x\)}-axis and \mbox{\(b\)} about the base \mbox{\(y\)}-axis. With
\mbox{\(e_z\)} denoting the base-frame \mbox{\(z\)}-axis unit vector, the corresponding
normal is calculated from}

\begin{equation}
n_s
=
R_y(b)R_x(a)e_z
=
\begin{bmatrix}
\sin(b)\cos(a)\\
-\sin(a)\\
\cos(b)\cos(a)
\end{bmatrix}.
\label{eq:implemented_surface_normal_angles}
\end{equation}

\Revised{The calibrated angles in the final controller configuration were}

\begin{equation}
a=-1.585^\circ,
\qquad
b=+0.988^\circ.
\label{eq:calibrated_surface_tilt_angles}
\end{equation}

\Revised{These values give the base-frame surface normal \mbox{\(n_s\)}}

\begin{equation}
n_s
\approx
\begin{bmatrix}
0.017245\\
0.027663\\
0.999469
\end{bmatrix},
\qquad
\|n_s\|=1.
\label{eq:surface_normal}
\end{equation}

\Revised{This three-point physical-plane fit was copied into the controller
configuration. The same normal \mbox{\(n_s\)} therefore defined the surface
frame, the controller geometry, and the alignment values reported in
\mbox{\Cref{ch:results_discussion}}. No separate evaluation normal was used.}

\Revised{The plane-validation residual \mbox{\(\varepsilon_{\mathrm{plane}}\)} is the
perpendicular distance of the independently probed fourth point \mbox{\(p_4\)}
from the calibrated plane:}

\begin{equation}
\varepsilon_{\mathrm{plane}}
=
\left|
n_s^\top(p_4-p_s)
\right|.
\label{eq:surface_plane_validation_residual}
\end{equation}

\Revised{The resulting validation residual was}

\begin{equation}
\varepsilon_{\mathrm{plane}}
=
0.82\,\mathrm{mm}.
\label{eq:surface_plane_validation_value}
\end{equation}

\Revised{The first surface tangent \mbox{\(t_1\)} is taken from the calibration itself. The direction
\mbox{\(a_s\)} from the first to the second probed point is projected into the
calibrated plane and normalised:}

\begin{equation}
t_1
=
\frac{
    (I-n_sn_s^\top)a_s
}{
    \left\|
        (I-n_sn_s^\top)a_s
    \right\|
}.
\label{eq:calibrated_first_surface_tangent}
\end{equation}

\Revised{The tangent therefore follows the probed geometry rather than a base-frame
axis, and the surface frame is rotated in the plane relative to the base
\mbox{\(x\)}- and \mbox{\(y\)}-directions.}

\Revised{The second surface tangent \mbox{\(t_2\)} completes the right-handed surface frame:}

\begin{equation}
t_2=n_s\times t_1.
\label{eq:calibrated_second_surface_tangent}
\end{equation}

\Revised{For the calibrated normal, the resulting tangent directions are approximately}

\begin{equation}
t_1
\approx
\begin{bmatrix}
-0.905786\\
-0.422854\\
 0.027332
\end{bmatrix},
\qquad
t_2
\approx
\begin{bmatrix}
 0.423385\\
-0.905776\\
 0.017765
\end{bmatrix}.
\label{eq:calibrated_surface_tangent_values}
\end{equation}

\Revised{The complete surface frame is therefore}

\begin{equation}
R_{\mathrm{surface}}
=
\begin{bmatrix}
t_1 & t_2 & n_s
\end{bmatrix}
\approx
\begin{bmatrix}
-0.905786 & 0.423385 & 0.017245\\
-0.422854 &-0.905776 & 0.027663\\
 0.027332 & 0.017765 & 0.999469
\end{bmatrix}.
\label{eq:surface_task_frame}
\end{equation}

\Revised{The surface normal \mbox{\(n_s\)} lies approximately \mbox{\(1.9^\circ\)} from the base
\mbox{\(z\)}-direction, while the tangents are rotated substantially within the plane
relative to the base \mbox{\(x\)}- and \mbox{\(y\)}-directions.}

\Revised{All surface-frame quantities in the contact campaign use the coordinate order}

\begin{equation}
[\,t_1,t_2,n_s\,].
\end{equation}

\Revised{This order applies to commanded angular offsets, rotational stiffness,
surface-resolved displacement and rotation, and the compliance-centre levers
\mbox{\(r_c\)}.}


\subsection{Grinding-Face Calibration}
\label{subsec:grinding_face_calibration}

\Revised{Four end-effector orientations, \mbox{\(T_1\)}--\mbox{\(T_4\)}, were captured
with the complete grinding face seated flat on the validated plane. The face
remained flat while its yaw about the plane normal was changed by approximately
\mbox{\(20^\circ\)}--\mbox{\(45^\circ\)} between samples. The first three rotations
were used for estimation, and the fourth was held out for validation.}

\Revised{With only the yaw changed, the end-effector-frame grinding-face normal
\mbox{\(n_{\mathrm{EE}}\)} should map to one invariant base-frame direction
\mbox{\(\bar n_B\)}. The matrices \mbox{\(R_i\)} are the rotational blocks of the
captured poses \mbox{\(T_i\)}:}

\begin{equation}
R_1n_{\mathrm{EE}}
\approx
R_2n_{\mathrm{EE}}
\approx
R_3n_{\mathrm{EE}}
=
\bar n_B.
\label{eq:tool_axis_invariant_direction}
\end{equation}

\Revised{The estimator formed the mean rotation matrix
\mbox{\(\bar R=(R_1+R_2+R_3)/3\)} and applied the
singular-value decomposition \mbox{\(\bar R=U\Sigma V^\top\)}. The matrix
\mbox{\(U\)} contains the left singular vectors. The matrix \mbox{\(\Sigma\)}
contains the singular values, and \mbox{\(V\)} contains the right singular
vectors. The dominant
right singular vector gave \mbox{\(n_{\mathrm{EE}}\)}, while the corresponding
left singular vector gave \mbox{\(\bar n_B\)}. The sign was selected toward
nominal \mbox{\(+Z_{\mathrm{EE}}\)}. The final controller configuration was}

\begin{equation}
n_{\mathrm{EE}}
=
\begin{bmatrix}
-0.006011\\
0.000246\\
0.999982
\end{bmatrix},
\qquad
\|n_{\mathrm{EE}}\|=1,
\label{eq:calibrated_grinding_face_direction}
\end{equation}

\Revised{expressed in the end-effector frame. The calibrated grinding-face normal
differs from nominal \mbox{\(+Z_{\mathrm{EE}}\)} by \mbox{\(0.34^\circ\)}. The
calibration does not by itself identify the physical source of this offset.}

\Revised{The three fit residuals were \mbox{\(0.19^\circ\)}--\mbox{\(0.41^\circ\)}.
For the held-out orientation, the invariant-axis residual
\mbox{\(\varepsilon_{\mathrm{tool}}\)} was}

\begin{equation}
\varepsilon_{\mathrm{tool}}
=
\angle\!\left(R_4n_{\mathrm{EE}},\bar n_B\right)
=
0.50^\circ.
\label{eq:grinding_face_calibration_residual}
\end{equation}

\Revised{The fit and held-out limits were both \mbox{\(0.50^\circ\)}. The
dominant-to-second singular-value gap was \mbox{\(0.210\)}, above the required
minimum of \mbox{\(0.005\)}, which confirmed that the yaw samples made the axis
observable. The fitted invariant base direction differed from the signed
plane-normal target by \mbox{\(1.36^\circ\)}. This separate check satisfied its
\mbox{\(2.0^\circ\)} limit; the plane normal did not participate in the
tool-axis estimate.}

\Revised{During each controller cycle, the grinding-face direction in the robot base
frame is calculated from the measured end-effector orientation:}

\begin{equation}
n_T
=
R_{\mathrm{EE}}n_{\mathrm{EE}}.
\label{eq:grinding_face_direction_base}
\end{equation}

\Revised{The direction \mbox{\(n_T\)} is therefore calculated from the measured end-effector
orientation and the calibrated grinding-face direction. This reconstruction
provides the grinding-face direction used throughout the evaluation.}

\Revised{The commanded flat orientation turns the grinding face toward the calibrated
surface according to}

\begin{equation}
R_d n_{\mathrm{EE}}
=
-n_s.
\label{eq:implemented_tool_axis_target}
\end{equation}

\Revised{The negative sign follows from the axis-direction sign \mbox{\(s_a\)}:}

\begin{equation}
s_a=-1,
\end{equation}

\Revised{which directs the positive grinding-face axis opposite the outward surface
normal \mbox{\(n_s\)}. The remaining spin about the desired grinding-face axis,
measured from \mbox{\(t_1\)}, was set to \mbox{\(115.05^\circ\)} and was held constant
for all reported runs. For the flat orientation, the grinding-face axis is
antiparallel to \mbox{\(n_s\)}. This spin places the long axis
\mbox{\(y_{\mathrm{EE}}\)} at \mbox{\(25.05^\circ\)} from the calibrated surface
tangent \mbox{\(t_2\)}. In the following sections this angle is written as
approximately \mbox{\(25^\circ\)}.}

\Revised{The parallel-jaw mounting permitted approximately
\mbox{\(\pm2^\circ\)} of relative tool rotation about \mbox{\(y_{\mathrm{EE}}\)}.
This relative motion contributes to the uncertainty of the alignment
calculated from the end-effector orientation.}

\Revised{\mbox{\Cref{fig:tool_face_plan_view}} shows the calibrated face and the
mounting axes in the surface plane.}

\begin{figure}[H]
  \centering
  \input{figures/tool_face_plan_view.tex}
  \caption{Calibrated grinding face and mounting axes in the surface plane.}
  \label{fig:tool_face_plan_view}
\end{figure}


\subsection{Calibrated Grinding-Face Geometry}
\label{subsec:calibrated_contact_geometry}

\Revised{The grinding face was represented by a rectangle expressed in the
end-effector frame. Its centre offset \mbox{\(r_{\mathrm{face,EE}}\)} was configured as}

\begin{equation}
r_{\mathrm{face,EE}}
=
\begin{bmatrix}
0\\
0\\
0.020
\end{bmatrix}
\mathrm{m}.
\label{eq:grinding_face_center_ee}
\end{equation}

\Revised{The half-width vector \mbox{\(r_{\mathrm{width,EE}}\)} and half-length vector
\mbox{\(r_{\mathrm{length,EE}}\)} were}

\begin{equation}
r_{\mathrm{width,EE}}
=
\begin{bmatrix}
0.020\\
0\\
0
\end{bmatrix}
\mathrm{m},
\qquad
r_{\mathrm{length,EE}}
=
\begin{bmatrix}
0\\
0.060\\
0
\end{bmatrix}
\mathrm{m}.
\label{eq:grinding_face_half_dimensions}
\end{equation}

\Revised{The complete grinding face therefore measured}

\begin{equation}
40\,\mathrm{mm}
\times
120\,\mathrm{mm},
\end{equation}

\Revised{with the width along \mbox{\(X_{\mathrm{EE}}\)}, the length along
\mbox{\(Y_{\mathrm{EE}}\)}, and its centre \mbox{\(20\,\mathrm{mm}\)} along
\mbox{\(+Z_{\mathrm{EE}}\)} from the reported end-effector origin.}

\Revised{For a point on the grinding face with end-effector-frame offset
\mbox{\(r_{g,\mathrm{EE}}\)}, its base-frame position \mbox{\(p_g\)} is calculated from}

\begin{equation}
p_g
=
p_{\mathrm{EE}}
+
R_{\mathrm{EE}}r_{g,\mathrm{EE}}.
\label{eq:controlled_grinding_face_position}
\end{equation}

\Revised{The controller transforms all four corners of the grinding face into the robot
base frame and compares their positions along the descent direction. The point
or edge of the grinding face that approaches the surface first is then used
for the geometric clearance calculation and the subsequent set-up motion.}

\Revised{The grinding-face contact-point selection tolerance
\mbox{\(\varepsilon_g\)} was}

\begin{equation}
\varepsilon_g
=
0.1\,\mathrm{mm}
\label{eq:grinding_face_selection_tolerance}
\end{equation}

\Revised{are treated as belonging to the same edge or face. Their positions are
averaged to form one geometric reference. This prevents a nominal edge
configuration from switching between its two endpoints because of small
numerical differences.}

\Revised{In the following sections, this geometry-based reference is called the point
or edge of the grinding face used for the set-up motion. Its position is used
for the clearance calculation, commanded set-up displacement, and reported
grinding-face displacement.}

\Revised{The point or edge is obtained entirely from the measured end-effector pose,
the calibrated grinding-face geometry, and the descent direction. The
controller therefore uses a geometric surface reference rather than a
separately measured physical contact position. The implementation of the
corner transformation and geometric selection is described in
\mbox{\Cref{sec:surface_task_plane}}.}

\section{Controller Parameters Used in the Experiments}
\label{sec:experimental_controller_parameters}
\label{sec:cartesian_stiffness_selection}

\Revised{The controller parameters were assigned according to the role of each
phase. Approach used a comparatively soft isotropic Cartesian impedance in the
surface frame. The operator gate applied a stiffer translational hold at the
clearance pose. Set-up and grinding used lower translational stiffness along
\mbox{\(n_s\)} than along \mbox{\(t_1\)} and \mbox{\(t_2\)}, together with lower
rotational stiffness about the surface tangents than about the surface normal
\mbox{\(n_s\)}.
The coordinate frame therefore forms part of every gain definition.}


\subsection{Fixed Phase and Motion Parameters}
\label{subsec:fixed_phase_motion_parameters}

\Revised{The phase-transition and trajectory parameters are listed in
\mbox{\Cref{tab:experimental_phase_parameters}}. The set-up push coordinate
starts at
the signed distance captured at the clearance transition, approximately
\mbox{\(-0.020\,\mathrm{m}\)}, and moves toward \mbox{\(+0.060\,\mathrm{m}\)}. At the
configured speed, the reference reaches this endpoint after approximately
\mbox{\(1.6\,\mathrm{s}\)} and is then held there until the phase ends. The endpoint
is a Cartesian spring reference beyond the calibrated plane and is not
interpreted as physical penetration of the same magnitude.}

\begin{table}[H]
\centering
\small
\caption{Fixed phase-transition and trajectory parameters.}
\label{tab:experimental_phase_parameters}
\begin{tabular}{@{}p{0.43\textwidth}p{0.20\textwidth}p{0.27\textwidth}@{}}
\toprule
\textbf{Quantity} & \textbf{Value} & \textbf{Role} \\
\midrule
Minimum orientation time & \(0.5\,\mathrm{s}\) & Prevents an immediate phase transition. \\
Tool-axis transition tolerance & \(0.035\,\mathrm{rad}\) & Maximum remaining tool-axis error before descent. \\
Descent speed & \(0.1\,\mathrm{m/s}\) & Reference speed along the descent direction. \\
Maximum descent distance & \(0.640\,\mathrm{m}\) & Terminates an unsuccessful approach. \\
Surface clearance & \(0.020\,\mathrm{m}\) & Geometric hand-off from approach to set-up. \\
Minimum set-up time & \(2.0\,\mathrm{s}\) & Earliest activation of the moment-based exit condition. \\
Set-up timeout & \(5.0\,\mathrm{s}\) & Time-based set-up termination. \\
Moment-change threshold & \(150\,\mathrm{N\,m}\) & Alternative set-up termination based on the model-estimated external moment. \\
Set-up push speed & \(0.080\,\mathrm{m/s}\) & Rate of the signed press coordinate. \\
Set-up push endpoint & \(+0.240\,\mathrm{m}\) & Final signed Cartesian spring reference. \\
Normal translational stiffness & \(350\,\mathrm{N/m}\) & Surface-frame entry along \(n_s\). \\
Grinding reference & fixed tangential coordinates & Set-up press reference maintained. \\
Pre-set-up gate & enabled & Holds the clearance pose until operator confirmation. \\
Pre-grind gate & enabled & Holds the set-up pose until operator confirmation. \\
\bottomrule
\end{tabular}
\end{table}


\subsection{Phase-Dependent Cartesian Stiffness}
\label{subsec:initial_stiffness_damping_values}

\Revised{During tool orientation and surface approach, the translational stiffness
\mbox{\(K_{p,\mathrm{app}}\)} and rotational stiffness
\mbox{\(K_{R,\mathrm{app}}\)} were isotropic in the calibrated surface frame:}

\begin{equation}
K_{p,\mathrm{app}}
=
\begin{bmatrix}
150 & 0 & 0\\
0 & 150 & 0\\
0 & 0 & 150
\end{bmatrix}
\,\mathrm{N/m},
\qquad
K_{R,\mathrm{app}}
=
\begin{bmatrix}
90 & 0 & 0\\
0 & 90 & 0\\
0 & 0 & 90
\end{bmatrix}
\,\mathrm{N\,m/rad}.
\label{eq:experimental_approach_stiffness}
\end{equation}

\Revised{At the pre-set-up gate, the base-frame translational stiffness
\mbox{\(K_{p,\mathrm{gate}}\)} was increased. The gate rotational stiffness
\mbox{\(K_{R,\mathrm{gate}}\)} retained the approach value:}

\begin{equation}
K_{p,\mathrm{gate}}
=
\begin{bmatrix}
5000 & 0 & 0\\
0 & 5000 & 0\\
0 & 0 & 5000
\end{bmatrix}
\,\mathrm{N/m},
\qquad
K_{R,\mathrm{gate}}
=
\begin{bmatrix}
90 & 0 & 0\\
0 & 90 & 0\\
0 & 0 & 90
\end{bmatrix}
\,\mathrm{N\,m/rad}.
\label{eq:experimental_gate_stiffness}
\end{equation}

\Revised{The surface-frame translational stiffness matrices used during set-up and
grinding, \mbox{\(K_{p,\mathrm{set}}\)} and \mbox{\(K_{p,\mathrm{grind}}\)}, were equal:}

\begin{equation}
K_{p,\mathrm{set}}
=
K_{p,\mathrm{grind}}
=
\begin{bmatrix}
2000 & 0 & 0\\
0 & 2000 & 0\\
0 & 0 & 350
\end{bmatrix}
\,\mathrm{N/m}.
\label{eq:experimental_setup_translational_stiffness}
\end{equation}

\Revised{The corresponding rotational stiffness matrices
\mbox{\(K_{R,\mathrm{set}}\)} and \mbox{\(K_{R,\mathrm{grind}}\)} were also equal:}

\begin{equation}
K_{R,\mathrm{set}}
=
K_{R,\mathrm{grind}}
=
\begin{bmatrix}
5 & 0 & 0\\
0 & 5 & 0\\
0 & 0 & 50
\end{bmatrix}
\,\mathrm{N\,m/rad}.
\label{eq:experimental_setup_rotational_stiffness}
\end{equation}

\Revised{The lower rotational tangent entries allow rotation about \mbox{\(t_1\)} and \mbox{\(t_2\)},
while the larger normal entry resists twist about \mbox{\(n_s\)}. The effective
normal translational stiffness \mbox{\(k_{p,n}\)} is the normal entry because
both matrices are diagonal in the surface frame:}

\begin{equation}
k_{p,n}
=
n_s^\top K_{p,\mathrm{set}}n_s
=
350\,\mathrm{N/m},
\label{eq:experimental_effective_normal_stiffness}
\end{equation}

\Revised{and the tangential entries act along \mbox{\(t_1\)} and \mbox{\(t_2\)} without
contributing to the normal direction.}

\Revised{The Cartesian pose-hold study used the isotropic base-frame translational
stiffness \mbox{\(K_{p,\mathrm{hold}}\)} and rotational stiffness
\mbox{\(K_{R,\mathrm{hold}}\)}:}

\begin{equation}
K_{p,\mathrm{hold}}
=
\begin{bmatrix}
2000 & 0 & 0\\
0 & 2000 & 0\\
0 & 0 & 2000
\end{bmatrix}
\,\mathrm{N/m},
\qquad
K_{R,\mathrm{hold}}
=
\begin{bmatrix}
50 & 0 & 0\\
0 & 50 & 0\\
0 & 0 & 50
\end{bmatrix}
\,\mathrm{N\,m/rad}.
\label{eq:experimental_hold_stiffness}
\end{equation}

\Revised{Manual guidance used joint damping of
\mbox{\(0.5\,\mathrm{N\,m\,s/rad}\)} and no Cartesian stiffness.}


\subsection{Inertia-Scaled Damping}
\label{subsec:experimental_inertia_scaled_damping}

\Revised{The directional damping coefficients were calculated at entry to the
corresponding phase group from the current Cartesian inertia, the active
stiffness, and the configured damping ratio, as described in
\mbox{\Cref{subsec:inertia_scaled_damping_calculation}}. Approach used
\mbox{\(\zeta=1.9\)}, while set-up and grinding used \mbox{\(\zeta=1.0\)}. The calculated
coefficients were used for the associated phase group.}

\Revised{The pre-set-up gate used separate damping ratios for translation and rotation.
They were derived from the configured translational damping target
\mbox{\(D_{p,\mathrm{gate}}^{\mathrm{ref}}\)}:}

\begin{equation}
D_{p,\mathrm{gate}}^{\mathrm{ref}}
=
\begin{bmatrix}
200 & 0 & 0\\
0 & 200 & 0\\
0 & 0 & 200
\end{bmatrix}
\,\mathrm{N\,s/m}.
\end{equation}

\Revised{The rotational gate ratio was derived from the damping calculated for
approach. For the pose-hold study, one damping ratio was derived from the
translational damping target \mbox{\(D_{p,\mathrm{hold}}^{\mathrm{ref}}\)}:}

\begin{equation}
D_{p,\mathrm{hold}}^{\mathrm{ref}}
=
\begin{bmatrix}
50 & 0 & 0\\
0 & 50 & 0\\
0 & 0 & 50
\end{bmatrix}
\,\mathrm{N\,s/m}
\end{equation}

\Revised{and applied to the translational and rotational inertia-scaled damping
calculations.}

\Revised{When an inertia estimate could not be used, the configured approach
fallbacks were the translational damping \mbox{\(D_{p,\mathrm{app}}^{\mathrm{ref}}\)}
and rotational damping \mbox{\(D_{R,\mathrm{app}}^{\mathrm{ref}}\)}:}

\begin{equation}
D_{p,\mathrm{app}}^{\mathrm{ref}}
=
\begin{bmatrix}
20 & 0 & 0\\
0 & 20 & 0\\
0 & 0 & 20
\end{bmatrix}
\,\mathrm{N\,s/m},
\qquad
D_{R,\mathrm{app}}^{\mathrm{ref}}
=
\begin{bmatrix}
12 & 0 & 0\\
0 & 12 & 0\\
0 & 0 & 12
\end{bmatrix}
\,\mathrm{N\,m\,s/rad}.
\label{eq:experimental_approach_damping_reference}
\end{equation}

\Revised{The corresponding set-up fallbacks were the translational damping
\mbox{\(D_{p,\mathrm{set}}^{\mathrm{ref}}\)} and rotational damping
\mbox{\(D_{R,\mathrm{set}}^{\mathrm{ref}}\)}:}

\begin{equation}
D_{p,\mathrm{set}}^{\mathrm{ref}}
=
\begin{bmatrix}
50 & 0 & 0\\
0 & 50 & 0\\
0 & 0 & 25
\end{bmatrix}
\,\mathrm{N\,s/m},
\qquad
D_{R,\mathrm{set}}^{\mathrm{ref}}
=
\begin{bmatrix}
10.01 & 0 & 0\\
0 & 10.01 & 0\\
0 & 0 & 10
\end{bmatrix}
\,\mathrm{N\,m\,s/rad}.
\label{eq:experimental_setup_damping_reference}
\end{equation}

\Revised{Both set-up matrices are expressed in the surface frame with the coordinate
order \mbox{\([t_1,t_2,n_s]\)}.}

\Revised{The factor \mbox{\(\zeta=1\)} defines the coefficient for each decoupled
direction in the calculation. The controller uses the resulting matrix as its
inertia-scaled damping setting after the compliance-centre shift.}


\subsection{Compliance-Centre Settings}
\label{subsec:impedance_around_pole}

\Revised{Cases~A and C used the decoupled Cartesian impedance at the TCP.
Case~C used the point-shifted impedance during set-up. The centre of compliance
\mbox{\(p_c\)} was specified through the lever \mbox{\(r_c\)}}

\begin{equation}
r_c
=
p_{\mathrm{TCP}}-p_c,
\label{eq:experimental_compliance_lever}
\end{equation}

\Revised{resolved in the surface frame \mbox{\([t_1,t_2,n_s]\)}. The lever was captured with
the clearance pose and remained constant during the set-up phase. The
stiffness and damping matrices were shifted to the TCP according to the
transformation derived in \mbox{\Cref{sec:coupled_pole_stiffness}}.}

\Revised{Cases~E to~H also used the point-shifted impedance during
set-up.}

\Revised{For a commanded tilt about \mbox{\(t_1\)}, the varied component was
\mbox{\(r_{c,t_2}\)}, while \mbox{\(r_{c,t_1}=0\)}. For a commanded tilt about \mbox{\(t_2\)},
the varied component was \mbox{\(r_{c,t_1}\)}, while \mbox{\(r_{c,t_2}=0\)}. The tested
perpendicular components were \mbox{\(-60\)}, \mbox{\(0\)}, and \mbox{\(+60\,\mathrm{mm}\)}, with
\mbox{\(r_{c,n}=0\)}.}

\Revised{The fourth setting placed the centre of compliance \mbox{\(p_c\)} at the centre of the
grinding face. The surface-frame levers \mbox{\(r_c^{(t_1)}\)} and
\mbox{\(r_c^{(t_2)}\)} correspond to the measurements about \mbox{\(t_1\)} and
\mbox{\(t_2\)}, respectively:}

\begin{equation}
r_c^{(t_1)}
=
\begin{bmatrix}
0\\
-3.5\\
19.7
\end{bmatrix}
\mathrm{mm},
\qquad
r_c^{(t_2)}
=
\begin{bmatrix}
3.5\\
0\\
19.7
\end{bmatrix}
\mathrm{mm}.
\label{eq:experimental_face_center_levers}
\end{equation}

\Revised{The sign of the perpendicular component determines the direction of the
moment generated by the translational response. Reversing this component
reverses the corresponding coupling moment about the tilted surface tangent
\mbox{\(t_1\)} or \mbox{\(t_2\)}.
The reported selections are the largest measured responses among the discrete
lever values tested.}

\Revised{For the directional measurements the commanded rotation axis lies in
the surface tangent plane and is separate from the offset angle. Written from the
surface-resolved angular components \mbox{\((\theta_{t_1},\theta_{t_2})\)}, the
selected tangential lever is}

\begin{equation}
r_{c,t}
=
\frac{\rho_c}{\sqrt{\theta_{t_1}^{2}+\theta_{t_2}^{2}}}
\left(\theta_{t_2}t_1-\theta_{t_1}t_2\right).
\label{eq:experimental_general_lever_rule}
\end{equation}

\Revised{Here \mbox{\(\rho_c>0\)} is the selected tangential lever magnitude. The
lever is perpendicular to the commanded rotation axis within the tangent plane,
which is what the phrase \emph{aligned lever} means in Case~C, and it reverses
when the commanded orientation offset does. The four commanded directions are
listed as surface-frame vectors in
\mbox{\cref{tab:experimental_case_c_vectors}}.}

\Revised{Case~C contains three internal comparisons. H1 rotates the
\mbox{\(60\,\mathrm{mm}\)} tangential lever with each of the four commanded rotation
directions. H2 holds the H1 \mbox{\(y_{\mathrm{EE}}\)} lever fixed while the other
three directions are commanded. H3 uses the H1
\mbox{\(y_{\mathrm{EE}}\)} tangential components and changes only the normal
coordinate.}

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\caption{Case-C commanded tool-orientation-offset components and compliance-centre lever vectors
in the surface frame.}
\label{tab:experimental_case_c_vectors}
\begin{tabular}{@{}llll@{}}
\toprule
Condition & Commanded rotation-axis description &
\([\theta_{t_1},\theta_{t_2}]\) (deg) &
\([r_{c,t_1},r_{c,t_2},r_{c,n}]\) (mm) \\
\midrule
H1 aligned & About \(y_{\mathrm{EE}}\) & \([-4.23,+9.06]\) & \([+54.4,+25.4,0]\) \\
H1 aligned & \(-45^\circ\) diagonal & \([+3.41,+9.40]\) & \([+56.4,-20.5,0]\) \\
H1 aligned & About \(x_{\mathrm{EE}}\) & \([+9.06,+4.23]\) & \([+25.4,-54.4,0]\) \\
H1 aligned & \(+45^\circ\) diagonal & \([-9.40,+3.41]\) & \([+20.5,+56.4,0]\) \\
\addlinespace
H2 fixed & \(-45^\circ\) diagonal & \([+3.41,+9.40]\) & \([+54.4,+25.4,0]\) \\
H2 fixed & About \(x_{\mathrm{EE}}\) & \([+9.06,+4.23]\) & \([+54.4,+25.4,0]\) \\
H2 fixed & \(+45^\circ\) diagonal & \([-9.40,+3.41]\) & \([+54.4,+25.4,0]\) \\
\addlinespace
H3 normal & About \(y_{\mathrm{EE}}\) & \([-4.23,+9.06]\) & \([+54.4,+25.4,-60]\) \\
H3 normal & About \(y_{\mathrm{EE}}\) & \([-4.23,+9.06]\) & \([+54.4,+25.4,+20]\) \\
H3 normal & About \(y_{\mathrm{EE}}\) & \([-4.23,+9.06]\) & \([+54.4,+25.4,+60]\) \\
H3 normal & About \(y_{\mathrm{EE}}\) & \([-4.23,+9.06]\) & \([+54.4,+25.4,+120]\) \\
\bottomrule
\end{tabular}
\end{table}

\Revised{\mbox{\Cref{fig:case_c_direction_rule}} shows the four commanded rotation
directions with the lever selected for each.}

\begin{figure}[H]
  \centering
  \input{figures/case_c_direction_rule.tex}
  \caption{Selected tangential lever for each commanded rotation direction.}
  \label{fig:case_c_direction_rule}
\end{figure}

\Revised{The H1 condition about \mbox{\(y_{\mathrm{EE}}\)} with
\mbox{\(r_{c,n}=0\)} also served as the zero-normal-offset reference for H3.
Together, the four H1, three H2, and four additional H3 settings account for
the eleven Case-C settings in \mbox{\cref{tab:experimental_test_cases}}.}


\subsection{Settings for Cases~D to~H}
\label{subsec:experimental_case_jn_settings}

\Revised{Cases~D to~H used their own phase configuration, listed in
\mbox{\Cref{tab:experimental_case_dh_parameters}}. The press ran faster and
deeper than in the earlier cases, and the pre-set-up gate was disabled so that
approach handed over to set-up without an intermediate hold. The gate-hold
impedance was therefore never active in these runs.}

\begin{table}[H]
\centering
\small
\caption{Phase parameters for Cases~D to~H.}
\label{tab:experimental_case_dh_parameters}
\begin{tabular}{@{}p{0.42\textwidth}p{0.22\textwidth}p{0.28\textwidth}@{}}
\toprule
Parameter & Value & Role \\
\midrule
Set-up push speed & \(0.080\,\mathrm{m/s}\) & Rate of the signed press coordinate. \\
Set-up push endpoint & \(+0.240\,\mathrm{m}\) & Final signed Cartesian spring reference. \\
Set-up timeout & \(5.0\,\mathrm{s}\) & Time-based set-up termination. \\
Minimum set-up time & \(2.0\,\mathrm{s}\) & Earliest activation of the moment-based exit. \\
Moment-change threshold & \(150\,\mathrm{N\,m}\) & Alternative set-up termination. \\
Pre-set-up gate & disabled & Approach hands over without stopping. \\
Pre-grind gate & enabled & Ends the trial once the set-up result has printed. \\
Translational stiffness frame & surface & \(K_p\) diagonal in \([t_1,t_2,n_s]\). \\
\bottomrule
\end{tabular}
\end{table}

\Revised{The reference reaches the endpoint after approximately
\mbox{\(3.3\,\mathrm{s}\)} and is held there for the remainder of the phase.
The measured press settled near \mbox{\(81\,\mathrm{N}\)} and lay between
\mbox{\(76\)} and \mbox{\(85\,\mathrm{N}\)} across the reported runs. The
spread is not an independent variable: the press is generated by a spring
against a commanded penetration, so a condition in which the tool rotates far
presents a different contact geometry and settles a few newtons lower. The
alignment comparisons therefore rest on a common commanded press rather than
on an identical measured one.}

\Revised{The alignment criterion is evaluated while the phase runs but never
ends it. A deviation below \mbox{\(2^\circ\)} held for \mbox{\(0.3\,\mathrm{s}\)}
marks the run as aligned and records the time; the crossing of half the
deviation present at first contact is recorded separately, together with the
smallest deviation reached and when. The last two are defined for every run,
including those that never align, so a condition that fails is kept with its
own numbers rather than left empty.}

\Revised{The commanded offsets were \mbox{\(0^\circ\)}, \mbox{\(5^\circ\)} and
\mbox{\(10^\circ\)} about \mbox{\(t_1\)}, and \mbox{\(5^\circ\)} and
\mbox{\(10^\circ\)} about \mbox{\(t_2\)}. The tangential lever was
\mbox{\(60\,\mathrm{mm}\)} for offsets about \mbox{\(t_1\)} and
\mbox{\(30\,\mathrm{mm}\)} for offsets about \mbox{\(t_2\)}. Case~G sampled
the in-plane position at \mbox{\(-50\)}, \mbox{\(-20\)}, \mbox{\(0\)},
\mbox{\(+20\)} and \mbox{\(+50\,\mathrm{mm}\)}; Case~H sampled the tool-axis
position at \mbox{\(-90\)}, \mbox{\(-60\)}, \mbox{\(-40\)}, \mbox{\(0\)},
\mbox{\(+40\)}, \mbox{\(+60\)} and \mbox{\(+90\,\mathrm{mm}\)}. Every setting
was repeated three times.}


\subsection{Null-Space Settings for the Contact Measurements}
\label{subsec:nullspace_parameter_rationale}

\Revised{Projected damping formed the active null-space configuration throughout the
surface-contact measurements. This held the redundant-motion policy constant
across Cases~A to~H. The damping gain was}

\begin{equation}
d_{\mathrm{null}}
=
2\,\mathrm{N\,m\,s/rad}.
\label{eq:experimental_contact_nullspace_gains}
\end{equation}

\Revised{The projected damping gain was held fixed throughout Cases~A to~H. Its role
in the null-space torque \mbox{\(\tau_{\mathrm{null}}\)} is described in
\mbox{\Cref{sec:nullspace_svd}}. The
contact measurements establish alignment performance with this configuration.
Matched pose-hold trials quantify projected damping and singular-value
conditioning under a repeatable disturbance.}

\Revised{The recorded parameters confirmed one phase configuration throughout
all reported contact runs: surface-frame translation,
\mbox{\(K_n=350\,\mathrm{N/m}\)} toward \mbox{\(+0.240\,\mathrm{m}\)},
a \mbox{\(5.0\,\mathrm{s}\)} set-up timeout, a moment threshold the contact
never reached, a disabled pre-set-up gate, and null-space mode~1. The detailed
values remain collected in
\mbox{\cref{tab:experimental_phase_parameters,tab:active_phase_gains,tab:nullspace_safety_parameters}}.}


\section{Contact Experiment Procedure}
\label{sec:contact_experimental_procedure}
\label{sec:experimental_procedure_contact}

\Revised{Before each run, the active parameter set was recorded and the robot was moved
to the configured initial joint posture. Damped manual guidance could be used
to adjust this posture, after which the selected start pose was captured. The
tool was held by the Franka Hand using the configured grasp width of
\mbox{\(66\,\mathrm{mm}\)} and a grasping force of \mbox{\(60\,\mathrm{N}\)}. The seating
was repeated in the same manner for every measurement, while the approximately
\mbox{\(\pm2^\circ\)} rotational play about \mbox{\(y_{\mathrm{EE}}\)} remained part of
the physical mounting condition.}

\Revised{The calibrated surface frame and grinding-face geometry were constructed
before torque control began. The active Cartesian gains, damping settings,
null-space parameters, and compliance-centre definition were then loaded, and
the robot-side collision behaviour was configured.}

\Revised{During tool orientation, the TCP position was held while the calibrated
grinding-face direction was rotated toward \mbox{\(-n_s\)}, including the commanded
angular offsets of the active case. The controller remained in this phase for
at least \mbox{\(0.5\,\mathrm{s}\)} and continued to surface approach when the
remaining tool-axis error was at most \mbox{\(0.035\,\mathrm{rad}\)}.}

\Revised{Surface approach moved the Cartesian reference along the descent direction at
\mbox{\(0.1\,\mathrm{m/s}\)}, with a maximum commanded travel of
\mbox{\(0.640\,\mathrm{m}\)}. During this motion, the controller continuously
calculated the point or edge of the rectangular grinding face that approached
the surface first. The geometric transition was reached when the leading
corner height was at most \mbox{\(20\,\mathrm{mm}\)} above the calibrated plane.
Corner heights agreeing within \mbox{\(0.1\,\mathrm{mm}\)} were averaged according
to the geometric selection described in
\mbox{\Cref{subsec:calibrated_contact_geometry}}.}

\Revised{At the clearance transition, the operator gate held the reached pose until the
set-up motion was confirmed. The controller then stored the TCP pose, the
orientation reference, the point or edge of the grinding face used for the
set-up motion, its projection onto the plane, and the model-estimated external
force \mbox{\(\hat f_{\mathrm{ext}}\)} and moment stored as reference values. This transition was defined by the
calibrated geometry; the model-estimated force \mbox{\(\hat f_{\mathrm{ext}}\)} was recorded for evaluation but
did not determine the transition.}

\Revised{During set-up, the signed press coordinate moved from its captured
value, approximately \mbox{\(-0.020\,\mathrm{m}\)}, toward
\mbox{\(+0.240\,\mathrm{m}\)} at \mbox{\(0.080\,\mathrm{m/s}\)} along
the descent direction. Cases~A, B and~D
used the decoupled impedance, whereas Cases~C and~E to~H applied the
point-shifted
\mbox{\(6\times6\)} stiffness and damping matrices associated with the selected
centre of compliance \mbox{\(p_c\)}. The orientation stored at the clearance transition
remained the rotational reference, so the alignment motion arose from the
contact response rather than from a time-varying tipping command.}

\Revised{After the minimum set-up time of \mbox{\(2\,\mathrm{s}\)}, the phase could end when
the norm of the model-estimated external-moment change from the clearance transition reached
\mbox{\(150\,\mathrm{N\,m}\)}. The independent \mbox{\(5\,\mathrm{s}\)} timeout provided
the second termination condition. The final signed coordinate is a virtual
Cartesian spring reference and therefore describes the controller command
rather than a measured penetration depth.}

\Revised{Every reported experimental set-up phase ended at the
\mbox{\(5\,\mathrm{s}\)} timeout; the \mbox{\(150\,\mathrm{N\,m}\)} moment-change threshold
was not reached. In this campaign, the threshold therefore functioned as an
exceptional alternative bound rather than the routine set-up termination
criterion.}

\Revised{The pre-grind gate then held the reached pose until the transition to grinding
was confirmed. The controller then switched to the decoupled set-up gain
matrices and maintained the final press reference. During the reported
measurements, the tangential coordinates remained at their achieved set-up
values.}

\Revised{The operator ended the run through the terminal interface. The buffered data
were then written to the run-specific CSV file, and the stored parameter set
was included with the measurement. Between the three repetitions of one
setting, the calibrated geometry, fixed gain entries, phase timing, and
procedure remained unchanged. Only the parameter assigned to the corresponding
case was modified.}


\section{Evaluation Metrics and Repeated-Run Treatment}
\label{sec:experimental_evaluation}
\label{sec:alignment_evaluation}

\subsection{Alignment Change}
\label{subsec:experimental_alignment_change}

\Revised{The grinding-face direction used in the evaluation was calculated from the
measured end-effector orientation and the calibrated direction expressed in
the end-effector frame:}

\begin{equation}
n_T
=
R_{\mathrm{EE}}n_{\mathrm{EE}}.
\label{eq:evaluation_grinding_face_direction}
\end{equation}

\Revised{The alignment angle \mbox{\(\theta_{\mathrm{align}}\)} measures the angular
difference between this direction and the calibrated surface:}

\begin{equation}
\theta_{\mathrm{align}}
=
\cos^{-1}\!\left(-n_T^\top n_s\right).
\label{eq:contact_alignment_error_cross}
\end{equation}

\Revised{The sign convention follows the orientation target
\mbox{\(R_d n_{\mathrm{EE}}=-n_s\)}. The angle is calculated from the measured
end-effector orientation and the calibrated tool geometry. Relative rotation
of the tool in the gripper therefore contributes to its uncertainty.}

\Revised{The alignment improvement \mbox{\(\Delta\theta_{\mathrm{align}}\)} is the
initial alignment angle \mbox{\(\theta_{\mathrm{align}}\)} minus the final alignment
angle \mbox{\(\theta_{\mathrm{align}}\)}:}

\begin{equation}
\Delta\theta_{\mathrm{align}}
=
\theta_{\mathrm{align,before}}
-
\theta_{\mathrm{align,after}},
\label{eq:alignment_error_improvement}
\end{equation}

\Revised{where the initial value is taken at the clearance transition and the final
value at the end of set-up. Positive values represent a reduction of the
angular difference between the grinding face and the calibrated surface.}


\subsection{Rotation, Displacement, and Model-Estimated Load}
\label{subsec:experimental_supporting_metrics}

\Revised{The finite set-up rotation vector \mbox{\(\Delta\theta_{\mathrm{set}}\)} was
evaluated from}

\begin{equation}
\Delta\theta_{\mathrm{set}}
=
\phi_{\mathrm{set}}u_{\mathrm{set}}.
\label{eq:contact_induced_rotation}
\end{equation}

\Revised{Here \mbox{\(\phi_{\mathrm{set}}\)} and \mbox{\(u_{\mathrm{set}}\)} are the rotation angle
and the unit rotation axis of the relative rotation
\mbox{\(R_{\mathrm{after}}R_{\mathrm{before}}^\top\)}, obtained from its trace and its
skew-symmetric part exactly as in \mbox{\Cref{eq:angle_from_trace}} and
\mbox{\Cref{eq:rotation_axis_from_matrix}}. The vector is expressed in the robot base
frame. Its magnitude is the angle turned during set-up, in radians, and its
direction is the axis about which that rotation occurred.}

\Revised{The surface-resolved set-up rotation
\mbox{\(\Delta\theta_{\mathrm{set}}^{S}\)} was obtained from}

\begin{equation}
\Delta\theta_{\mathrm{set}}^{S}
=
R_{\mathrm{surface}}^\top
\Delta\theta_{\mathrm{set}}.
\label{eq:surface_resolved_setup_rotation}
\end{equation}

\Revised{This representation separates the rotations about \mbox{\(t_1\)}, \mbox{\(t_2\)}, and
\mbox{\(n_s\)} and permits direct comparison with the commanded tilt direction.}

\Revised{Let \mbox{\(p_g\)} denote the position of the point or edge of the grinding face used
for the set-up motion. Its displacement vector \mbox{\(\Delta p_g\)} and
displacement magnitude \mbox{\(s_g\)} were}

\begin{equation}
\Delta p_g
=
p_{g,\mathrm{after}}-p_{g,\mathrm{before}},
\qquad
s_g
=
\lVert\Delta p_g\rVert.
\label{eq:contact_feature_displacement}
\end{equation}

\Revised{The TCP displacement vector \mbox{\(\Delta p_{\mathrm{TCP}}\)} and its
magnitude \mbox{\(s_{\mathrm{TCP}}\)} were evaluated in the same form:}

\begin{equation}
\Delta p_{\mathrm{TCP}}
=
p_{\mathrm{TCP,after}}-p_{\mathrm{TCP,before}},
\qquad
s_{\mathrm{TCP}}
=
\lVert\Delta p_{\mathrm{TCP}}\rVert.
\label{eq:tcp_displacement_metric}
\end{equation}

\Revised{The surface-frame components of both displacement vectors were recorded to
show whether the angular change was accompanied by normal motion or tangential
sliding.}

\Revised{The model-estimated external force \mbox{\(\hat f_{\mathrm{ext}}\)} was reported
by libfranka. The clearance-transition reference \mbox{\(\hat f_{\mathrm{ref}}\)}
was subtracted from it. The normal-force magnitude \mbox{\(F_n\)} was}

\begin{equation}
F_n
=
\left|
n_s^\top
\left(
\hat f_{\mathrm{ext}}-\hat f_{\mathrm{ref}}
\right)
\right|.
\label{eq:model_estimated_normal_load}
\end{equation}

\Revised{This change in the robot-model force estimate provided the supporting load
quantity for matched-run comparisons. Its common calculation preserved a
consistent load basis across all contact settings.}

\Revised{The controller orientation error \mbox{\(e_R\)} is referenced to the orientation
stored at the clearance transition. It therefore describes deviation from the
held controller reference, while \mbox{\(\theta_{\mathrm{align}}\)} describes the
relationship between the grinding face and the calibrated surface. The latter
was used as the principal alignment quantity.}


\subsection{Treatment of Repeated Runs}
\label{subsec:experimental_repeated_run_treatment}
\label{sec:evaluation_criteria_contact}

\Revised{Each parameter setting was repeated three times. For a measured quantity
\mbox{\(y\)}, the repetition value \mbox{\(y_j\)} carries the repetition index
\mbox{\(j\)}. The arithmetic mean \mbox{\(\bar y\)} was}

\begin{equation}
\bar y
=
\frac{1}{3}
\sum_{j=1}^{3}y_j.
\label{eq:experimental_sample_mean}
\end{equation}

\Revised{The sample standard deviation \mbox{\(s_y\)} was}

\begin{equation}
s_y
=
\sqrt{
\frac{1}{2}
\sum_{j=1}^{3}
\left(y_j-\bar y\right)^2
}.
\label{eq:experimental_sample_standard_deviation}
\end{equation}

\Revised{Cases~A and C compare
the alignment change \mbox{\(\Delta\theta_{\mathrm{align}}\)} with the corresponding stiffness coordinate, while
using the rotation, displacement, and model-estimated load as supporting
quantities. Case~C compares direction-selected and fixed levers
across four tilt directions and evaluates the normal compliance-centre
coordinate.}

\Revised{Case~D provides the reference response to commanded tilt without a
lever. Cases~E and~F compare that response with a tangential lever named in the
end-effector frame and in the surface frame. Cases~G and~H evaluate the
alignment improvement \mbox{\(\Delta\theta_{\mathrm{align}}\)} and residual
angle as functions of the compliance-centre position within the contact plane
and along the tool axis.}

\Revised{The comparison was restricted to settings with matching calibrated geometry,
phase timing, fixed gains, and null-space parameters. Differences were
interpreted together with the within-setting variation and the zero-tilt
reference reported in \mbox{\Cref{sec:results_repeatability}}. The largest response
among the sampled levels is reported as the best tested magnitude.}


\section{Secondary Null-Space Pose-Hold Study}
\label{sec:nullspace_hold_study}
\label{subsec:nullspace_automatic_method}

\Revised{Matched Cartesian pose-hold trials quantified the two null-space torque
\mbox{\(\tau_{\mathrm{null}}\)} contributions. The pose-hold controller maintained the
initial TCP pose, while an internally commanded joint torque
\mbox{\(\tau_{\mathrm{dist}}\)} represented the
effect of a point force applied at a specified robot-link location. This
repeatable software command provided a common disturbance input for all
settings.}

\Revised{The force point was assigned to link~3 at the link-frame offset
\mbox{\(r_p\)}:}

\begin{equation}
r_p
=
\begin{bmatrix}
0\\
0\\
0.200
\end{bmatrix}
\mathrm{m}.
\label{eq:nullspace_disturbance_point}
\end{equation}

\Revised{At the initial posture \mbox{\(q_0\)}, the structural null direction \mbox{\(v_7(q_0)\)}
and the translational point Jacobian \mbox{\(J_p(q_0)\)} defined the base-frame
unit force direction \mbox{\(u_f\)}:}

\begin{equation}
u_f
=
\frac{
J_p(q_0)v_7(q_0)
}{
\lVert J_p(q_0)v_7(q_0)\rVert_2
}.
\label{eq:nullspace_disturbance_direction}
\end{equation}

\Revised{The direction was held constant throughout the trial. The commanded
disturbance-force magnitude was \mbox{\(F_d=20\,\mathrm{N}\)}. The disturbance
force \mbox{\(f_d(t)\)} was mapped by the current point Jacobian to the disturbance
torque \mbox{\(\tau_{\mathrm{dist}}(t)\)}:}

\begin{equation}
\tau_{\mathrm{dist}}(t)
=
J_p(q(t))^\top f_d(t),
\qquad
f_d(t)
=
F_d s(t)u_f.
\label{eq:nullspace_automatic_disturbance}
\end{equation}

\Revised{The scale \mbox{\(s(t)\)} increased from zero to one through a half-cosine between
\mbox{\(5\,\mathrm{s}\)} and \mbox{\(7\,\mathrm{s}\)}, remained at one until
\mbox{\(8\,\mathrm{s}\)}, and returned to zero during the following second. The
joint-torque-equivalent disturbance was limited to}

\begin{equation}
\lVert\tau_{\mathrm{dist}}\rVert_2
\leq
3\,\mathrm{N\,m}.
\end{equation}

\Revised{Four null-space settings were tested with three repetitions each. The first
setting disabled the null-space torque \mbox{\(\tau_{\mathrm{null}}\)}. The second used projected damping with
\mbox{\(d_{\mathrm{null}}=2\,\mathrm{N\,m\,s/rad}\)}. The remaining settings used
singular-value conditioning with \mbox{\(k_\sigma=1.5\,\mathrm{N\,m}\)} and
\mbox{\(k_\sigma=2\,\mathrm{N\,m}\)}, respectively. The SVD probe step, sign
deadband, and relative singular-value threshold remained at
\mbox{\(0.08\,\mathrm{rad}\)}, \mbox{\(10^{-6}\)}, and \mbox{\(10^{-4}\)}.}

\Revised{All twelve runs used the fixed pose-hold damping matrices
\mbox{\(D_p=50I_3\,\mathrm{N\,s/m}\)} and
\mbox{\(D_R=8I_3\,\mathrm{N\,m\,s/rad}\)}. The archived configuration used these
fixed matrices for pose hold.}

\Revised{The predefined task-retention limit was a peak Cartesian position error
\mbox{\(e_p\)} of
\mbox{\(2\,\mathrm{mm}\)}. The integrated projected joint motion
\mbox{\(E_N\)} measured the null-space motion during the disturbance:}

\begin{equation}
E_N
=
\int_{5\,\mathrm{s}}^{9\,\mathrm{s}}
\lVert N_q(q)\dot q\rVert_2
\,\mathrm{d}t.
\label{eq:nullspace_excursion_metric}
\end{equation}

\Revised{The integral was converted to degrees for comparison with the angular
quantities reported elsewhere. Singular-value conditioning was evaluated from
the change in the smallest singular value \mbox{\(\sigma_{\min}\)} between the initial and final
states. Cartesian task retention was evaluated from the peak position-error
norm during the pose hold. The acceptance outcome and measured differences
between the four settings are reported in the results chapter.}

\Revised{The disturbance is the torque equivalent of a commanded point force. It
provides a repeatable controller input for comparing the four null-space
settings.}


\section{Robot-Side Monitoring and Procedural Limits}
\label{sec:experimental_monitoring_limits}
\label{sec:damping_safety_limits_contact}

\Revised{Before torque control started, the robot-side collision behaviour was
configured with equal acceleration and nominal joint-torque thresholds of
\mbox{\(140\,\mathrm{N\,m}\)}. The six Cartesian force and moment components used
equal thresholds of \mbox{\(240\,\mathrm{N}\)} or
\mbox{\(240\,\mathrm{N\,m}\)}, according to the component type. These values define
the conditions for the Franka reflex response.}

\Revised{The joint-torque callback returned the calculated controller command directly
to libfranka. Robot-side monitoring supplied the reflex mechanism through the
configured joint and Cartesian collision thresholds. These values document the
operating configuration used throughout the measurements.}

\Revised{The motion sequence added several procedural limits. Surface approach was
bounded to \mbox{\(0.640\,\mathrm{m}\)}, and the transition to set-up occurred at a
geometrically defined clearance of \mbox{\(20\,\mathrm{mm}\)}. The operator gate held
the reached pose with the translational stiffness of
\mbox{\Cref{eq:experimental_gate_stiffness}} until the set-up motion was confirmed.
Set-up was bounded by a \mbox{\(5\,\mathrm{s}\)} timeout and by the alternative
\mbox{\(150\,\mathrm{N\,m}\)} change in the norm of the model-estimated external
moment after the minimum phase time.}

\Revised{The set-up stiffness and signed press reference also define a command
scale. For a completely blocked \mbox{\(0.060\,\mathrm{m}\)} displacement along the
calibrated normal, the blocked-reference normal-force scale
\mbox{\(F_{n,\mathrm{block}}\)} follows from
\mbox{\Cref{eq:experimental_setup_translational_stiffness}}:}

\begin{equation}
F_{n,\mathrm{block}}
=
0.060\,
n_s^\top K_{p,\mathrm{set}}n_s
=
48.0\,\mathrm{N},
\label{eq:experimental_blocked_normal_force_scale}
\end{equation}

\Revised{and, because \mbox{\(K_{p,\mathrm{set}}\)} is diagonal in the surface frame, a total
magnitude of the same value,}

\begin{equation}
\left\lVert
K_{p,\mathrm{set}}
\left(0.060n_s\right)
\right\rVert_2
=
48.0\,\mathrm{N}.
\label{eq:experimental_blocked_total_force_scale}
\end{equation}

\Revised{This value describes the virtual elastic command under a fully blocked
reference. The actual robot motion reduces the tracking error, and the value is
therefore a command-scale bound rather than a measured contact load.}

\Revised{The set-up moment threshold was applied to the change in libfranka's
model-estimated external moment after subtraction of the value stored at the
clearance transition. That estimate is referenced to the base frame origin, so
its magnitude is dominated by the reach of the arm rather than by the load at
the tool: at a press near \mbox{\(80\,\mathrm{N}\)} it reaches
\mbox{\(42\,\mathrm{N\,m}\)}, where the moment referenced to the TCP is about
\mbox{\(1\,\mathrm{N\,m}\)}. The threshold therefore terminated no run, and
set-up ended on its timeout throughout. It served as a controller termination
condition rather than a calibrated contact-force or contact-moment boundary. The timeout,
collision configuration, operator gates, logged torque command, displacement,
and model-estimated wrench were considered together when assessing each run.}
