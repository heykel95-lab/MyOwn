\chapter{Results and Discussion}
\label{ch:results_discussion}

The surface-contact results are presented according to the experimental cases
defined in \mbox{\Cref{ch:methodology}}, and each result is interpreted where
it appears. The signed set-up rotation
\mbox{\(\Delta\theta_{\mathrm{set}}\)} about the commanded rotation direction
is the primary controller-response quantity, as defined in
\mbox{\cref{sec:experimental_evaluation}}. The TCP-height flatness criterion
and the alignment angle \mbox{\(\theta_{\mathrm{align}}\)} calculated from the
end-effector pose provide supporting information, together with the
displacements, the commanded wrench, and the model-estimated normal load.

Repeated settings are reported by their mean and standard deviation. The
median within-setting standard deviation of the signed set-up rotation was
approximately \mbox{\(0.04^\circ\)} about \mbox{\(t_1\)} and
\mbox{\(0.14^\circ\)} about \mbox{\(t_2\)}, so differences below approximately
\mbox{\(0.1^\circ\)} are treated as being of the same order as the observed
scatter. This value is a descriptive comparison scale and not a statistical
significance limit. Larger scatter occurred at individual transition
conditions in Case~D, which are identified where they arise.

The final response was close to steady state for most tested conditions.
During the final second of set-up, the median changes in the model-estimated
normal load and the alignment angle were \mbox{\(0.10\,\mathrm{N}\)} and
\mbox{\(0.00^\circ\)}; the largest were \mbox{\(1.10\,\mathrm{N}\)} and
\mbox{\(0.60^\circ\)}. Selected time histories are therefore shown only where
they help explain the underlying controller behaviour.

The alignment angle at the beginning of set-up was not identical to the
commanded orientation offset, because the preceding orientation phase ended
with finite tracking error. Matched conditions nevertheless started
reproducibly: a commanded \mbox{\(+10^\circ\)} offset reached
\mbox{\(9.34\pm0.02^\circ\)} about \mbox{\(t_1\)} and
\mbox{\(10.04\pm0.03^\circ\)} about \mbox{\(t_2\)}. The commanded offset
identifies the experimental condition, and the measured initial value is
retained in the tables where it is required.

\section{Surface-Contact Results}
\label{sec:results_cases_a_h}

Case~A provides the zero-lever reference. Cases~B and~C quantify stiffness
sensitivity. Case~D then tests the tangential compliance-centre lever over both
surface tangents and both signs of the commanded orientation offset. Cases~E,
F, and~G examine the lever-definition frame, the tool-axis centre position,
and the commanded orientation-offset magnitude. Case~H is presented last
because it tests whether the direction rule obtained from the principal
surface tangents extends to intermediate tangent-plane directions.

\subsection{Zero-Lever Reference: Case~A}
\label{sec:results_case_a}

Case~A places the centre of compliance on the TCP. The commanded press
therefore produces no moment through a compliance-centre lever, and the
measured response provides the reference for the remaining contact cases under
the common Cartesian impedance and null-space settings.

\begin{table}[H]
\centering
\small
\caption{Case-A zero-lever response.}
\label{tab:results_case_a}
\begin{tabular}{@{}lrrrc@{}}
\toprule
Commanded orientation offset & \(\theta_{\mathrm{align,before}}\) & \(\Delta\theta_{\mathrm{set}}\)
& \(\Delta\theta_{\mathrm{align}}\) & \shortstack{Flat by\\TCP height} \\
\midrule
None & \(0.74\) & \(-0.97\) & \(-1.56\pm0.01\) & yes \\
\addlinespace
\(+10^\circ\) about \(t_1\) & \(9.32\) & \(+7.57\) & \(+5.94\pm0.03\) & yes \\
\(-10^\circ\) about \(t_1\) & \(9.43\) & \(-10.83\) & \(+7.19\pm0.24\) & yes \\
\(+10^\circ\) about \(t_2\) & \(10.07\) & \(-1.63\) & \(-1.74\pm0.17\) & yes \\
\(-10^\circ\) about \(t_2\) & \(9.59\) & \(-1.25\) & \(+1.20\pm0.19\) & yes \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\input{figures/results_case_a_bars.tex}
\caption{Set-up rotation with the centre of compliance at the TCP.}
\label{fig:results_case_a}
\end{figure}

With the centre of compliance at the TCP, the response is strongly
direction-dependent. As \cref{tab:results_case_a} records and
\cref{fig:results_case_a} plots against the commanded tangent, the positive
\mbox{\(10^\circ\)} command rotates the end effector by
\mbox{\(+7.57^\circ\)} about \mbox{\(t_1\)} but by \mbox{\(-1.63^\circ\)}
about \mbox{\(t_2\)}. Reversing the commanded orientation gives
\mbox{\(-10.83^\circ\)} about \mbox{\(t_1\)} and \mbox{\(-1.25^\circ\)} about
\mbox{\(t_2\)}. The \mbox{\(t_1\)} direction therefore already exhibits
substantial alignment-directed motion without a compliance-centre lever, while
the \mbox{\(t_2\)} direction does not.

This baseline governs how the following cases are read, because the effect of
a parameter change has to be interpreted relative to the response already
present without lever-induced coupling. The two principal surface tangents
cannot be treated as dynamically equivalent. The asymmetry is consistent with
the rectangular contact geometry and with the direction-dependent compliance of
the tool mounting: the tool face has different half-dimensions across the two
surface tangents, and unloaded clearance was observed near the mounting
direction associated with \mbox{\(t_2\)}. The experiments do not isolate the
individual contributions of contact geometry and tool-mount motion.

All five Case-A settings satisfy the TCP-height criterion. That criterion and
the alignment angle answer different questions and are not equivalent
measurements of physical tool orientation. TCP height rests on the calibrated
plane and tool geometry, whereas the alignment angle is calculated from the
measured end-effector orientation and the calibrated tool normal. A difference
between them is consistent with relative tool--gripper motion or with the
assumptions of the geometric reconstruction, and relative motion between the
tool and its mount was not measured independently.

The zero-orientation-offset condition shows why the alignment angle is kept
separate from the controller-response metric. It changes from
\mbox{\(0.74^\circ\)} at set-up entry to approximately
\mbox{\(2.30^\circ\)} after set-up although no initial orientation offset is
commanded, while the TCP-height criterion still classifies the tool as flat.
The signed set-up rotation is therefore used for the controller comparisons
that follow.

\subsection{Influence of Cartesian Stiffness}

\subsubsection{Case~B: Rotational Stiffness}
\label{sec:results_case_b}

Case~B varies the rotational stiffness about the commanded surface tangent.
The \mbox{\(5\,\mathrm{N\,m/rad}\)} condition is the corresponding zero-lever
reference from Case~A and is not a separate measurement.

\begin{table}[H]
\centering
\small
\caption{Case-B response by rotational stiffness.}
\label{tab:results_case_b}
\begin{tabular}{@{}llrrc@{}}
\toprule
Commanded orientation offset & Varied entry & Value & \(\Delta\theta_{\mathrm{set}}\) &
\shortstack{Flat by\\TCP height} \\
 & & \([\mathrm{N\,m/rad}]\) & \([{}^\circ]\) & \\
\midrule
\(+10^\circ\) about \(t_1\) & \(K_{R,t_1}\) & 5 & \(+7.57\) & yes \\
 & & 15 & \(+7.29\) & yes \\
 & & 50 & \(+2.73\) & \textbf{no} \\
\addlinespace
\(+10^\circ\) about \(t_2\) & \(K_{R,t_2}\) & 5 & \(-1.63\) & yes \\
 & & 15 & \(-1.52\) & yes \\
 & & 50 & \(-0.48\) & yes \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_B_KR.pdf}
\caption{Set-up rotation about the commanded tangent by rotational stiffness.}
\label{fig:results_case_b}
\end{figure}

Increasing the rotational stiffness about the commanded tangent suppresses the
contact-induced rotation. \Cref{tab:results_case_b} lists the response at the
three tested stiffnesses and \cref{fig:results_case_b} plots it. About
\mbox{\(t_1\)}, raising \mbox{\(K_{R,t_1}\)} from \mbox{\(5\)} to
\mbox{\(50\,\mathrm{N\,m/rad}\)} reduces the signed set-up rotation from
\mbox{\(+7.57\)} to \mbox{\(+2.73^\circ\)}. The final TCP-height
classification changes from flat at the two lower stiffnesses to tilted at the
highest value, where the TCP remains \mbox{\(3.6\,\mathrm{mm}\)} above the
flat-tool height. About \mbox{\(t_2\)}, the response stays small across the
whole tested range, changing from \mbox{\(-1.63\)} to \mbox{\(-0.48^\circ\)},
and all three settings are classified as flat.

Rotational stiffness therefore controls the resistance to alignment rotation
directly. Changing it alone does not overcome the weak zero-lever response
about \mbox{\(t_2\)}.

\subsubsection{Case~C: Cross-Axis Translational Stiffness}
\label{sec:results_case_c}

Case~C varies the translational stiffness along the surface tangent
perpendicular to the commanded rotation direction. A further condition
combines the lowest tested cross-axis translational stiffness,
\mbox{\(300\,\mathrm{N/m}\)}, with the highest tested command-axis rotational
stiffness, \mbox{\(50\,\mathrm{N\,m/rad}\)}.

\begin{table}[H]
\centering
\footnotesize
\caption{Case-C response by cross-axis translational stiffness.}
\label{tab:results_case_c}
\begingroup
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}
>{\raggedright\arraybackslash}p{0.16\textwidth}
>{\raggedright\arraybackslash}p{0.12\textwidth}
>{\raggedright\arraybackslash}p{0.11\textwidth}
rrrc@{}}
\toprule
Commanded orientation offset & Varied entries & Setting & \shortstack{Cross-axis\\translation} &
\shortstack{Command-axis\\rotation} & \(\Delta\theta_{\mathrm{set}}\) &
\shortstack{Flat by\\TCP height} \\
 & & & \([\mathrm{N/m}]\) & \([\mathrm{N\,m/rad}]\) & \([{}^\circ]\) & \\
\midrule
\(+10^\circ\) about \(t_1\) & \(K_{p,t_2}, K_{R,t_1}\) & translation only
  & 300 & 5 & \(+7.59\) & yes \\
 & & translation only & 800 & 5 & \(+7.56\) & yes \\
 & & reference & 2000 & 5 & \(+7.57\) & yes \\
 & & both varied & 300 & 50 & \(+2.80\) & \textbf{no} \\
\addlinespace
\(+10^\circ\) about \(t_2\) & \(K_{p,t_1}, K_{R,t_2}\) & translation only
  & 300 & 5 & \(-2.24\) & yes \\
 & & translation only & 800 & 5 & \(-1.78\) & yes \\
 & & reference & 2000 & 5 & \(-1.63\) & yes \\
 & & both varied & 300 & 50 & \(-1.03\) & yes \\
\bottomrule
\end{tabular}
\endgroup
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_C_KP.pdf}
\caption{Set-up rotation by cross-axis translational stiffness.}
\label{fig:results_case_c}
\end{figure}

The effect of cross-axis translational stiffness is considerably smaller.
\Cref{tab:results_case_c} separates the translation-only settings from the
combined condition, and \cref{fig:results_case_c} plots the translation-only
line; the combined condition is a separate point and not part of that line.
About \mbox{\(t_1\)}, changing the cross-axis stiffness from \mbox{\(300\)} to
\mbox{\(2000\,\mathrm{N/m}\)} changes the signed set-up rotation by only
\mbox{\(0.02^\circ\)}. About \mbox{\(t_2\)}, the same variation changes it by
\mbox{\(0.61^\circ\)}, from \mbox{\(-2.24\)} to \mbox{\(-1.63^\circ\)}. All
translation-only settings are classified as flat.

The combined condition is consistent with the individual stiffness effects
acting approximately additively at the resolution of these measurements. About
\mbox{\(t_1\)}, the additive estimate is approximately \mbox{\(+2.75^\circ\)}
against the measured \mbox{\(+2.80^\circ\)}; about \mbox{\(t_2\)}, the
corresponding values are \mbox{\(-1.09^\circ\)} and \mbox{\(-1.03^\circ\)}.
The differences of \mbox{\(0.05^\circ\)} and \mbox{\(0.06^\circ\)} lie within
the descriptive comparison scale, so no additional interaction is resolved.

Taken together, the two stiffness cases span \mbox{\(4.84^\circ\)} about
\mbox{\(t_1\)} for the rotational entry and \mbox{\(0.61^\circ\)} about
\mbox{\(t_2\)} for the translational entry. Within the tested ranges, the
stiffness variations therefore influence the response less strongly than the
compliance-centre position examined next.

\subsection{Tangential Compliance-Centre Lever: Case~D}
\label{sec:results_case_d}

Case~D is the main compliance-centre sweep. The centre of compliance is moved
along the surface tangent perpendicular to the commanded rotation direction.
Seven positions from \mbox{\(-40\)} to \mbox{\(+40\,\mathrm{mm}\)} are
tested for both surface tangents and for both signs of the commanded
orientation offset.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Case-D response by tangential centre position.}
\label{tab:results_case_d}
\begin{tabular}{@{}lrrrrrrrr@{}}
\toprule
\shortstack{Commanded orientation\\offset} & Initial & \(-40\) & \(-20\) & \(-10\) & \(0\) & \(+10\)
& \(+20\) & \(+40\,\mathrm{mm}\) \\
\midrule
\(+10^\circ\) about \(t_1\) & \(-9.32\) & \(+0.14\) & \(+0.99\) & \(+7.45\)
& \(+7.57\) & \(+7.67\) & \(+7.73\) & \(+7.87\) \\
\(-10^\circ\) about \(t_1\) & \(+9.36\) & \(-11.30\) & \(-11.18\) & \(-11.14\)
& \(-10.83\) & \(-10.36\) & \(-5.01\) & \(-0.09\) \\
\(+10^\circ\) about \(t_2\) & \(-10.03\) & \(-4.51\) & \(-1.95\) & \(-1.85\)
& \(-1.63\) & \(-1.31\) & \(-1.12\) & \(+4.43\) \\
\(-10^\circ\) about \(t_2\) & \(+9.55\) & \(-10.51\) & \(-1.51\) & \(-1.29\)
& \(-1.25\) & \(-1.29\) & \(-1.28\) & \(+4.68\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\input{figures/results_case_d_panels.tex}
\caption{Set-up rotation by tangential centre position, by commanded axis.}
\label{fig:results_case_d}
\end{figure}

The tangential compliance-centre position is the dominant geometric parameter
in the tested contact conditions. \Cref{tab:results_case_d} gives the seven
tested positions, and \cref{fig:results_case_d} separates the two commanded
axes so that the sign reversal is readable within each panel. Reversing
the commanded orientation direction reverses the side of the TCP that assists
the motion. About \mbox{\(t_1\)}, the positive command produces
\mbox{\(+7.87^\circ\)} at \mbox{\(+40\,\mathrm{mm}\)} but only
\mbox{\(+0.14^\circ\)} at \mbox{\(-40\,\mathrm{mm}\)}; the reversed command
exchanges the assisting and opposing sides, giving \mbox{\(-11.30^\circ\)} at
\mbox{\(-40\,\mathrm{mm}\)} and \mbox{\(-0.09^\circ\)} at
\mbox{\(+40\,\mathrm{mm}\)}.

About \mbox{\(t_2\)}, the lever matters most relative to the zero-lever
reference. For the positive command, the signed response changes from
\mbox{\(-1.63^\circ\)} at the TCP to \mbox{\(+4.43^\circ\)} at the assisting
\mbox{\(+40\,\mathrm{mm}\)} position, and the final configuration satisfies
the TCP-height flatness criterion. For the reversed command, the assisting end
is exchanged and gives \mbox{\(-10.51^\circ\)} at
\mbox{\(-40\,\mathrm{mm}\)}. The alignment angle calculated from the
end-effector pose does not by itself establish a residual physical tool
orientation, because relative motion between the tool and its mount was not
measured independently.

The sign dependence follows directly from the point-shifted impedance. With
the press directed approximately along the negative surface normal,
\mbox{\(f_{\mathrm{press}}\approx-F_{\mathrm{press}} n_s\)}, the coupling
moment is

\begin{equation}
    m_c=-r_c\times f_{\mathrm{press}}.
\end{equation}

For the two principal surface tangents, the selected tangential lever is

\begin{equation}
r_{c,t}=-\rho_c t_2
\quad\text{about } t_1,
\qquad
r_{c,t}=+\rho_c t_1
\quad\text{about } t_2,
\qquad \rho_c>0,
\label{eq:discussion_general_lever_selection}
\end{equation}

which gives, under the same normal-force approximation,

\begin{equation}
m_c\approx-F_{\mathrm{press}}\rho_c t_1
\quad\text{about } t_1,
\qquad
m_c\approx-F_{\mathrm{press}}\rho_c t_2
\quad\text{about } t_2.
\label{eq:discussion_general_lever_moment}
\end{equation}

The lever must therefore lie along the tangent perpendicular to the commanded
rotation direction, with a sign chosen so that the induced moment acts in the
alignment direction. Reversing the commanded orientation offset reverses the
required lever sign, which is what the four curves show.

The two tangents also differ in their sensitivity on the assisting side. About
\mbox{\(t_1\)}, positions from \mbox{\(-10\)} to \mbox{\(+40\,\mathrm{mm}\)}
all give set-up rotations between \mbox{\(+7.45\)} and \mbox{\(+7.87^\circ\)},
close to the \mbox{\(+7.57^\circ\)} zero-lever value, because substantial
alignment-directed rotation is already present there. About \mbox{\(t_2\)},
the lever is required to obtain any positive alignment-directed rotation from
the positive-command zero-lever response.

The transition between assisting and opposing behaviour is not equally smooth
at all positions. The positive \mbox{\(t_1\)} condition changes sharply
between \mbox{\(-10\)} and \mbox{\(-20\,\mathrm{mm}\)}, and the
\mbox{\(-20\,\mathrm{mm}\)} setting has a standard deviation of
\mbox{\(0.42^\circ\)}. The positive \mbox{\(t_2\)} condition at
\mbox{\(-40\,\mathrm{mm}\)} has a standard deviation of
\mbox{\(2.35^\circ\)}. These points are treated separately from the
approximately \mbox{\(0.1^\circ\)} descriptive comparison scale.

The preceding sweep establishes the final dependence on lever position. To
examine the mechanism responsible for that dependence, one representative
command direction is considered in more detail. The positive \mbox{\(t_1\)}
condition is selected because its two outer lever positions produce a large
difference in set-up rotation while the TCP-centred condition provides a
common reference.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_D_wrench.pdf}
\caption[Set-up rotation, commanded force and moment at three centre
positions.]{Set-up rotation \(\Delta\theta_{\mathrm{set},t_1}\), commanded
normal force \(F_{n,\mathrm{cmd}}\), and commanded TCP moment
\(M_{t_1,\mathrm{cmd}}\) for three centre positions. All conditions carry a
commanded orientation offset of \(+10^\circ\) about \(t_1\). The legends state the measured
orientation at the beginning of set-up.}
\label{fig:results_case_d_wrench}
\end{figure}

\Cref{fig:results_case_d_wrench} places the set-up rotation, the commanded
normal force, and the commanded TCP moment on a common time axis for three
centre positions. At \mbox{\(-40\)}, \mbox{\(0\)}, and
\mbox{\(+40\,\mathrm{mm}\)}, the commanded normal force settles in a similar
range of approximately \mbox{\(-77\)} to \mbox{\(-82\,\mathrm{N}\)}, while the
commanded TCP moment about \mbox{\(t_1\)} settles at approximately
\mbox{\(+3.28\)}, \mbox{\(+0.66\)}, and \mbox{\(-2.44\,\mathrm{N\,m}\)}. The
outer positions therefore produce moments of similar magnitude and opposite
sign while their set-up rotations differ by \mbox{\(7.73^\circ\)}. The large
difference in set-up rotation is associated primarily with the lever-induced
moment rather than with a substantial change in the commanded normal press.
The same two positions are resolved into all three surface-frame components in
\mbox{\Cref{app:additional_plots}}.

\subsection{Further Compliance-Centre Dependencies}

\subsubsection{Case~E: Lever-Definition Frame}
\label{sec:results_case_e}

Case~E tests whether specifying the same nominal \mbox{\(40\,\mathrm{mm}\)}
lever in the tool frame or in the surface frame produces the same behaviour.
The tool-fixed lever follows the end-effector rotation, whereas the
surface-fixed lever remains fixed relative to the calibrated surface.

\begin{table}[H]
\centering
\small
\caption{Case-E response by lever-definition frame.}
\label{tab:results_case_e}
\begin{tabular}{@{}lrrr@{}}
\toprule
Commanded orientation offset & Initial & Tool frame (Case~D) & Surface frame \\
\midrule
None & \(0.76\) & \(-0.06\pm0.01\) & \(-1.05\pm0.02\) \\
\(+10^\circ\) about \(t_1\) & \(9.33\) & \(+7.87\pm0.01\)
& \(+0.13\pm0.01\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{figures/MAIN_E_frame.pdf}
\caption{Case-E set-up rotation for tool- and surface-frame lever definitions.}
\label{fig:results_case_e}
\end{figure}

It does not. \Cref{tab:results_case_e} pairs the two definitions at each
command, and \cref{fig:results_case_e} shows how far apart they fall. For the
\mbox{\(+10^\circ\)} command about \mbox{\(t_1\)}, the tool-fixed definition
gives \mbox{\(+7.87\pm0.01^\circ\)} of set-up rotation, whereas the
surface-fixed definition gives only \mbox{\(+0.13\pm0.01^\circ\)}. The
surface-fixed condition is also classified as tilted by TCP height. At zero
commanded orientation offset, the two definitions produce
\mbox{\(-0.06\pm0.01^\circ\)} and \mbox{\(-1.05\pm0.02^\circ\)}. The
coordinate frame in which the compliance-centre displacement is defined is
therefore part of the controller parameter itself, because a tool-fixed lever
rotates with the tool while a surface-fixed lever stays constant in the
surface frame.

\subsubsection{Case~F: Tool-Axis Centre Position}
\label{sec:results_case_f}

Case~F asks whether displacing the centre of compliance in any direction
produces alignment, or whether the tangential component is specifically
responsible. The centre is moved along the tool axis at a fixed
\mbox{\(+10^\circ\)} command about \mbox{\(t_1\)}, so that the results are
directly comparable with the tangential sweep of Case~D at the same command.

\begin{table}[H]
\centering
\small
\caption{Case-F response by tool-axis centre position.}
\label{tab:results_case_f}
\begin{tabular}{@{}lrrrrrrr@{}}
\toprule
Position & \(-40\) & \(-20\) & \(-10\) & \(0\) & \(+10\) & \(+20\)
& \(+40\,\mathrm{mm}\) \\
\midrule
\(\Delta\theta_{\mathrm{set},t_1}\) & \(+7.26\) & \(+7.49\) & \(+7.58\)
& \(+7.57\) & \(+7.58\) & \(+7.55\) & \(+7.60\) \\
Standard deviation & \(0.13\) & \(0.01\) & \(0.01\) & \(0.01\) & \(0.01\)
& \(0.03\) & \(0.04\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\input{figures/results_case_f_comparison.tex}
\caption{Tangential against tool-axis centre displacement at the same command.}
\label{fig:results_case_f}
\end{figure}

The tangential component is responsible. \Cref{tab:results_case_f} lists the
seven axial positions with their standard deviations, and
\cref{fig:results_case_f} places the two displacement directions on one pair of
axes for the same command. The tool-axis sweep is flat across its whole range:
the signed set-up rotation changes from \mbox{\(+7.26\)} to
\mbox{\(+7.60^\circ\)} between \mbox{\(-40\)} and \mbox{\(+40\,\mathrm{mm}\)},
a span of \mbox{\(0.34^\circ\)} against \mbox{\(7.73^\circ\)} for the
tangential displacement over the same interval. Every Case-F position is
classified as flat, and the response is not strictly monotonic.

This supports the cross-product interpretation of
\mbox{\cref{eq:discussion_general_lever_moment}}: a lever component parallel
to the normal press contributes no alignment moment. For a tool inclined by
approximately \mbox{\(10^\circ\)}, an axial \mbox{\(40\,\mathrm{mm}\)}
displacement has only \mbox{\(40\sin 10^\circ\approx6.9\,\mathrm{mm}\)} of
tangential projection, which explains the weak residual dependence. The
measured variation can also arise from tangential force components, finite
rotation, evolving contact geometry, or tool-mount compliance; these
contributions are not isolated by the experiment.

\subsubsection{Case~G: Commanded Orientation-Offset Magnitude}
\label{sec:results_case_g}

Case~G asks whether the lever effect persists when the commanded misalignment
changes. The zero-lever and assisting-lever conditions are repeated at
\mbox{\(+5^\circ\)} and \mbox{\(+10^\circ\)} about both surface tangents.

\begin{table}[H]
\centering
\small
\caption{Case-G response by commanded orientation-offset magnitude.}
\label{tab:results_case_g}
\begin{tabular}{@{}lrrrr@{}}
\toprule
& \multicolumn{2}{c}{About \(t_1\)} & \multicolumn{2}{c}{About \(t_2\)} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Centre position & \(+5^\circ\) & \(+10^\circ\) & \(+5^\circ\) & \(+10^\circ\) \\
\midrule
\(\theta_{\mathrm{align,before}}\) & \(4.29\) & \(9.34\) & \(5.19\) & \(10.04\) \\
\addlinespace
\(0\,\mathrm{mm}\)   & \(+2.55\) & \(+7.57\) & \(-1.40\) & \(-1.63\) \\
\(+40\,\mathrm{mm}\) & \(+2.70\) & \(+7.87\) & \(+5.91\) & \(+4.43\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\input{figures/results_case_g_panels.tex}
\caption{Set-up rotation by commanded orientation offset, with and without the lever.}
\label{fig:results_case_g}
\end{figure}

It does. \Cref{tab:results_case_g} sets the two commanded magnitudes side by
side, and \cref{fig:results_case_g} plots the response against the commanded
offset with the two centre positions as the compared series. About
\mbox{\(t_1\)}, the assisting lever adds only \mbox{\(0.15^\circ\)} at the
\mbox{\(5^\circ\)} command and \mbox{\(0.30^\circ\)} at
\mbox{\(10^\circ\)}, because substantial zero-lever alignment is already
present. About \mbox{\(t_2\)}, the lever reverses the response at both tested
magnitudes: from \mbox{\(-1.40\)} to \mbox{\(+5.91^\circ\)} at
\mbox{\(5^\circ\)} and from \mbox{\(-1.63\)} to \mbox{\(+4.43^\circ\)} at
\mbox{\(10^\circ\)}.

The set-up rotation is not proportional to the commanded orientation offset.
The zero-lever \mbox{\(t_1\)} response changes from \mbox{\(+2.55^\circ\)} at
the \mbox{\(5^\circ\)} command to \mbox{\(+7.57^\circ\)} at the
\mbox{\(10^\circ\)} command. The commanded orientation magnitude is therefore
treated as an independent operating parameter rather than a scale factor on
the measured rotation.

\subsection{Generalisation of the Lever-Direction Rule: Case~H}
\label{sec:results_case_h}

Cases~A and~D establish the required lever direction on the principal
tangents. Case~H tests whether the same rule extends to intermediate
directions in the tangent plane. Each direction is compared with a
direction-selected \mbox{\(40\,\mathrm{mm}\)} lever and with the fixed lever
that assists the \mbox{\(t_1\)} command.

\begin{table}[H]
\centering
\small
\caption{Case-H direction-rule comparison at the common \(40\,\mathrm{mm}\) lever magnitude.}
\label{tab:results_case_h}
\label{tab:discussion_general_direction_selection}
\begin{tabular}{@{}lrrr@{}}
\toprule
Commanded rotation direction &
Selected \([r_{c,t_1},r_{c,t_2}]\) &
\multicolumn{2}{c}{\(\Delta\theta_{\mathrm{set}}\) \([{}^\circ]\)} \\
\cmidrule(lr){3-4}
 & \([\mathrm{mm}]\) & selected & fixed at \(t_1\) \\
\midrule
About \(t_1\) & \([0,-40.0]\) & \(+7.87\) & \(+7.87\) \\
\(-45^\circ\) from \(t_1\) & \([-28.3,-28.3]\) & \(+4.46\) & \(+4.39\) \\
\(+45^\circ\) from \(t_1\) & \([+28.3,-28.3]\) & \(+4.85\) & \(+4.80\) \\
About \(t_2\) & \([+40.0,0]\) & \(+4.43\) & \(-1.61\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_H_direction.pdf}
\caption{Case-H set-up rotation with the direction-selected and fixed levers.}
\label{fig:results_case_h}
\end{figure}

\Cref{tab:results_case_h} gives the lever vectors with both responses, and
\cref{fig:results_case_h} shows the two levers against each other at every
tested direction. At the two diagonal directions the selected and fixed levers
differ by only \mbox{\(0.07^\circ\)} and \mbox{\(0.05^\circ\)}, which is
within the observed scatter, because the fixed lever still has a substantial
projection along the selected direction. The clearest separation occurs about
\mbox{\(t_2\)}, where the direction-selected lever gives
\mbox{\(+4.43^\circ\)} while the fixed \mbox{\(t_1\)} lever gives
\mbox{\(-1.61^\circ\)}.

The measurements therefore support the geometric lever-direction rule of
\mbox{\cref{eq:experimental_general_lever_rule}} over the four tested
directions, while not establishing a universal best lever magnitude.

Within a similar contact geometry, the measurements suggest selecting the
compliance-centre lever before refining the stiffness values. The direction
follows from the commanded rotation, the sign is chosen so that the
press-induced moment assists alignment, and the definition frame must be
stated with the coordinates, since Case~E shows that the same nominal
coordinates in two frames give very different responses. The rotational
stiffness about the commanded axis can then be refined, and the perpendicular
translational stiffness treated as a secondary variable for this geometry.
This ordering is a practical interpretation of the present measurements rather
than a universal rule.

Across the \mbox{\(57\)} tested settings, \mbox{\(50\)} satisfied the
TCP-height flatness criterion; the seven that did not are listed in
\mbox{\Cref{app:additional_plots}}.

\section{Null-Space Pose-Hold Results}
\label{sec:results_nullspace}

The null-space pose-hold study is separate from the contact cases. Matched
trials compare the selectable null-space terms under the same internally
commanded point-force-equivalent disturbance. The commanded disturbance
reached \mbox{\(20.00\,\mathrm{N}\)} in all twelve trials, with peak equivalent
joint-torque norms between \mbox{\(2.17\)} and
\mbox{\(2.45\,\mathrm{N\,m}\)}. The torque-limit scale remained equal to one,
so the waveform was applied without clipping.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{figures/MAIN_NS_nullspace_automatic.pdf}
\caption[Null-space response during the Cartesian pose-hold disturbance
test.]{Null-space response during the Cartesian pose-hold disturbance test. In
(a), the solid curves show the mean cumulative null-space excursion and the
shaded bands indicate \(\pm\) one standard deviation across the three
repetitions.}
\label{fig:results_nullspace_automatic}
\end{figure}

\Cref{fig:results_nullspace_automatic} shows the cumulative null-space
excursion and the singular-value response across the four settings, with the
spread over the three repetitions shaded. Projected null-space damping reduced
the cumulative redundant joint excursion from
\mbox{\((7.596\pm0.916)^\circ\)} without null-space torque to
\mbox{\((5.687\pm0.228)^\circ\)} at
\mbox{\(d_{\mathrm{null}}=2\,\mathrm{N\,m\,s/rad}\)}, a reduction of
approximately \mbox{\(25\,\%\)}. The final change in
\mbox{\(\sigma_{\min}\)} became less negative, from
\mbox{\((-2.13\pm0.47)\times10^{-3}\)} to
\mbox{\((-1.26\pm0.10)\times10^{-3}\)}.

Both sigma-only settings returned \mbox{\(\sigma_{\min}\)} approximately to
its initial value by the end of the trial. The final changes were
\mbox{\((1.95\pm0.07)\times10^{-5}\)} at
\mbox{\(k_\sigma=1.5\,\mathrm{N\,m}\)} and
\mbox{\((1.93\pm0.03)\times10^{-5}\)} at
\mbox{\(2\,\mathrm{N\,m}\)}. The lower torque magnitude achieved the same final
recovery with lower cumulative excursion,
\mbox{\((0.283\pm0.007)^\circ\)} instead of
\mbox{\((1.619\pm0.130)^\circ\)}, and a lower mean peak Cartesian position
error, \mbox{\(0.889\pm0.024\,\mathrm{mm}\)} instead of
\mbox{\(0.983\pm0.053\,\mathrm{mm}\)}. The largest position error measured in
the complete pose-hold study was \mbox{\(1.304\,\mathrm{mm}\)}, below the
predefined \mbox{\(2\,\mathrm{mm}\)} acceptance limit.

The pose-hold trials isolate the projected damping and sigma-conditioning
terms under the repeatable commanded disturbance. The surface-contact
experiments use both contributions together in the combined secondary-torque
mode and therefore do not separate their individual effects during contact.
The geometric Jacobian combines translational and rotational rows without a
common length scale, so the reported \mbox{\(\sigma_{\min}\)} is used only as a
relative indicator under the fixed Jacobian convention.
