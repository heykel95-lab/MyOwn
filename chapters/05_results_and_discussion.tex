\chapter{Results and Discussion}
\label{ch:results_discussion}
\ReviewMark{purple}{The chapter was revised in full and every paragraph is
settled. No further editing is to be applied to it, and any remaining concern
is raised as a comment rather than as a change.}

\Comment{In the settled prose, use ``commanded tool orientation offset'' for
the deliberate angular command and ``commanded rotation axis'' or ``commanded
rotation direction'' for its components. Retain ``tilt'' only for a physical
inclination.}

\Revised{This chapter presents the measured response of the surface-contact tests
and complementary Cartesian pose-hold trials. The eight contact cases introduced in
\mbox{\cref{tab:experimental_test_cases}} are evaluated using the alignment change
\mbox{\(\Delta\theta_{\mathrm{align}}\)}
relative to the calibrated surface, together with the measured rotation,
displacement, and model-estimated external load. The results
are interpreted as answers to the three linked research questions rather than
as separate case-specific findings: Cases~A and~B establish the stiffness
response; Case~C tests the tilt-dependent selection rule; and Cases~D to~H
establish the compliance-centre direction, frame, and position behaviour.}

\section{Data Quality and Evaluation Basis}
\label{sec:results_data_quality}
\label{sec:results_repeatability}
\label{sec:results_alignment}

\Revised{All contact runs completed the set-up phase and were included in the analysis.
Each parameter setting was repeated three times, and the tables report the mean
and standard deviation of the corresponding repetitions. One repetition in the
\mbox{\(10^\circ\)} \mbox{\(t_2\)} condition differed substantially from the other two. It
was included so that the reported mean and dispersion represent the complete
measured response. Its possible relation to the tool mounting is discussed in
\mbox{\cref{sec:results_limitations}}.}

\Revised{The primary result is the alignment improvement
\mbox{\(\Delta\theta_{\mathrm{align}}\)}, calculated from the change during set-up in
the angle between the calibrated surface normal \mbox{\(n_s\)} and the grinding-face direction calculated from the
measured end-effector orientation. A positive value denotes a reduction of this
angle and therefore an improvement in alignment. Because the tool could rotate
by approximately \mbox{\(\pm2^\circ\)} relative to the gripper about
\mbox{\(y_{\mathrm{EE}}\)}, the quantity represents the combined response of the
robot, gripper, and tool. Measurement uncertainty is treated in
\mbox{\cref{sec:results_limitations}}.}

\Revised{Repeatability was assessed from the three repetitions of each contact
setting. In Cases~A to~C the median within-setting standard deviation of the
alignment change \mbox{\(\Delta\theta_{\mathrm{align}}\)} was
\mbox{\(0.03^\circ\)} about \mbox{\(t_1\)} and \mbox{\(0.05^\circ\)}
about \mbox{\(t_2\)}; in Cases~D to~H it was \mbox{\(0.04\)} and
\mbox{\(0.14^\circ\)}. Differences smaller than approximately
\mbox{\(0.1^\circ\)} are therefore treated as being of the same order as the
observed experimental scatter. This value is used as a descriptive comparison
scale rather than as a statistical significance limit.}

\Revised{The scatter is not uniform across the conditions. It is largest where
the contact is near the transition between correcting and not correcting, and
reaches \mbox{\(0.42^\circ\)} about \mbox{\(t_1\)} and
\mbox{\(2.35^\circ\)} about \mbox{\(t_2\)} at single positions of Case~E.
Those settings are named where they are reported, and the comparison scale
does not apply to them.}

\Revised{The reported values were calculated from settled intervals. During the final
second of set-up, the median change in the model-estimated normal load was
\mbox{\(0.10\,\mathrm{N}\)}, and the median change in the alignment angle
\mbox{\(\theta_{\mathrm{align}}\)} was
\mbox{\(0.00^\circ\)}. The largest corresponding changes were
\mbox{\(1.10\,\mathrm{N}\)} and \mbox{\(0.60^\circ\)}. For settings that kept the zero
compliance-centre lever \mbox{\(r_c\)}, the steady model-estimated normal load
\mbox{\(F_n\)} in Cases~A, B and~C remained
between \mbox{\(44.5\)} and \mbox{\(49.4\,\mathrm{N}\)}. The contact comparisons were
therefore made under similar estimated preload levels, except where the lever
position itself altered the contact response.}

\Revised{The alignment angle \mbox{\(\theta_{\mathrm{align}}\)} at the beginning
of set-up differed from the commanded angular offset. The orientation phase
leaves a steady-state error, because joint friction acts against a finite
rotational stiffness and the phase exits once the error falls inside its
tolerance. Raising the rotational stiffness about the surface tangents from
\mbox{\(90\)} to \mbox{\(180\,\mathrm{N\,m/rad}\)} reduced that error from
\mbox{\(1.3\)} to \mbox{\(0.5^\circ\)}, at the cost of doubling the
high-frequency content of the commanded joint torque during the phase, and the
larger value was not carried further for that reason.}

\Revised{The difference is neither uniform nor always a deficit. A commanded
\mbox{\(+10^\circ\)} about \mbox{\(t_1\)} arrived as
\mbox{\(9.34\pm0.02^\circ\)} and the reversed one as
\mbox{\(9.37\pm0.07^\circ\)}, both short of the command, whereas a commanded
\mbox{\(+10^\circ\)} about \mbox{\(t_2\)} arrived as
\mbox{\(10.04\pm0.03^\circ\)} and overshot it. At half the command the same
pattern holds, \mbox{\(4.29^\circ\)} about \mbox{\(t_1\)} against
\mbox{\(5.19^\circ\)} about \mbox{\(t_2\)}. What the two tangents share is the
scatter, at most \mbox{\(0.07^\circ\)} within a group, so every condition of a
comparison starts from the same attitude even where that attitude is not the
commanded one.}

\Revised{The tables therefore report the measured angle rather than the
commanded one. The commanded offset names the condition, because it is the
setting that was varied and its sign is what the direction rule is read
against, but every quantity computed from an angle uses what the tool actually
held when contact began.}

\Revised{\Cref{fig:results_setup_descent} shows the angular deviation over one
set-up phase for two compliance-centre positions. The deviation holds near its
initial value while the press builds, falls as the tool rolls onto the surface,
and settles on the residual the condition reaches. Each curve is one trial.}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_DQ_descent.pdf}
\caption{Angular deviation during set-up for two compliance-centre positions.}
\label{fig:results_setup_descent}
\end{figure}

\section{Case A: Effect of Rotational Stiffness}
\label{sec:results_case_a}

\Revised{In Case~A, only the rotational stiffness about the tilted surface tangent was
varied. The stiffness about the orthogonal tangent remained at
\mbox{\(5\,\mathrm{N\,m/rad}\)}. The \mbox{\(5\,\mathrm{N\,m/rad}\)} entries in
\mbox{\cref{tab:results_case_a}} are the reference setting, measured at a
commanded offset of \mbox{\(10^\circ\)}.}

\begin{table}[H]
\centering
\small
\caption{Case-A alignment improvement for the tested rotational stiffnesses.}
\label{tab:results_case_a}
\begin{tabular}{@{}lrrr@{}}
\toprule
Commanded rotation axis & \(5\,\mathrm{N\,m/rad}\) & \(15\,\mathrm{N\,m/rad}\)
& \(50\,\mathrm{N\,m/rad}\) \\
\midrule
\(t_1\) & \(+0.64\pm0.08\) & \(+0.57\pm0.03\) & \(+0.49\pm0.02\) \\
\(t_2\) & \(+1.06\pm0.92\) & \(+1.58\pm0.04\) & \(+1.14\pm0.03\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/MAIN_A_KR.pdf}
\caption{Case-A alignment improvement by rotation-axis stiffness.}
\label{fig:results_case_a}
\end{figure}

\Revised{For the \mbox{\(t_1\)} tests, the alignment improvement
\mbox{\(\Delta\theta_{\mathrm{align}}\)} decreased from
\mbox{\(0.64^\circ\)} at \mbox{\(5\,\mathrm{N\,m/rad}\)} to \mbox{\(0.57^\circ\)} at
\mbox{\(15\,\mathrm{N\,m/rad}\)} and \mbox{\(0.49^\circ\)} at
\mbox{\(50\,\mathrm{N\,m/rad}\)}. Increasing the rotational stiffness therefore
reduced the measured alignment response about \mbox{\(t_1\)} over the tested range.}

\Revised{For \mbox{\(t_2\)}, the highest tested stiffness produced less improvement than the
\mbox{\(15\,\mathrm{N\,m/rad}\)} setting. The reused
\mbox{\(5\,\mathrm{N\,m/rad}\)} reference contains the anomalous repetition
noted in \mbox{\cref{sec:results_data_quality}}; the remaining two repetitions
at this setting average approximately
\mbox{\(1.59^\circ\)}, close to the \mbox{\(1.58^\circ\)} measured at
\mbox{\(15\,\mathrm{N\,m/rad}\)}. The data therefore support a reduction at
\mbox{\(50\,\mathrm{N\,m/rad}\)}, while the variability of the reference prevents a
reliable ordering of the two softer settings.}

\Revised{Because the two tangent entries were varied separately, these findings
characterise the individual tilted-axis stiffness.}

\section{Case B: Effect of Translational Stiffness}
\label{sec:results_case_b}

\Revised{Case~B varied the translational stiffness perpendicular to the tilted surface
tangent. The \mbox{\(2000\,\mathrm{N/m}\)} entries in
\mbox{\cref{tab:results_case_b}} are the reference setting at a commanded
offset of \mbox{\(10^\circ\)}.
One additional condition for each tilted axis combined the lowest tested
translational stiffness, \mbox{\(300\,\mathrm{N/m}\)}, with the highest tested
rotational stiffness, \mbox{\(50\,\mathrm{N\,m/rad}\)}.}

\begin{table}[H]
\centering
\small
\caption{Case-B alignment improvement for the tested perpendicular
translational stiffnesses.}
\label{tab:results_case_b}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Commanded rotation axis & \(300\,\mathrm{N/m}\) & \(800\,\mathrm{N/m}\)
& \(2000\,\mathrm{N/m}\) & Combined condition \\
\midrule
\(t_1\) & \(+0.58\pm0.02\) & \(+0.52\pm0.01\) & \(+0.64\pm0.08\)
& \(+0.50\pm0.02\) \\
\(t_2\) & \(+1.79\pm0.09\) & \(+1.57\pm0.00\) & \(+1.06\pm0.92\)
& \(+1.20\pm0.04\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/MAIN_B_KP.pdf}
\caption{Case-B alignment improvement for the tested perpendicular
translational stiffnesses.}
\label{fig:results_case_b}
\end{figure}

\Revised{For a tilt about \mbox{\(t_2\)}, reducing the perpendicular translational stiffness
increased the measured alignment improvement \mbox{\(\Delta\theta_{\mathrm{align}}\)}. The largest value,
\mbox{\(1.79^\circ\)}, occurred at \mbox{\(300\,\mathrm{N/m}\)}. This was the largest
improvement measured with the compliance-centre lever \mbox{\(r_c\)} kept at zero. For a tilt
about \mbox{\(t_1\)}, the difference across the tested translational-stiffness range
was \mbox{\(0.12^\circ\)}, which is close to the descriptive comparison scale. The
measured translational-stiffness response was therefore axis-dependent within
the tested range.}

\Revised{The combined low-translational-stiffness and high-rotational-stiffness
condition produced less improvement than the low-translational-stiffness
condition alone. About \mbox{\(t_2\)}, the improvement decreased from
\mbox{\(1.79^\circ\)} to \mbox{\(1.20^\circ\)} when the rotational stiffness was raised
from \mbox{\(5\)} to \mbox{\(50\,\mathrm{N\,m/rad}\)}. The response at this tested
combination was therefore non-additive. Additional combinations would be
required to characterise the interaction systematically.}

\section{Case C: Generalisation Across Commanded Rotation Directions}
\label{sec:results_case_c}

\Revised{The other cases applied commanded tool orientation offsets about the
two calibrated surface tangents. In Case~C the offset was commanded in four
directions in the tangent plane,
including two at \mbox{\(45^\circ\)} between the tool-face axes. The lever was
held at \mbox{\(60\,\mathrm{mm}\)} and the offset angle at \mbox{\(10^\circ\)}
throughout. Here an aligned lever is perpendicular to the commanded
rotation axis within the tangent plane, as defined in
\mbox{\cref{eq:experimental_general_lever_rule}}. The exact surface-frame
vectors are listed in \mbox{\cref{tab:experimental_case_c_vectors}}. Case~C
evaluates this direction rule while holding the magnitude constant across the
tangent-plane directions.}

\begin{table}[H]
\centering
\small
\caption{Case-C response with the lever selected for each rotation direction
(mean \(\pm\) SD, n=3).}
\label{tab:results_case_c_rotated}
\begin{tabular}{@{}lrrr@{}}
\toprule
Commanded rotation direction & Initial angle & Alignment improvement & Fraction removed \\
\midrule
About \(y_{\mathrm{EE}}\)  & \(7.01\) & \(+2.97\pm0.12\) & \(42\,\%\) \\
\(-45^\circ\) diagonal     & \(9.17\) & \(+7.10\pm0.07\) & \(77\,\%\) \\
About \(x_{\mathrm{EE}}\)  & \(6.82\) & \(+2.27\pm0.03\) & \(33\,\%\) \\
\(+45^\circ\) diagonal    & \(7.45\) & \(+3.47\pm0.07\) & \(47\,\%\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/MAIN_C_general_pole.pdf}
\caption{Alignment response by rotation direction and normal lever coordinate.}
\label{fig:results_general_pole}
\end{figure}

\Revised{The four directions started from different initial angles, so their
improvements are not directly comparable. The fraction of the initial angle
removed ranged from \mbox{\(33\,\%\)} to \mbox{\(77\,\%\)}, as
\mbox{\cref{tab:results_case_c_rotated}} shows. The direction-selected lever
therefore assisted at every tested direction, while the size of the
response varied between them.}

\begin{table}[H]
\centering
\small
\caption{Direction-selected against fixed lever at the same rotation direction
(mean \(\pm\) SD, n=3).}
\label{tab:results_case_c_fixed}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Commanded rotation direction & Selected lever & Fixed lever & Absolute loss & Reduction \\
\midrule
\(-45^\circ\) diagonal    & \(+7.10\pm0.07\) & \(+3.70\pm0.29\) & \(3.40^\circ\) & \(48\,\%\) \\
About \(x_{\mathrm{EE}}\) & \(+2.27\pm0.03\) & \(+1.25\pm0.04\) & \(1.02^\circ\) & \(45\,\%\) \\
\(+45^\circ\) diagonal   & \(+3.47\pm0.07\) & \(+2.34\pm0.16\) & \(1.13^\circ\) & \(32\,\%\) \\
\bottomrule
\end{tabular}
\end{table}

\Revised{Holding one lever while the tilt direction turned reduced the improvement
at every tested direction. The absolute loss was
\mbox{\(1.02^\circ\)} to \mbox{\(3.40^\circ\)}, corresponding to
\mbox{\(32\)}--\mbox{\(48\,\%\)}. The paired settings began within
\mbox{\(0.04^\circ\)} of each other, so these comparisons isolate the lever
direction more closely than comparisons between unmatched initial angles. The
matched reductions therefore support selection of the lever from the
commanded tilt direction, as \mbox{\cref{tab:results_case_c_fixed}} shows.}

\begin{table}[H]
\centering
\small
\caption{Case-C response with the compliance centre moved along the surface
normal (mean \(\pm\) SD, n=3).}
\label{tab:results_case_c_normal}
\begin{tabular}{@{}lr@{}}
\toprule
Compliance-centre position & Alignment improvement \\
\midrule
\(80\,\mathrm{mm}\) above the surface & \(+2.23\pm0.15\) \\
In the surface plane                  & \(+3.05\pm0.18\) \\
At the TCP, \(r_{c,n}=0\)             & \(+2.97\pm0.12\) \\
\(40\,\mathrm{mm}\) below             & \(+3.15\pm0.03\) \\
\(100\,\mathrm{mm}\) below            & \(+2.78\pm0.04\) \\
\bottomrule
\end{tabular}
\end{table}

\Revised{Moving the compliance centre \mbox{\(p_c\)} along the surface normal
\mbox{\(n_s\)} changed the
improvement by \mbox{\(0.9^\circ\)} across the tested range, with the largest
values near the surface plane. The normal
coordinate therefore has a measurable effect at displacements of
\mbox{\(60\)} to \mbox{\(120\,\mathrm{mm}\)}, and that effect remains small
against the \mbox{\(2.27\)} to \mbox{\(7.10^\circ\)} the direction-selected
tangential lever produced across the four directions.}

\section{Case D: Response of the Contact Alone}
\label{sec:results_case_d}

\Revised{Case~D places the centre of compliance on the TCP, so the commanded
press makes no moment through a lever and what acts is the contact and the
finite rotational stiffness. The commanded offset is
\mbox{\(10^\circ\)}, applied about each surface tangent and in both signs, and
nothing else differs between those four conditions. A fifth commands no offset
at all.}

\Revised{That fifth condition fixes what the residuals of the whole campaign
are read against. With nothing commanded the tool arrives at
\mbox{\(0.74^\circ\)} and the press leaves it at \mbox{\(2.30^\circ\)}:
pressing a nominally flat tool onto the surface tilts it by
\mbox{\(1.56\pm0.01^\circ\)}. No condition can be expected to end below
that, so \mbox{\(2.30^\circ\)} rather than zero is the floor of the
arrangement, and a residual is called flat in what follows when it reaches
it.}

\begin{table}[H]
\centering
\small
\caption{Case-D alignment change with the centre of compliance on the TCP
(mean \(\pm\) SD, n=3). A residual is called flat when it reaches the
\(2.30^\circ\) the arrangement leaves with nothing commanded.}
\label{tab:results_case_d}
\begin{tabular}{@{}lrrrc@{}}
\toprule
Commanded offset & Initial deviation & Alignment change & Residual & Flat \\
\midrule
None & \(0.74\) & \(-1.56\pm0.01\) & \(2.30\) & --- \\
\addlinespace
\(+10^\circ\) about \(t_1\) & \(9.32\) & \(+5.94\pm0.03\) & \(3.38\) & no \\
\(-10^\circ\) about \(t_1\) & \(9.43\) & \(+7.19\pm0.24\) & \(2.23\) & yes \\
\(+10^\circ\) about \(t_2\) & \(10.07\) & \(-1.74\pm0.17\) & \(11.81\) & no \\
\(-10^\circ\) about \(t_2\) & \(9.59\) & \(+1.20\pm0.19\) & \(8.38\) & no \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_D_contact.pdf}
\caption{Alignment change with the centre of compliance on the TCP, by
commanded offset.}
\label{fig:results_case_d}
\end{figure}

\Revised{The two tangents do not behave alike, as
\mbox{\cref{fig:results_case_d}} shows. About \mbox{\(t_1\)} the contact
recovers most of the commanded offset on its own, \mbox{\(+5.94\)} and
\mbox{\(+7.19^\circ\)} at the two signs. About \mbox{\(t_2\)} it recovers
almost nothing at one sign and drives the tool further from the surface at the
other, from \mbox{\(10.07\)} to \mbox{\(11.81^\circ\)}.}

\Revised{The grinding face is \mbox{\(60\,\mathrm{mm}\)} half-length across
\mbox{\(t_1\)} and \mbox{\(20\,\mathrm{mm}\)} half-width across
\mbox{\(t_2\)}, so a pressing tool is supported over three times the distance
on one tangent than on the other and rocks over the short dimension. The
measured asymmetry follows that geometry rather than the commanded offset, and
it sets what the compliance centre has to achieve on each axis: about
\mbox{\(t_1\)} it can only add to a correction already present, whereas about
\mbox{\(t_2\)} it has to produce one.}


\section{Case E: Direction and Sign of the Compliance-Centre Offset}
\label{sec:results_case_e}

\Revised{Case~E moves the centre of compliance along the tangent perpendicular
to the commanded rotation axis, which is the direction
\mbox{\cref{eq:general_compliance_lever_selection}} selects. Seven positions
are sampled from \mbox{\(-40\)} to \mbox{\(+40\,\mathrm{mm}\)}, and the sign
of the commanded offset is reversed at every one of them. The positive
direction is the one that assists a positive commanded offset, so a rule that
follows the sign of the offset appears as a mirror between the two.}

\begin{table}[H]
\centering
\small
\caption{Case-E alignment change by centre position along the assisting
tangent (mean \(\pm\) SD, n=3).}
\label{tab:results_case_e}
\begin{tabular}{@{}lrrrrrrrr@{}}
\toprule
Commanded offset & Initial & \(-40\) & \(-20\) & \(-10\) & \(0\) & \(+10\)
& \(+20\) & \(+40\,\mathrm{mm}\) \\
\midrule
\(+10^\circ\) about \(t_1\) & \(9.34\) & \(-0.12\) & \(+0.70\) & \(+5.69\)
& \(+5.94\) & \(+5.84\) & \(+6.08\) & \(+6.19\) \\
\(-10^\circ\) about \(t_1\) & \(9.37\) & \(+6.22\) & \(+6.08\) & \(+6.08\)
& \(+7.19\) & \(+6.30\) & \(+4.43\) & \(-0.13\) \\
\(+10^\circ\) about \(t_2\) & \(10.04\) & \(-4.60\) & \(-2.06\) & \(-1.95\)
& \(-1.74\) & \(-1.43\) & \(-1.24\) & \(+4.18\) \\
\(-10^\circ\) about \(t_2\) & \(9.59\) & \(+7.65\) & \(+1.46\) & \(+1.25\)
& \(+1.20\) & \(+1.24\) & \(+1.23\) & \(-4.71\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/MAIN_E_sign.pdf}
\caption{Alignment change by centre position along the assisting tangent, for
both surface tangents and both signs of the commanded offset.}
\label{fig:results_case_e}
\end{figure}

\Revised{The response reverses with the sign of the commanded offset on both
tangents. About \mbox{\(t_1\)} the positive offset improves by
\mbox{\(+6.19^\circ\)} at \mbox{\(+40\,\mathrm{mm}\)} and by
\mbox{\(-0.12^\circ\)} at \mbox{\(-40\,\mathrm{mm}\)}, while the negative
offset gives \mbox{\(-0.13\)} and \mbox{\(+6.22^\circ\)} at the same two
positions. About \mbox{\(t_2\)} the same exchange holds, between
\mbox{\(+4.18\)} and \mbox{\(-4.60^\circ\)} for the positive offset and
\mbox{\(-4.71\)} and \mbox{\(+7.65^\circ\)} for the negative one. The
direction that assists is therefore selected by the signed commanded rotation
direction, and the selection is not optional: the opposing direction removes
the correction entirely on \mbox{\(t_1\)} and reverses it on \mbox{\(t_2\)}.}

\Revised{What the offset achieves differs sharply between the tangents. About
\mbox{\(t_1\)} the assisting side is flat: from \mbox{\(-10\)} to
\mbox{\(+40\,\mathrm{mm}\)} the change stays between \mbox{\(+5.69\)} and
\mbox{\(+6.19^\circ\)}, against \mbox{\(+5.94^\circ\)} with the centre on the
TCP. Displacing the centre adds nothing there that the contact was not already
delivering. About \mbox{\(t_2\)} the centre is what produces the correction at
all: it carries the response from \mbox{\(-1.74\)} to \mbox{\(+4.18^\circ\)}
at the positive offset, and from \mbox{\(+1.20\)} to \mbox{\(+7.65^\circ\)} at
the negative one. Only one condition of the sweep reaches the
\mbox{\(2.30^\circ\)} floor and leaves the tool flat, the reversed
\mbox{\(t_2\)} offset with the centre \mbox{\(40\,\mathrm{mm}\)} along its
assisting tangent, at \mbox{\(1.95\pm0.31^\circ\)}. Every other condition ends
above it, including all of those about \mbox{\(t_1\)}, whose residuals cluster
near \mbox{\(3.3^\circ\)}.}

\Revised{The transition between the two regimes is not gradual. About
\mbox{\(t_1\)} at a positive offset the change collapses from
\mbox{\(+5.69\)} to \mbox{\(+0.70^\circ\)} between \mbox{\(-10\)} and
\mbox{\(-20\,\mathrm{mm}\)}, and the three repetitions at
\mbox{\(-20\,\mathrm{mm}\)} scatter by \mbox{\(0.42^\circ\)} against a median
within-setting scatter of \mbox{\(0.05^\circ\)} elsewhere. The
\mbox{\(-40\,\mathrm{mm}\)} position of the positive \mbox{\(t_2\)} condition
scatters by \mbox{\(2.35^\circ\)} for the same reason. Near the transition the
contact resolves one way or the other from nominally identical starts.}

\Revised{\mbox{\Cref{fig:results_case_e_wrench}} reads one comparison in the
order the load acts: the deviation first, then the press along the surface
normal, then \mbox{\(M_{t_1}\)}, the moment about the commanded tangent, each as the
controller commanded it and as the robot model estimated it. The press is negative
because \mbox{\(n_s\)} points out of the plate, and it reaches
\mbox{\(-77\)} to \mbox{\(-82\,\mathrm{N}\)} in all three conditions, so they
are compared under the same normal load.}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_E_wrench.pdf}
\caption{Deviation, normal press \(F_n\) and moment \(M_{t_1}\) for three
centre positions, each commanded and model-estimated. All three carry a
commanded offset of \(+10^\circ\) about \(t_1\); the deviation each arrived
with is printed beside the start of its curve.}
\label{fig:results_case_e_wrench}
\end{figure}

\Revised{Both moments are referred to the TCP, which is the only point at
which the commanded and the estimated one compare without a contact model
between them. They track each other closely. At \mbox{\(-40\)}, \mbox{\(0\)}
and \mbox{\(+40\,\mathrm{mm}\)} the commanded moment settles at
\mbox{\(+3.28\)}, \mbox{\(+0.66\)} and \mbox{\(-2.44\,\mathrm{N\,m}\)}
and the estimated one at \mbox{\(+2.94\)}, \mbox{\(-0.32\)} and
\mbox{\(-3.54\,\mathrm{N\,m}\)}, so the environment returns within about
\mbox{\(1\,\mathrm{N\,m}\)} of what the controller asks for.}

\Revised{The two agree in sign at the outer positions and disagree only at
the centred one, where the commanded moment is \mbox{\(+0.66\)} and the
estimated \mbox{\(-0.32\,\mathrm{N\,m}\)}. Neither is the negative of the
other, because the estimate reports the external wrench in the same sense as
the command: a press directed into the plate is negative in both, so
agreement means the contact is returning what the controller applies. What
separates them is the moment the estimate does not attribute to the contact,
\mbox{\(0.34\)} to \mbox{\(1.10\,\mathrm{N\,m}\)} across the three
conditions, which is where joint friction and the error of the model itself
appear. At the centred position that gap is larger than either value, so the
sign of the smaller pair is not resolved by the measurement; at the outer
positions the moments exceed it several times over and the sign is.}

\Revised{What the centre position changes is the sign of that moment, not its
size. The magnitudes at the two outer positions are alike, and the conditions
end at \mbox{\(9.39\)} and \mbox{\(3.07^\circ\)}. The larger moment is the
one that leaves the tool tilted. Under a common press this rules out both the
load and the size of the moment as what separates the conditions, and leaves
the direction in which it acts.}

\Revised{Referring the same two wrenches to the centre of compliance instead
gives a nearly constant pair: \mbox{\(+0.66\)}, \mbox{\(+0.69\)} and
\mbox{\(+0.01\,\mathrm{N\,m}\)} commanded against \mbox{\(-0.32\)},
\mbox{\(-0.46\)} and \mbox{\(-0.29\,\mathrm{N\,m}\)} estimated. That is
the point shift working as constructed: the centre sees a block-diagonal
impedance, so the moment there is the rotational spring acting on the
orientation error and little else. The displacement does not change what the
centre carries; it changes where that point sits relative to the contact, and
with it the moment at the TCP.
\mbox{\Cref{fig:results_case_e_diagnostics}} resolves both referenced moments
and the rotation on the surface axes for the two extremes of the same
comparison.}

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{figures/MAIN_E_diagnostics.pdf}
\caption{Rotation, press force and both referenced moments during set-up for
two centre positions.}
\label{fig:results_case_e_diagnostics}
\end{figure}


\section{Case F: Frame in Which the Offset Is Defined}
\label{sec:results_case_f}

\Revised{Case~F names the same physical offset in surface coordinates instead
of end-effector coordinates. At zero commanded offset the two definitions
describe one point; as the tool turns they separate, because a surface-fixed
point keeps its direction while a tool-fixed one follows the tool.}

\begin{table}[H]
\centering
\small
\caption{Case-F alignment change by the frame the offset is named in
(mean \(\pm\) SD, n=3).}
\label{tab:results_case_f}
\begin{tabular}{@{}lrrr@{}}
\toprule
Commanded offset & Initial & Tool frame (Case~E) & Surface frame \\
\midrule
None & \(0.76\) & \(-1.09\pm0.04\) & \(-1.71\pm0.17\) \\
\(+10^\circ\) about \(t_1\) & \(9.33\) & \(+6.19\pm0.09\)
& \(-0.13\pm0.01\) \\
\bottomrule
\end{tabular}
\end{table}

\Revised{At \mbox{\(10^\circ\)} about \mbox{\(t_1\)} the two definitions
separate by \mbox{\(6.32^\circ\)}: the surface-frame definition leaves
\mbox{\(-0.13\pm0.01^\circ\)} where the tool-frame definition gives
\mbox{\(+6.19\pm0.09^\circ\)} at the same distance. Naming the offset in
surface coordinates therefore does not weaken the response, it removes it, and
removes with it the part the contact produces on its own. Resolved along the
surface axes the rotation about the commanded tangent is
\mbox{\(+0.13^\circ\)}, so the tool did not turn toward the surface at all.}

\Revised{At zero commanded offset the two definitions describe one point, so
they are expected to agree, and they do not: the tool-frame trial gave
\mbox{\(-1.09\pm0.04^\circ\)} against \mbox{\(-1.71\pm0.17^\circ\)} in
surface coordinates, a difference of \mbox{\(0.62^\circ\)}. That is six
times the descriptive comparison scale and larger than either scatter. Both
trials carry the same commanded spin, the same distance and the same
assisting direction, so the geometry does not account for it; what remains is
the tool-mount uncertainty of \mbox{\cref{sec:results_limitations}}, which
acts on the residual and not on a difference between conditions. The
zero-offset pair therefore does not deliver the consistency check it was
measured for, and the separation at \mbox{\(10^\circ\)} should be read as a
comparison between two conditions rather than as a deviation from an
established agreement.}


\section{Case G: Position Along the Tool Axis}
\label{sec:results_case_g}

\Revised{Case~G moves the centre of compliance along the tool axis at a fixed
commanded offset of \mbox{\(+10^\circ\)} about \mbox{\(t_1\)}, which the tool
arrived with as \mbox{\(9.31\pm0.03^\circ\)}. A press normal
to the surface makes no moment about a lever parallel to it, so a first-order
coupling model leaves this coordinate without effect and any dependence on it
is not produced by the normal component of the press alone.}

\begin{table}[H]
\centering
\small
\caption{Case-G alignment change by centre position along the tool axis
(mean \(\pm\) SD, n=3).}
\label{tab:results_case_g}
\begin{tabular}{@{}lrrrrrrr@{}}
\toprule
Position & \(-40\) & \(-20\) & \(-10\) & \(0\) & \(+10\) & \(+20\)
& \(+40\,\mathrm{mm}\) \\
\midrule
\(\Delta\theta_{\mathrm{align}}\) & \(+5.37\) & \(+5.77\) & \(+5.84\)
& \(+5.94\) & \(+5.94\) & \(+6.09\) & \(+6.25\) \\
Standard deviation & \(0.04\) & \(0.03\) & \(0.02\) & \(0.03\) & \(0.03\)
& \(0.06\) & \(0.09\) \\
Residual & \(3.97\) & \(3.55\) & \(3.48\) & \(3.38\) & \(3.36\) & \(3.17\)
& \(3.07\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/MAIN_G_toolaxis.pdf}
\caption{Alignment change by centre position along the tool axis.}
\label{fig:results_case_g}
\end{figure}

\Revised{The coordinate does act, and it acts monotonically: the change rises
from \mbox{\(+5.37\)} to \mbox{\(+6.25^\circ\)} across the sampled range and
the residual falls from \mbox{\(3.97\)} to \mbox{\(3.07^\circ\)}. The whole
span is \mbox{\(0.88^\circ\)}, against the \mbox{\(6.3^\circ\)} the tangential
direction of Case~E carries at the same commanded offset. The zero-moment
statement holds to first order and is not exact: a purely normal press is an
idealisation, and tangential force components, the finite rotation, the
evolving contact geometry and compliance in the tool mounting each leave the
coordinate something to act through.}


\section{Case H: Dependence on the Commanded Magnitude}
\label{sec:results_case_h}

\Revised{Case~H repeats the zero and the assisting position at half the
commanded offset, \mbox{\(+5^\circ\)} instead of \mbox{\(+10^\circ\)}, on both
tangents. It separates what the compliance centre does from what the size of
the commanded offset does.}

\begin{table}[H]
\centering
\small
\caption{Alignment change at both commanded magnitudes (mean \(\pm\) SD, n=3).}
\label{tab:results_case_h}
\begin{tabular}{@{}lrrrr@{}}
\toprule
& \multicolumn{2}{c}{About \(t_1\)} & \multicolumn{2}{c}{About \(t_2\)} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Centre position & \(+5^\circ\) & \(+10^\circ\) & \(+5^\circ\) & \(+10^\circ\) \\
\midrule
Initial deviation & \(4.29\) & \(9.34\) & \(5.19\) & \(10.04\) \\
\addlinespace
\(0\,\mathrm{mm}\)   & \(+1.53\) & \(+5.94\) & \(-1.58\) & \(-1.74\) \\
\(+40\,\mathrm{mm}\) & \(+1.63\) & \(+6.19\) & \(+3.39\) & \(+4.18\) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/MAIN_H_magnitude.pdf}
\caption{Alignment change at both commanded magnitudes, by centre position
along the assisting tangent.}
\label{fig:results_case_h}
\end{figure}

\Revised{The pattern of Case~E survives the halving. About \mbox{\(t_1\)} the
centre position changes little at either magnitude, \mbox{\(0.10^\circ\)} at
\mbox{\(5^\circ\)} and \mbox{\(0.25^\circ\)} at \mbox{\(10^\circ\)}. About
\mbox{\(t_2\)} it decides the sign of the response at both, carrying
\mbox{\(-1.58\)} to \mbox{\(+3.39^\circ\)} at the smaller offset and
\mbox{\(-1.74\)} to \mbox{\(+4.18^\circ\)} at the larger one.}

\Revised{The correction is not proportional to the commanded offset. About
\mbox{\(t_1\)} the response falls from \mbox{\(+5.94\)} to
\mbox{\(+1.53^\circ\)} when the offset is halved, so a smaller commanded
offset leaves a smaller correction rather than the same fraction of it. What
remains after set-up moves much less: \mbox{\(3.38^\circ\)} at
\mbox{\(10^\circ\)} against \mbox{\(2.76^\circ\)} at \mbox{\(5^\circ\)}. The
residual is therefore closer to a property of the arrangement than of the
commanded offset, which is the reading the assisting positions of Case~E also
support.}



\section{Null-Space Pose-Hold Results}
\label{sec:results_nullspace}

\Revised{Matched Cartesian pose-hold trials evaluated the null-space terms under a
repeatable internally commanded disturbance. The point-force equivalent reached
\mbox{\(20.00\,\mathrm{N}\)} in all twelve trials. Its peak equivalent joint-torque
norm lay between \mbox{\(2.17\)} and \mbox{\(2.45\,\mathrm{N\,m}\)}. The torque-limit scale
remained equal to one, so the requested waveform was applied without clipping.
All trials completed and were included in the analysis.}

\Revised{Projected null-space damping reduced the cumulative redundant joint excursion
from \mbox{\((7.596\pm0.916)^\circ\)} without null-space torque
\mbox{\(\tau_{\mathrm{null}}\)} to
\mbox{\((5.687\pm0.228)^\circ\)} at
\mbox{\(2\,\mathrm{N\,m\,s/rad}\)}. This corresponds to a reduction of approximately
\mbox{\(25\,\%\)} relative to the zero-torque condition. The final change in the
minimum singular value \mbox{\(\sigma_{\min}\)} also became less negative, from
\mbox{\((-2.13\pm0.47)\times10^{-3}\)} to
\mbox{\((-1.26\pm0.10)\times10^{-3}\)}.}

\Revised{In both sigma-only settings, the minimum singular value
\mbox{\(\sigma_{\min}\)} was approximately at
its initial level by the end of the trial. Its final change was
\mbox{\((1.95\pm0.07)\times10^{-5}\)} at
\mbox{\(k_\sigma=1.5\,\mathrm{N\,m}\)} and
\mbox{\((1.93\pm0.03)\times10^{-5}\)} at \mbox{\(2\,\mathrm{N\,m}\)}. Both commands
therefore achieved the same final singular-value recovery. The
\mbox{\(1.5\,\mathrm{N\,m}\)} setting did so with lower cumulative excursion,
\mbox{\((0.283\pm0.007)^\circ\)} rather than \mbox{\((1.619\pm0.130)^\circ\)}, and a
lower mean peak Cartesian position error \mbox{\(e_p\)},
\mbox{\(0.889\pm0.024\,\mathrm{mm}\)} rather
than \mbox{\(0.983\pm0.053\,\mathrm{mm}\)}. The largest position error \mbox{\(e_p\)} measured in
the complete pose-hold study was \mbox{\(1.304\,\mathrm{mm}\)}, below the predefined
\mbox{\(2\,\mathrm{mm}\)} acceptance limit.}

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{figures/MAIN_NS_nullspace_automatic.pdf}
\caption[Null-space response during the Cartesian pose-hold disturbance
test.]{Null-space response during the Cartesian pose-hold disturbance test. In
(a), the solid curves show the mean cumulative null-space excursion and the
shaded bands indicate \(\pm\) one standard deviation across the three
repetitions.}
\label{fig:results_nullspace_automatic}
\end{figure}

\Revised{Matched pose-hold trials isolate the projected damping and sigma-torque
terms under a repeatable internally commanded disturbance. The contact
measurements establish alignment performance with projected damping as the
active null-space configuration.}

\section{Discussion}
\label{sec:discussion_contact}

\subsection{Relative Influence of the Varied Parameters}
\label{sec:discussion_parameter_influence}
\label{sec:discussion_pole_height}

\Revised{The measured effect sizes differed substantially across the varied parameter
families. For \mbox{\(t_1\)}, increasing the tilted-axis rotational stiffness from
\mbox{\(5\)} to \mbox{\(50\,\mathrm{N\,m/rad}\)} reduced the alignment improvement
\mbox{\(\Delta\theta_{\mathrm{align}}\)} by
\mbox{\(0.15^\circ\)}. For \mbox{\(t_2\)}, the highest tested rotational stiffness reduced
the response by \mbox{\(0.44^\circ\)} relative to the
\mbox{\(15\,\mathrm{N\,m/rad}\)} condition. Reducing the perpendicular translational
stiffness from \mbox{\(2000\)} to \mbox{\(300\,\mathrm{N/m}\)} changed the response by
\mbox{\(-0.06^\circ\)} about \mbox{\(t_1\)} and by \mbox{\(+0.73^\circ\)} about \mbox{\(t_2\)}.}

\Revised{The compliance-centre position \mbox{\(p_c\)} produced the largest
measured changes. Across Case~E the alignment change ranges from
\mbox{\(-4.60\)} to \mbox{\(+7.65^\circ\)}, a span of
\mbox{\(12.25^\circ\)}, against at most \mbox{\(0.44^\circ\)} from either
stiffness family over its tested range.}

\Revised{Those two spans come from the two groups of
\mbox{\cref{sec:results_two_presses}} and were measured under different
presses, so the comparison is not a controlled one. It survives the objection
by its size rather than by its design. The press difference moved one matched
condition by a factor of nine; applying a factor of that order to the
stiffness span still leaves it several times below the compliance-centre one.
Within the tested configuration and parameter range the compliance-centre
position therefore had the stronger influence, and the ordering is what the
measurements support rather than the ratio.}

\Revised{The measurements support considering the compliance-centre lever
\mbox{\(r_c\)} before
refining the stiffness entries. The rotational stiffness controls the angular
resistance of the impedance, and the perpendicular translational stiffness can
modify the response for particular directions. The lever, however, directly
determines whether the normal press produces a moment that assists or opposes
the required rotation.}

\subsection{Role of the Compliance-Centre Lever}
\label{sec:discussion_compliance_lever}
\label{sec:discussion_placement}

\Revised{The sign dependence observed in Case~E follows from the point-shifted
impedance. With the press directed approximately along the negative surface
normal \mbox{\(n_s\)}, \mbox{\(f_{\mathrm{press}}\approx-F_n n_s\)}, the coupling moment is}

\begin{equation}
    m_c=-r_c\times f_{\mathrm{press}}.
\end{equation}

\Revised{The tangential lever \mbox{\(r_{c,t}\)} follows the rule derived in
\mbox{\cref{eq:general_compliance_lever_selection}}, perpendicular to the
commanded rotation axis within the tangent plane:}

\begin{equation}
r_{c,t}=-\rho_c t_2
\quad\text{about } t_1,
\qquad
r_{c,t}=+\rho_c t_1
\quad\text{about } t_2,
\qquad \rho_c>0.
\label{eq:discussion_general_lever_selection}
\end{equation}

\Revised{With \mbox{\(f_{\mathrm{press}}\approx-F_n n_s\)}, this selection gives}

\begin{equation}
m_c\approx-F_n\rho_c t_1
\quad\text{about } t_1,
\qquad
m_c\approx-F_n\rho_c t_2
\quad\text{about } t_2.
\label{eq:discussion_general_lever_moment}
\end{equation}

\Revised{The induced moment is therefore opposite to the commanded
rotation in both cases. The lever lies along
the tangent perpendicular to the commanded rotation axis. Reversing
the orientation offset reverses the commanded rotation axis and the selected
lever with it. Case~E
tested this vector rule on both surface tangents and at both signs of the
commanded offset, and Case~C four directions in the tangent plane.}

\Revised{A lever component along the tangent perpendicular to the commanded
rotation axis therefore
creates a moment about that axis. Reversing the lever reverses the moment. One
sign assists the rotation that brings the grinding face towards the surface,
whereas the opposite sign opposes it. Exchanging the roles of \mbox{\(t_1\)} and
\mbox{\(t_2\)} changes the cross-product direction, which is consistent with the
assisting lever signs being opposite for the two tilted axes.}

\Revised{The component of \mbox{\(r_c\)} parallel to the press direction contributes no moment
because its cross product with \mbox{\(f_{\mathrm{press}}\)} is zero. A centre displaced along
the tool axis is close to that case, because the tool stands nearly normal to
the surface. At a \mbox{\(10^\circ\)} tilt, a \mbox{\(90\,\mathrm{mm}\)} axial
displacement has a tangential projection of approximately
\mbox{\(15.6\,\mathrm{mm}\)}, about one quarter of the
\mbox{\(40\,\mathrm{mm}\)} tangential offset. To first order the
coordinate should therefore do little.}

\Revised{The normal coordinate nevertheless changes the response. Case~C
measured a \mbox{\(0.9^\circ\)} range along \mbox{\(n_s\)}, and Case~G a
\mbox{\(0.88^\circ\)} range along the tool axis, rising monotonically from
\mbox{\(+5.37\)} to \mbox{\(+6.25^\circ\)} across the sampled positions. The
zero-moment statement assumes a purely normal force. Tangential force
components, finite translation and rotation, the evolving contact geometry, and
compliance within the tool mounting could each make the coordinate act. The
point-shifted transformation also changes the effective rotational resistance
when a normal offset is introduced.}

\Revised{For an axial displacement \mbox{\(d\)} and a tilt angle \mbox{\(\theta\)}, the effective
tangential component is \mbox{\(d\sin\theta\)}. At \mbox{\(10^\circ\)}, an axial
displacement of approximately \mbox{\(345\,\mathrm{mm}\)} would be required to
produce a tangential component of \mbox{\(60\,\mathrm{mm}\)}. The tested axial
placement therefore generated substantially less coupling moment than the
direct tangential placement.}

\Revised{The lower model-estimated normal loads \mbox{\(F_n\)} at the assisting lever positions are
consistent with the same mechanism. A larger alignment motion allows part of
the commanded displacement to be accommodated through rotation, reducing the
remaining translational compression. This explanation remains model-based
because the external load was estimated by the robot model. Relative
tool--gripper rotation remains part of the alignment uncertainty.}

\Revised{The controller regulated pose through Cartesian impedance rather than
controlling the contact force directly. Changing the compliance-centre lever
\mbox{\(r_c\)}
therefore also changed the resulting contact load, so the measured improvement
characterises the complete coupled contact response rather than an isolated
moment effect.}

\Revised{The lever is a commanded setting and does not change sign on its own
during a run. Case~E measured the consequence of its sign directly, and at
both signs of the commanded offset. About \mbox{\(t_1\)} the response moved
between \mbox{\(+6.19\)} and \mbox{\(-0.12^\circ\)} as the centre was
carried from one side of the TCP to the other, and reversing the commanded
offset exchanged the two ends. About \mbox{\(t_2\)} the same exchange spans
\mbox{\(+4.18\)} to \mbox{\(-4.60^\circ\)}. The signed commanded rotation
direction therefore defines the assisting offset direction. For every reported
run, that direction was selected before set-up from the commanded rotation
direction.}

\subsection{What the Contact Contributes Without a Lever}
\label{sec:discussion_contact_alone}

\Revised{Case~D separates the contact from the coupling. With the centre of
compliance at the TCP the commanded press produces no moment, and what remains
is the response of the contact and the finite rotational stiffness. That
response is large about \(t_1\): \(5.94^\circ\) of the measured \(9.32^\circ\)
was recovered with the centre on the TCP, and \(7.19^\circ\) of \(9.43^\circ\)
at the reversed offset.}

\Revised{About \(t_2\) the same contact recovers almost nothing at one sign,
\(+1.20^\circ\), and increases the misalignment at the other, from \(10.07\) to
\(11.81^\circ\). The contact face is \(20\,\mathrm{mm}\) half-width across
\(t_2\) and \(60\,\mathrm{mm}\) across \(t_1\), so the two directions present
different support to a pressing tool. The measured asymmetry follows the face
geometry rather than the commanded offset.}

\Revised{This changes what the compliance centre has to achieve on each axis.
About \(t_1\) a correction is already present and the centre adds almost
nothing to it: across the whole assisting side of Case~E the response stays
between \(+5.69\) and \(+6.19^\circ\), against \(+5.94^\circ\) with the centre
on the TCP. About \(t_2\) the centre is what produces the correction at all,
carrying \(-1.74\) to \(+4.18^\circ\) at the positive offset and \(+1.20\) to
\(+7.65^\circ\) at the negative one. The same displacement is therefore
decisive on one tangent and irrelevant on the other.}

\subsection{Frame and Magnitude of the Lever}
\label{sec:discussion_lever_frame}

\Revised{Case~F separates the frame the offset is named in from the offset
itself. At \(10^\circ\) about \(t_1\) the two definitions differ by
\(6.32^\circ\): the surface-frame definition leaves \(-0.13^\circ\) where the
tool-frame one gives \(+6.19^\circ\) at the same distance, and where the
contact alone gives \(+5.94^\circ\). Naming the point in surface coordinates
removes the correction rather than weakening it.}

\Revised{The usable magnitude is set by the face dimension the tool rocks
over. About \(t_2\) an offset of \(60\,\mathrm{mm}\) carried a \(5^\circ\)
commanded offset out to a \(20.7^\circ\) excursion, and \(80\,\mathrm{mm}\)
aborted the motion on a wrist-torque reflex, which is why the reported sweeps
stop at \(40\,\mathrm{mm}\). About \(t_1\), where the face is three times
longer, the same distances stayed in range throughout. The magnitude that
suits one tangent therefore does not suit the other.}

\subsection{Implications for Controller Parameter Selection}
\label{sec:discussion_parameter_selection}

\Revised{The selection below is quasi-static. It rests on the balance between
the moment the press produces through the lever and the restoring stiffness
that opposes the rotation, both evaluated at the settled end of set-up, and it
says nothing about the transient between contact and that state. Two things
justify treating the problem this way here. The reported values were read from
settled intervals, over which the load and the angle no longer changed. And the
range over which a setting may be varied is itself bounded statically: at a
tool-axis displacement of \mbox{\(120\,\mathrm{mm}\)} the commanded torque of
the sixth joint reached \mbox{\(21.4\,\mathrm{N\,m}\)} against its limit of
\mbox{\(12\,\mathrm{N\,m}\)} and the run aborted, while across the reported
sweep that joint stayed near \mbox{\(6\,\mathrm{N\,m}\)} and rose to
\mbox{\(15.1\,\mathrm{N\,m}\)} only at the last position. A selection method
for this arrangement therefore has to respect a wrist torque before it
optimises a response.}

\Revised{The measured ordering suggests a practical sequence for selecting controller
parameters within a similar contact geometry. The compliance-centre lever
\mbox{\(r_c\)} is
considered first because it determines the direction and approximate magnitude
of the coupling moment. Its tangential direction should be perpendicular to
the commanded rotation axis, and its sign should be selected so that the induced
moment assists alignment.}

\Revised{After the lever has been selected, the rotational stiffness about the commanded
rotation axis can be refined. Within the tested range, increasing this stiffness reduced
the available alignment motion. The perpendicular translational stiffness can
then be adjusted where the measurements show a directional benefit. In the
present tests, reducing it increased the response about \mbox{\(t_2\)}, while the
\mbox{\(t_1\)} variation remained within the descriptive comparison scale.}

\Revised{This measured sequence provides an initial tuning procedure for the
investigated contact geometry and parameter range.
Cases~C and~E establish the direction and sign rule at the tested conditions,
and Cases~E and~G establish where the centre should sit along each coordinate.
On the tangent the best position depends on the sign of the commanded offset;
along the tool axis it was the largest sampled displacement, where the
response was still rising.}

\begin{table}[H]
\centering
\small
\caption{Case-C evaluation of the direction rule at a common
\(60\,\mathrm{mm}\) magnitude.}
\label{tab:discussion_general_direction_selection}
\begin{tabular}{@{}llll@{}}
\toprule
Commanded rotation axis & Selected \([r_{c,t_1},r_{c,t_2}]\) &
Alignment improvement & Fraction removed \\
\midrule
About \(y_{\mathrm{EE}}\)  & \([+54.4,+25.4]\,\mathrm{mm}\) & \(+2.97\pm0.12\) & \(42\,\%\) \\
\(-45^\circ\) diagonal    & \([+56.4,-20.5]\,\mathrm{mm}\) & \(+7.10\pm0.07\) & \(77\,\%\) \\
About \(x_{\mathrm{EE}}\) & \([+25.4,-54.4]\,\mathrm{mm}\) & \(+2.27\pm0.03\) & \(33\,\%\) \\
\(+45^\circ\) diagonal    & \([+20.5,+56.4]\,\mathrm{mm}\) & \(+3.47\pm0.07\) & \(47\,\%\) \\
\bottomrule
\end{tabular}
\end{table}

\Revised{The selected lever assisted in all four Case-C directions. The common
magnitude isolates the vector direction rule in
\mbox{\cref{eq:discussion_general_lever_selection}}. The measured fractions describe
the direction-selection response at this shared magnitude.}

\Revised{Where the sign of the initial tilt is unknown, a fixed tangential lever
can assist one direction and oppose the other, and Case~E measured the cost of
the wrong sign at both: about \mbox{\(t_1\)} the response fell from
\mbox{\(+6.19\)} to \mbox{\(-0.12^\circ\)}, and about \mbox{\(t_2\)} from
\mbox{\(+4.18\)} to \mbox{\(-4.60^\circ\)}. A placement along the tool axis
carries no such choice, because it produces no first-order moment about either
tangent, and Case~G measured its largest response at the furthest sampled
position. Matching the tangential direction to the signed offset is therefore
required only where that displacement is used. The reported controller applied
this matching during experimental set-up.}

\section{Limitations}
\label{sec:results_limitations}

\subsection{Two Presses in One Chapter}
\label{sec:results_two_presses}

\Revised{Cases~A to~C and Cases~D to~H were not measured under the same press.
The earlier group ran with a normal translational stiffness of
\mbox{\(800\,\mathrm{N/m}\)} against a commanded penetration of
\mbox{\(0.060\,\mathrm{m}\)}, the later group with
\mbox{\(350\,\mathrm{N/m}\)} against \mbox{\(0.240\,\mathrm{m}\)}, and
the steady normal load differs accordingly, \mbox{\(44.5\)} to
\mbox{\(49.4\,\mathrm{N}\)} against \mbox{\(76\)} to
\mbox{\(85\,\mathrm{N}\)}.}

\Revised{The consequence is visible where the two groups meet. A commanded
\mbox{\(10^\circ\)} about \mbox{\(t_1\)} with the centre of compliance on
the TCP appears in both: as the \mbox{\(5\,\mathrm{N\,m/rad}\)} reference
of Case~A, where it gives \mbox{\(+0.64\pm0.08^\circ\)}, and as the
positive \mbox{\(t_1\)} row of Case~D, where it gives
\mbox{\(+5.94\pm0.03^\circ\)}. The settings that differ between them are
the two above, so the ninefold difference cannot be assigned to any single one
of them.}

\Revised{No comparison in this chapter is drawn across the two groups, and
none of the conclusions rests on one. What the difference does bound is the
reach of the stiffness results: Cases~A and~B establish how the response moves
with the stiffness entries at their own press, not at the press under which the
compliance-centre cases were measured. It also qualifies the statement in
\mbox{\cref{sec:experimental_design}} that a larger press cannot be assumed
to help. Within the \mbox{\(76\)} to \mbox{\(85\,\mathrm{N}\)} of
Cases~D to~H that statement holds, and the two groups together neither support
nor contradict it, because the press did not move alone.}

\subsection{Experimental Range}

\Revised{The parameter matrix includes commanded offsets of \mbox{\(5^\circ\)}
and \mbox{\(10^\circ\)} about both surface tangents. The rotational and
translational stiffnesses were varied at three levels. The combined comparison
used one low-translational-stiffness and high-rotational-stiffness pair for each
axis. The compliance-centre comparison sampled seven positions along the
assisting tangent at both signs of the commanded offset, and six along the
tool axis. Case~C additionally
sampled four combined in-plane tilt and lever directions at one common
magnitude. These settings establish the direction and sign of the assisting
displacement and the dependence of the response on the centre position. The combined
stiffness result applies to the selected pair, and the direction result covers
the four sampled in-plane directions.}

\Revised{The measured centre positions are geometry-specific design values
defined by the tool-face dimensions, commanded press, and calibrated surface.
The transferable controller results are the rule relating lever direction and
sign to the signed tilt direction, and the finding that displacing the centre
along the tool axis improves the response without a sign to choose. The
distances are then selected for the contact geometry.}

\Revised{A fixed Panda configuration, rectangular tool, plane surface, and commanded
press trajectory provided a common contact geometry across all comparisons.
This controlled basis isolates the effects of the tested controller parameters
and defines the reference response of the complete robot--gripper--tool system.}

\subsection{Measurement and Tool-Mount Uncertainty}

\Revised{The alignment metric combines the measured end-effector orientation with the
calibrated grinding-face direction and surface normal \mbox{\(n_s\)}. This measurement chain
quantifies the combined robot--gripper--tool response. The approximately
\mbox{\(\pm2^\circ\)} rotational freedom about \mbox{\(y_{\mathrm{EE}}\)} defines the
mount-related contribution to its uncertainty.}

\Revised{For the tested mounting orientation, \mbox{\(y_{\mathrm{EE}}\)} lay approximately
\mbox{\(25^\circ\)} from \mbox{\(t_2\)}. Relative tool motion may therefore have contributed
more strongly to the \mbox{\(t_2\)} conditions than to the \mbox{\(t_1\)} conditions. The
\mbox{\(10^\circ\)} \mbox{\(t_2\)} repetition gave an alignment change
\mbox{\(\Delta\theta_{\mathrm{align}}\)} near zero. It
was included in the reported mean and standard deviation, preserving the
observed variability.}

\Revised{Within-setting scatter grew with the size of the correction on both
tangents. The largest spread about \mbox{\(t_1\)} was measured in the
\mbox{\(6.05^\circ\)} compliance-centre condition. These repeatability measurements
provide the quantitative scale for the combined alignment metric.}

\Revised{Within-setting scatter is not the only scale that bounds a comparison.
The direction of the grinding face in end-effector coordinates was measured by
seating the face on the surface at several orientations about the surface normal
and solving for the direction those seatings share. Within one such measurement
the seatings agreed to between \mbox{\(0.12\)} and \mbox{\(0.28^\circ\)}.
Repeated measurements of the same arrangement, however, differed by
\mbox{\(1.70\)} and \mbox{\(2.17^\circ\)}, and the difference appeared as the
tool settled in the mount under repeated pressing. The absolute zero of the
alignment metric is therefore known to one to two degrees, and no better.}

\Revised{This bounds the absolute residuals and not the comparisons. A constant
error in the measured face direction shifts every condition by the same amount
and cancels in a difference between conditions. The rotation since first
contact, resolved along the surface axes, is measured from joint angles alone
and carries neither the face direction nor the surface normal; it agreed with
the change in the alignment metric to \mbox{\(0.05^\circ\)} on every trial
compared. The reported changes are therefore not limited by this uncertainty,
whereas a statement that the tool ended flat within a degree would be.
\Cref{fig:results_metric_agreement} places the two quantities side by side: the
left panel overlays them for one trial, and the right panel puts the magnitude
of the reported change against the rotation measured from joint angles for
every archived trial.}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/MAIN_DQ_metric_comparison.pdf}
\caption{Agreement between the reported angular deviation and the rotation
measured from joint angles alone.}
\label{fig:results_metric_agreement}
\end{figure}

\Revised{Many conditions about \mbox{\(t_1\)} ended near a residual of
\mbox{\(3.3^\circ\)}, which invites reading that value as a limit of the
arrangement. Other conditions passed it: the reversed offset about
\mbox{\(t_1\)} with the centre on the TCP left \mbox{\(2.23\pm0.24^\circ\)},
and the assisting position about \mbox{\(t_2\)} left
\mbox{\(1.95\pm0.31^\circ\)} at the full commanded offset and
\mbox{\(1.80\pm0.06^\circ\)} at half of it. The residual is a property of the
settings rather than of the mount.}

\Revised{The model-estimated force change from the clearance transition provided the
supporting normal-load quantity. It was evaluated with the alignment metric
across matched runs. Both quantities follow a common robot-state and model
basis, preserving a consistent comparison across the contact campaign.}

\subsection{Null-Space Configuration and Real-Time Operation}

\Revised{The surface-contact results establish alignment performance with projected
damping as the active null-space configuration. Matched pose-hold trials
quantify projected damping and singular-value conditioning under an internally
commanded point-force equivalent.}

\Revised{The communication check described in
\mbox{\cref{subsec:experimental_software_computer}} passed the \mbox{\(0.90\)}
average command-success criterion before the experiments. The controller then
completed every reported contact and pose-hold trial through the nominal
\mbox{\(1\,\mathrm{kHz}\)} Franka Control Interface. The reported campaign therefore
ran to completion at the nominal control rate.}
